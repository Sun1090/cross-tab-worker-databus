# AGENTS.md — cross-tab-worker-databus

A framework-agnostic cross-tab data bus with Dedicated/Shared Worker clustering and Centrifuge support.

## Autonomous Execution and Branch Lifecycle

- Work continuously from repository evidence: inspect → choose the highest-priority executable task → implement → test → fix → verify → commit → update progress → inspect again. Do not stop merely because one task, commit, PR, release, or milestone is complete.
- Before coding, inspect the roadmap/milestones, TODO/FIXME markers, CI/build/test status, and `docs/progress.md`; create `docs/progress.md` when the repository uses no equivalent progress log.
- Prioritize blockers, failing quality/security gates, core bugs, milestone critical paths, tests/migrations, performance/CI, dependencies/security, then documentation. Fix discovered issues when feasible instead of only recording them.
- Stop only when all executable work is complete, a product decision or credential/external permission is required, an upstream dependency blocks every remaining task, or a hard tool/context limit prevents further progress.
- Use a focused topic branch and atomic Conventional/Angular commits unless this repository explicitly requires direct-to-main development. Rebase with `git fetch origin && git rebase origin/<base>`; never create merge commits, force-push, rewrite shared history, or change repository protection rules.
- **Remote topic branches are temporary PR transport, not persistent storage.** Do not push `codex/*`, `feat/*`, or any other topic branch merely for backup/checkpoints. Push one only when opening or updating its PR. After a PR is merged or closed, delete its remote branch immediately and prune stale tracking refs. Before starting another branch, audit open PRs and remote branches; finish/merge viable work and remove branches already merged.
- **Do not rebase a branch whose PR is already open; rebase instead means a new ref.** `docs/progress.md` entries all append at the same `## Next candidates` anchor, so two open PRs that touch it leave the older branch unmergeable, and fixing that requires a rebase — which cannot be pushed to the open ref without a force-push (prohibited). It has cost two superseding PRs (#248 → #250, #253 → #254). The cheap side of the trade is *sequencing*: keep at most one open PR touching `docs/progress.md` at a time, and let it merge before pushing the next one's entry. The expensive side is the escape hatch: push the rebased branch under a new name, open its PR, then close the old one with a comment naming the new ref, showing that `git diff --stat <old-head> <new-head>` accounts for every commit, and deleting the old remote branch in the same pass. Never leave the superseded PR open pointing at stale content. One consequence worth pre-empting: a rebase invalidates every commit hash the entry quotes for work *on that branch* (it happened to Phase 163's rollback line, which named a hash the rebase had replaced), so name such a commit by its subject rather than its OID — and re-read the entry's own `branch`/`commit`/count claims as the last step before the push, since a rebased tree can change all of them.
- Never push directly to `main`/`master` when the repository uses protected-branch PR review. Where direct-to-main is explicitly documented, that repository-specific rule takes precedence.
- Keep `docs/progress.md` (or the repository equivalent) current with milestone/version, status, branch/commit, completed work, changed files, verification, blockers, risks/rollback, next task, and update date.

## Quick reference

| Action | Command |
|---|---|
| Install deps | `pnpm install` |
| Build | `pnpm build` |
| Type check | `pnpm typecheck` |
| Unit test | `pnpm test` (vitest; excludes the perf gates) |
| Lint | `pnpm lint` (`eslint .`) — **not part of `pnpm check`**, and CI runs it as its own blocking step after `check`, so a green `check` says nothing about lint |
| Aggregate local gate | `pnpm check` = `typecheck` → `build` → `test` → `test:perf`. It does **not** lint, does not run `test:coverage`, and does not run `test:e2e`; the local pair to run before pushing is `pnpm check && pnpm lint`, and the full set adds `test:coverage`, `test:e2e` and the `verify:*` trio |
| Coverage / arm ledger | `pnpm test:coverage` (instruments `src/**` only, per `vitest.config.ts`; the zero-count-branch ledger is read from `coverage/coverage-final.json`) |
| Perf gates | `pnpm test:perf` (own step — absolute-ms ceilings need an unscheduled core) |
| Watch tests | `pnpm test:watch` |
| E2E test | `pnpm test:e2e` (Playwright, requires `pnpm build` first) |
| Public-surface gates | `pnpm verify:compat` (package.json `exports`/type fields vs the base tag), `pnpm verify:types` (declared export names vs the base tag + type-graph closure; requires `pnpm build`), `pnpm verify:pack` (imports every entry from the tarball) |
| Dependency audit | `pnpm audit` — a blocking step in CI's verify job, after `bench`. Locally it needs `--registry=https://registry.npmjs.org`: the mirror this repository's npm is configured to serves no audit endpoint, so an unqualified run answers nothing and reads like a clean tree. `pnpm-workspace.yaml` overrides pin the patched ranges; read its output rather than assuming a pass, and note which registry produced it |
| Micro-bench | `pnpm bench` (vitest bench; CI runs it in the verify job, so a non-zero exit does fail the build, but it asserts no thresholds — the thresholds live in `test:perf` and `bench:compare`) |
| Browser bench + its gate | `pnpm bench:browser` (builds, then drives a real browser and appends to the untracked `bench-results/` archive) → `pnpm bench:compare --fail-above-pct 50`. Deliberately **local-only**: `bench-results/` is not in git, so no CI step can see the archive. `pnpm bench:trend` regenerates `docs/benchmarks.md` + its zh mirror from that archive |
| Published-consumer gate | `pnpm verify:published` with `PUBLISHED_VERSION=<n>` — installs the *published* package from `registry.npmjs.org` (the registry is pinned in the script; CI's release workflow runs the same step after publishing) |
| Bundle example vendor deps | `pnpm build:examples` (React for `examples/react`; run by `pnpm examples` and `pnpm test:e2e`) |
| Run demo | `pnpm examples` → http://localhost:4173/examples/demo/ (adapter pages: `/examples/react/`, `/examples/vue/`) |

## Directory layout

```
src/
  index.ts                      # Public API barrel export
  core/
    cluster.ts                  # WorkerClusterRuntime — cross-tab coordination
    data-bus.ts                 # CrossTabDataBus — public API, transport lifecycle
    dedup-manager.ts            # DedupManager — bounded messageId dedup + adaptive TTL
    replay-manager.ts           # ReplayManager — ring buffers, durable persistence, retention
    environment.ts              # ClusterEnvironment adapter (port/adapter pattern)
    hash.ts                     # createOpaqueKey — 128-bit non-cryptographic hash
    routing.ts                  # Pure functions: owner selection, load balancing
    storage-batch.ts            # BatchingStorageWriter — write coalescing + backoff
    publication.ts              # parseDataBusPublication — legacy flat, envelope, and canonical shapes
    replay-persistence.ts       # createIndexedDbReplayPersistence — durable replay store (object store keyed by topic)
    replay-pruning.ts           # pruneReplayHistory — count/age pruning shared by the ring and the durable store
    version.ts                  # SDK version injected at bundle time (esbuild define)
    trace.ts                    # DataBusTraceReporter — metrics/latency diagnostics
    types.ts                    # All shared type definitions
  utils/
    constants.ts                # Shared string constants; literal types derive from them
    metadata.ts                 # publicationMetadata — {messageId,timestamp} spreading
    storage-utils.ts            # readJson/writeJson/listKeys/readAllByPrefix
    validation.ts               # Option validation asserts (replay/dedup/recovery/centrifuge)
    error-utils.ts              # serializeError/deserializeWorkerError + SerializedWorkerError
  centrifuge.ts                 # CentrifugeWorkerTransport + createCentrifugeDataBus
  centrifuge-session.ts         # CentrifugeSession wrapper
  centrifuge-protocol.ts        # Worker ↔ main-thread message protocol
  websocket.ts                  # WebSocketTransport + createWebSocketDataBus (zero-dep backend)
  hooks.ts                      # React hooks adapter (separate entry; React optional peer)
  vue.ts                        # Vue 3 composables adapter (separate entry; Vue optional peer)
  worker-mode.ts                # selectWorkerBackend capability detection + degradation
  workers/
    centrifuge.worker.ts         # Dedicated Worker entry
    centrifuge.shared.worker.ts  # SharedWorker entry
    port-reaper.ts               # PortReaper — dead-port detection
tests/
  cluster.test.ts               # WorkerClusterRuntime tests
  data-bus.test.ts              # CrossTabDataBus integration tests
  routing.test.ts               # Pure function tests (owner selection, load balancing)
  storage-batch.test.ts         # BatchingStorageWriter write coalescing + backoff
  storage-utils.test.ts         # readJson/writeJson/listKeys/readAllByPrefix failure tolerance
  storage-channel.test.ts       # createStorageEventChannel probes + the frame fallback
  dedup-manager.test.ts         # DedupManager accept/suppress, adaptive TTL, disabled instance
  replay-manager.test.ts        # ReplayManager rings, pruning, retention, suspend
  replay-persistence.test.ts    # createIndexedDbReplayPersistence round trip + stale-signal teardown
  publication.test.ts           # parseDataBusPublication legacy, envelope, canonical shapes
  error-utils.test.ts           # serializeError/deserializeWorkerError + describeFailure
  hash.test.ts                 # createOpaqueKey determinism + collision properties
  worker-mode.test.ts           # selectWorkerBackend capability detection + degradation
  port-reaper.test.ts           # PortReaper adaptive cadence + session timeout
  environment.test.ts           # createBrowserEnvironment probes + getOrCreateTabId
  trace.test.ts                 # DataBusTraceReporter caps, percentiles, sink isolation
  centrifuge.test.ts            # CentrifugeWorkerTransport backend selection + lifecycle
  centrifuge-session.test.ts    # CentrifugeSession subscribe/publish/stop protocol
  websocket.test.ts             # WebSocketTransport lifecycle + frame protocol
  stability.test.ts             # Owner-handoff ACK validation against the route record
  property.test.ts              # selectActiveWorkers invariants (subset, bound, never throws)
  regression.test.ts            # Locale-independent routing tie-break
  coordination-invariants.test.ts # Multi-tab seeded fuzz: one owner, no orphan, once-per-publication, no plaintext
  storage-writers.test.ts      # Every durable write in src/ enumerated, and the topic-bearing ones are the two opt-ins
  lifecycle-invariants.test.ts  # Interleaving sweep over bus/cluster/transport flags
  hooks.test.tsx                # React hooks (jsdom + @testing-library/react)
  vue.test.ts                   # Vue composables (jsdom, app lifecycle, status mirror)
  demo-centrifuge-server.test.ts # Demo Centrifugo hub contract
  demo-ws-server.test.ts        # DemoWsBusHub exact + wildcard fan-out
  dual-format.test.ts           # ESM + CJS dist artifacts expose the public API
  verify-packed-consumer.test.ts # Tarball entry points + export-target flattening
  release-version.test.ts       # Tag/version/notes gate + CHANGELOG-vs-registry completeness
  publication-lag.test.ts       # the lag sweep's attempt selection + named exclusions, on a stub `gh`
  version-compat.test.ts        # Handoff protocol across throwaway git repos + spawned gate
  workflows.test.ts             # CI/release workflow text gates (lint, retry budget, job order)
  documentation.test.ts         # Shipped-prose gates: coverage, citations, en/zh parity
  bench-compare.test.ts         # bench:compare threshold parsing + regression detection
  bench-trend.test.ts           # bench:trend report generation + which clock stamps a report
  bench-browser.test.ts         # Browser bench env parsing + the documented defaults
  bench/cluster.bench.ts        # Cluster coordination micro-benchmarks (`pnpm bench`)
  bench/data-bus.bench.ts       # DataBus hot-path micro-benchmarks
  bench/routing.bench.ts        # Routing micro-benchmarks
  perf-gate.test.ts             # Absolute-ms hot-path ceilings — own step, `pnpm test:perf`
  setup.ts                      # Restores real timers after every test (budget-clock hygiene)
  fakes.ts                      # Shared test doubles (MemoryStorage, ChannelHub, FakeTransport, etc.)
e2e/
  demo.spec.ts                  # Playwright multi-tab browser E2E
  adapters.spec.ts              # Vue composables driven in real tabs (examples/vue)
  topics.ts                     # uniqueTopic() — a topic is also a server-wide channel
docs/
  architecture.md               # Detailed design doc (English + Chinese)
  api.md                        # Public API reference
  configuration.md              # TTL, worker modes, security model
  getting-started.md            # Installation & usage
scripts/                        # build, bench, demo servers, release verification (`pnpm <script>` names the file)
examples/                       # demo/ (vanilla), react/, vue/ — `pnpm build:examples` bundles the vendor deps
```

## Architecture principles

1. **Port/adapter pattern** — `ClusterEnvironment` abstracts browser APIs (localStorage, BroadcastChannel, timers, lifecycle). Core never touches `window`/`document` directly. Tests inject fake environments.
2. **Pure function routing** — `routing.ts` is side-effect-free. Owner selection, load balancing, and active worker filtering are pure reduce/sort operations.
3. **Write coalescing** — `BatchingStorageWriter` merges same-task writes to localStorage and flushes in one microtask. Quota failures retry with exponential backoff (50ms → 1600ms). Heartbeat writes (3s interval) + route/subscriber updates would otherwise thrash storage.
4. **Sticky routes** — Topics keep their owner until the owner's heartbeat expires. Load and visibility only influence placement of *new* routes — never migrate existing ones.
5. **Strict handoff** — `generation` monotonic version + `handoffFromWorkerId` + `ROUTE_RELEASED` ACK protocol prevents overlapping subscriptions during pagehide. Uncontrolled exit (crash) falls back to heartbeat-TTL recovery.
6. **Degradation** — Storage unavailable → local mode (no cross-tab coordination). BroadcastChannel unavailable → same. Both degrade gracefully, the local transport remains usable.

## Storage key structure

```
cross-tab-worker-databus:{clusterHash}:worker:{workerId}
cross-tab-worker-databus:{clusterHash}:route:{topicKey}
cross-tab-worker-databus:{clusterHash}:subscriber:{topicKey}:{tabId}
```

`clusterHash` = `createOpaqueKey(clusterKey)`. Topic plaintext never appears in localStorage keys. BroadcastChannel control messages carry topic plaintext in memory only.

Neither does it appear in the coordination plane's own *values*: every key and every record the cluster persists is derived from `topicKey`, which the multi-tab sweep in `tests/coordination-invariants.test.ts` enforces on both sides — it plants a per-seed marker in each topic name and searches the whole registry for it after every step, not only on the quiesced end state, because a record deleted again by the next reconcile would leave the converged state clean while the plaintext was readable in between. Two mutations fix the halves apart, and the numbers say which half had no gate: a route record carrying a `topic` field beside its `topicKey` fails **1 test in 969** (that sweep alone), while a plaintext-bearing key also trips two pre-existing cases that are about something else — a `clusterKey` namespace-containment assertion and the pagehide leftovers assertion. The two documented opt-ins that *do* write plaintext are outside the sweep by construction, since it drives the BroadcastChannel-shaped hub and installs no persistence adapter: `channelFallback: 'storage-event'` (whole frames) and `replay.persistence` (the object store is keyed by the plaintext topic).

That leaves exactly one way to add a *third* one — a new `setItem`/`.put`/`writeJson` in `src/` — and `tests/storage-writers.test.ts` is what makes it a decision rather than an accident: it pins the set of durable write sites to a table, so an unlisted writer fails the build, and it re-derives which of those sites carry a topic, so a row cannot be added quietly. Read that file's `TOPIC_CARRIER` note before extending it: the obvious word test was measured *passing* a leak that travelled through a variable named `topicKey`, which is why the term that carries the weight is `knownTopics`, the one map that turns a hash back into a name.

## Opaque key design

- `createOpaqueKey()` is a non-cryptographic 128-bit hash (four-lane MurmurHash-style mixing).
- Each topic gets a stable `topicKey`. The reverse mapping (`topicKey → topic`) is held in the `knownTopics` in-memory cache, whose FIFO eviction scan is capped at `MAX_KNOWN_TOPICS = 500` **entries this worker does not own** — when every entry is owned the scan declines to drop anything and the cap slips. Measured: 1,200 adopted peer `CONTROL/SUBSCRIBE` frames grew `knownTopics` and `assignedTopics` to 1,200 together, and one reconcile tick took both back to 0. For that flood the memory bound is the sweep, not the cap.
- `knownTopics`' eviction scan skips two candidates, and both are load-bearing for different reasons. A key the worker still owns (`this.assignedTopics.has(candidate)`), because the storage-less fallback path recovers its plaintext only from here; and the key being remembered (`candidate === topicKey`), because a fresh `Map.set` lands at the **back**, so the front-scan reaches it only in the all-owned state above — and there deleting it would discard the mapping the call exists to install. Both are pinned by named cases in `tests/cluster.test.ts`; the second was live and unpinned until 0.21.34, when deleting it was found to leave the whole suite green.
- `isAssigned()` deliberately recomputes the hash via `createOpaqueKey` rather than calling `rememberTopic()` — it's a read-only query that must not populate the cache.
- `clusterKey` defines the cluster boundary: different clusterKeys = fully isolated storage and BroadcastChannel namespaces. One exception, in the derivation rather than the hash: `options.clusterKey || '__default__'` means an **empty** key is hashed as the literal `'__default__'`, so `''` and `'__default__'` are the same cluster (pinned by `tests/cluster.test.ts`'s 'treats an empty clusterKey as the default cluster…').

## Wildcard topic subscriptions

- Patterns are topic strings: `*` (match everything) or a `prefix.*` suffix wildcard. `topicMatchesPattern(pattern, topic)` in `routing.ts` is the single source of matching truth (segment-boundary prefix, so `chat.*` does not match `chatter.1`).
- A pattern subscription flows through routing/ownership/transport as a literal channel — zero special-casing in the cluster coordination plane.
- Only the dispatch gates understand patterns: `cluster.isAssigned(topic)` (owner fan-out gate), `cluster.hasLocalSubscriber(topic)` (local gate), and `data-bus dispatch()` (handler iteration). Servers either deliver publications tagged with concrete topics (pattern-aware, recommended) or with the pattern itself (exact path).

## Testing guide

The project uses Vitest with fake environments. Key infrastructure in `tests/fakes.ts`:

| Utility | Purpose |
|---|---|
| `MemoryStorage` | In-memory `StorageLike` implementation. Use in place of localStorage. Provides `entries()` for inspecting all persisted keys and `failNextWrites(n)` to make the next n `setItem` calls throw — only writes fail, so a test that needs a *lost deletion* stages it through the write whose flush the failure cut short. |
| `ChannelHub` | Simulates BroadcastChannel between runtimes. `dropNextControl()` drops the next CONTROL message to simulate channel loss. `failNextPost()` makes the next `postMessage` throw. |
| `createFakeEnvironment()` | Creates a `ClusterEnvironment` with controllable time, storage, channels, and lifecycle. Returns `.runIntervals()`, `.pageHide()`, `.pageShow()`, `.setVisibility()` for manual control. |
| `FakeTransport` | Minimal `DataBusTransport` implementation. Track `subscribeCalls`, `unsubscribeCalls`, `publishCalls`. `startShouldFail` makes `start()` report error. `stopGate` delays stop. `emit(topic, data)` simulates an incoming message; `emitError(error)` simulates an asynchronous runtime failure. |
| `expectRejectionMessage()` | Assert a promise rejects with a real `Error` whose message contains a string. Use this whenever the *message* is the behaviour under test. |

**Typical test pattern:**

```ts
const storage = new MemoryStorage();
const hub = new ChannelHub();
let now = 1_000;
const env = createFakeEnvironment({ storage, hub, now: () => now, randomId: 'test' });
const runtime = new WorkerClusterRuntime({
  clusterKey: 'test-cluster',
  environment: env.environment,
  tabId: 'tab-test',
  workerId: 'worker-test',
  handlers: { onControl: vi.fn(), onEvent: vi.fn() }
});
runtime.start();
runtime.subscribe('topic');
await Promise.resolve();        // let microtask flush settle
env.runIntervals();              // trigger heartbeat + reconcile
env.pageHide();                  // simulate pagehide
env.pageShow();                  // simulate pageshow
```

**Simulating message loss:** `hub.dropNextControl()` — the next CONTROL posted to the BroadcastChannel is silently dropped. The test then verifies that the retry logic (reconcile loop) recovers the lost assignment.

**Testing startup failure:** `transport.startShouldFail = true` — the transport reports `onStatus('error')` during start. The test verifies that `CrossTabDataBus` surfaces the error via `ready()` rejection and does not leave the transport in a half-started state.

## BroadcastChannel protocol

Four message types on one channel per cluster:

| Type | Direction | Purpose |
|---|---|---|
| CONTROL | point-to-point (A → B) | SUBSCRIBE / UNSUBSCRIBE / PUBLISH |
| EVENT | broadcast (owner → all tabs) | Fan-out a publication |
| REGISTRY | broadcast | Trigger immediate reconciliation |
| ROUTE_RELEASED | point-to-point (old owner → new owner) | Handoff ACK |

The channel is unauthenticated — any same-origin script can post into it — so a receiving runtime
treats frame fields as attacker-controlled except where it checks them. Two invariants carry that
weight: `topicKey` must equal `createOpaqueKey(topic)`, because ownership is authorized by the route
stored under `topicKey` while the transport is named by `topic`, and a mismatch is therefore a
substitution rather than a variant; and `targetWorkerId` must name this worker. Both point-to-point
handlers that carry the pair enforce it — `handleControlMessage` and
`handleRouteReleasedMessage`, which is the one that *completes* a handoff, authorizes itself against the
durable route under `topicKey`, and then writes `topic` into `assignedTopics` and hands it to the
transport. `EVENT` and `REGISTRY` need no such check because they carry no key/plaintext pair, and the
sender of an ACK can itself only be poisoned through those two handlers, which is why checking the
receiver is enough: a durable route record stores `topicKey` and never the plaintext, so the key → name
mapping exists only in `knownTopics`/`assignedTopics` in memory and cannot be injected through
localStorage. When adding a field the receiver acts on, ask which of those two
it belongs to, and pin the forged-frame case — `tests/cluster.test.ts`'s
"drops a control frame whose topicKey disagrees with its topic" is the shape, and it must fail when the
guard comes out.

A third check is the *shape* of an optional field rather than its value: a `CONTROL/PUBLISH` frame that
carries `items` at all must carry a non-empty array of them, because `publishBatch()`'s
`Array.prototype.map` is the only producer. Both halves of that sentence are load-bearing, and they are
load-bearing on different legs — dropping a non-array is what keeps a `{ length: 2 }` value from throwing
out of the message listener and a `"ab"` string from becoming two publications of `undefined`, while
requiring non-empty is only visible when the runtime has an `onPublishBatch` handler (without one the loop
runs zero times either way). Presence and emptiness are checked together for the same reason: a frame with
`items` has no `data`, so letting an unusable batch fall through to the single-publication tail makes the
owner publish `undefined`. Do not extend this to validating item *content*: `{ data: anything }` is what a
legit batch looks like, so per-item shape checks would stop nothing a well-formed frame cannot do, and
`messageId`/`timestamp` are forwarded only into `transport.publish()` — the receiving side already drops a
non-string id and a non-finite timestamp in `parseDataBusPublication`.

`EVENT` is deliberately *not* held to that standard, and this is decided rather than an oversight: it
checks `eventType === PUBLICATION_EVENT` and the minimum payload shape (a `topic` string), and forwards
the rest. Delivery then requires `cluster.hasLocalSubscriber(topic)` and reaches `dispatch()`, so the
blast radius of a forged frame is "a publication appears on a topic this tab already subscribes to, and
lands in that topic's replay ring buffer" — strictly weaker than what a same-origin script can already do
by calling the page's own bus. A forged `CONTROL` frame was different in kind, not in degree: it wrote
*shared* coordination state (ownership, the durable route confirmation, the transport subscription name)
that other tabs then observe. Do not "harden" `EVENT` with an `isAssigned`/route gate: it buys nothing
against the same-origin threat and would break the older and newer SDK versions the shape-only check
exists to tolerate. Reopen this only if `EVENT` payloads gain reach beyond local handlers plus that
topic's own replay buffer.

## Reconcile loop (3s default)

Every heartbeat tick: prune stale workers (TTL), orphaned subscribers (no live tab), orphaned routes (no subscribers + expired TTL); repair a confirmed route that names this worker after its assignment was cleared (`reconcileStrandedSelfRoutes()` — reclaim when it still subscribes the topic, release the record otherwise); refresh role; re-send unconfirmed CONTROL/SUBSCRIBE; recompute load. Write to worker/route/subscriber storage also broadcasts a REGISTRY nudge so peers reconcile immediately instead of waiting for the next heartbeat — and a *deletion* has to `flushStorage()` first, because the nudge's whole purpose is a peer reading the repaired state, which it is not if the write is still queued in the deleting worker's own batching writer (`pause()` and `handoffAssignedTopics()` carry the same rule; the pin is a synchronous post-pass read).

## Key internal state (see docs/architecture.md)

| Concept | Section | Summary |
|---|---|---|
| Three-tier subscription | `### Subscription state layers` | `topicHandlers` → `subscribedTopics` → `assignedTopics` → `transportSubscribedTopics` |
| Lifecycle state machine | `## Lifecycle State Machine` | `started`/`stopping`/`suspended`/`transportReady` + promise gates |
| Cache key design | `### In-memory topic key cache` | `knownTopics` FIFO eviction, storage-less dependency |
| Dispatch flow | `### Dispatch flow: three gates` | `isAssigned` → `broadcastEvent` → `hasLocalSubscriber` |

## Testing conventions

- Unit tests use Vitest with `createFakeEnvironment()` + `MemoryStorage` + `ChannelHub`.
- `ChannelHub.dropNextControl()` simulates BroadcastChannel message loss.
- `FakeTransport.startShouldFail` / `FakeTransport.stopGate` simulates transport failure/async stop.
- Fake environment exposes `runIntervals()`, `pageHide()`, `pageShow()`, `setVisibility()` for lifecycle control.
- Two traps that make a coordination test pass while proving nothing. (1) A peer runtime that shares the `ChannelHub` reacts to the teardown it is supposed to observe: a "tab that died without releasing" must own its hub (shared `storage`, isolated control plane), or its own reconcile re-elects the route and the assertions hold with the code under test deleted. (2) Worker-record and route writes go through `BatchingStorageWriter`, so a peer started without an intervening `await Promise.resolve()` is invisible to the others — an empty active-worker set silently turns "did not migrate" into "had nowhere to migrate".
- When a mutation "passes" the new test, check that the mutant hit the code you meant to break before deleting the test. `publish()` and `publishBatch()` contain textually identical wildcard-probe blocks, so a non-global `perl -0pi -e 's/…/…/'` edits only the first twin and the probe actually under test stays intact. Same for the other duplicated guards; prefer an explicit `/g` plus a `git diff --stat src/` that shows the expected number of hunks.
- Diff the mutant itself before believing what it kills. A mutation that deletes a whole statement instead of only its guard ("remove `if (this.started) this.writeRecord(true);`" → drop the guard, keep the call) makes *unrelated* tests fail — here, four routing/TTL cases that depend on a record ever existing — while the new test passes, which reads as "this leg is already pinned". Both readings were wrong: the real mutant failed exactly one test. Print the diff (or an anchor `assert` on the line being changed) and check it removes only the condition under study; keep the intended-mutant and the accidental-mutant results separate in the record. **The same failure can land in a leg whose whole job is to be green, and it reads as a finding rather than as a broken instrument.** Forcing `pending` to reject by *replacing* that variable, instead of appending a link to the real `startPromise ?? pendingStop ?? Promise.resolve()`, failed three tests in both of the absorb probe's green legs — the two that exist to show "nothing observes this". A note that was in fact correct would have been rewritten as stale on that evidence. So when a leg is a control, treat any failure in it as an indictment of the construction first: check what promise the test actually waits on before believing the mutation.
- **A mutant that was never inserted reports as a surviving mutant.** The mirror of the bullet above: there the diff was too big, here it was empty. A batch of operand deletions in `src/utils/validation.ts` substituted each predicate through an unquoted pattern, so `!Number.isSafeInteger(value)` — `(`, `)` and `.` all pattern syntax — matched nothing on three of six legs, and the suite answered **936 passed** against a file byte-identical to HEAD. That is indistinguishable from the result the pass was looking for (a leg no test pins), and only the runner's own `applied=<count>` line exposed it. So a mutation harness must print whether the substitution landed, *before* the run, and assert it, and **the counter has to key on changed lines rather than added ones**: `diff pristine mutant | grep -c '^>'` reads 0 for a pure deletion, which is most of what an operand-removal probe does. Writing exactly that recipe into a harness produced three `VOID — mutant not applied` reports for three mutants that had applied and were simply not adding a line; the same three died to named tests once the count became `grep -c '^[<>]'`. A probe whose "did it land" check is wrong is a probe reporting its own instrumentation as a result — and notice which way that failure biases: it manufactures *surviving* mutants, i.e. gaps that were never there, which is precisely the finding a reader will act on. Same rule as the un-diffed mutant above, one level up: check the instrument against a case whose answer you already know. And the fix for the escaping failure stays: substitute with a literal, line-targeted replacement, never an unquoted pattern. Where a batch reports several greens in a row, check the insertion counts before believing any of them.
- A redundant re-read of a field is load-bearing in the wrong direction: it can never decide anything at runtime, and it is exactly what makes the guard it follows look untestable. `trace.ts`'s `metricsActive` getter documents its `!stopped` term as contractual, and two of its seven readers appended `|| this.stopped` to the guard that had just consulted the getter. Measured both ways: with the repeats in place, deleting the getter's term left the whole suite green; with them removed, the same deletion fails two. So when a comment claims a term is part of a guard's contract, grep that field's readers in the same file before believing either the claim or its absence, and let the deletion be the pin — a repeat that cannot decide anything has no defensive reading to keep. The same probe is what makes a *removed* guard honest: report which tree each mutant was run against, because the two kill sets are the finding.
- Measure a disjunction operand by operand, not by whether the guard runs. Every operand of `flushNow`'s activity check (`received > 0 || dispatched > 0 || dedupAccepted > 0 || dedupSuppressed > 0`) is evaluated on every flush, so the coverage report says nothing about it, and deleting either dedup term kept the whole suite green. The trap that made the leg look covered: the one test touching those counters raises **both** in a single window, so whichever operand survives still emits — a joint test pins the sum, never the halves. Same shape in `websocket.ts`, where three of four listener guards had a staleness operand that decided nothing any test could see. So: enumerate the single-operand deletions before concluding a chain is pinned, and when the operands count comparable events ask which *one-event* window proves each. Then check reach separately from decidability — `DedupManager.isDuplicate` runs one statement before `trace.recordReceived`, so a suppression-only window is reachable through the bus while an acceptance-only window is not, which no assertion on the reporter can show and which decides whether an unpinned operand is a live gap or a dormant one.
- A **state** guard has no input vector to differential, so run a flag-vector census instead: instrument the guard to record the operand-relevant fields at every evaluation, run the whole suite once, and compare the observed rows against the single assignment each surviving operand needs to be the decider. `data-bus.ts`'s `stop()` early return and its recovery-timer guard hold eight operands between them; two were pinned, and across the 20521 `stop()`-guard evaluations and 50 timer-callback entries recorded when that census was re-run on 2026-09-25, the other six premises occurred **zero** times. That count is only half the proof — it says no test gets there, not that the state cannot be reached — so pair it with the assignment enumeration (which sites install the field, and whether any caller boundary exists between them), and name at the site the future *edit* each kept leg is held against. When a premise row does occur but its deletion still passes everything, print a short stack on first sight of it: that names the **production path** reaching the guard, which is what turned `runTransport()`'s `!this.stopping` from a surviving mutant into a pinned behavior (the row came from `unsubscribeTransport()` ← a control frame ← `WorkerClusterRuntime.stop()`'s handoff, and the test needed a *second* tab, because with no remaining subscriber the handoff drops the route instead of posting a frame). **A first-sight stack names a path; it does not establish that the path is the only one**, and the shipped note had written "the only thing in the suite that reaches it". Widening the capture to every evaluation of that row and counting distinct signatures settled it — 2520 evaluations, exactly one stack (`beginStop()` → `stop()` → `pause()` → `handoffAssignedTopics()` → `onControl()` → `unsubscribeTransport()`) — so if a sentence is going to say *only*, capture at every occurrence rather than the first, and report the signature count. Two corollaries found doing it. A leg can be dominated by a statement **two lines away** rather than by another operand: the recovery timer's three dead legs are enforced by its own single arming site (`updateStatus()`'s `status === ERROR && this.started && !this.stopping`) and by `cancelScheduledRecovery()` in `beginStop()`/`suspendTransport()`, not by anything inside the `if` — so a second arming site is what would revive them, and that is what the comment must say. And a census that reports through `console.warn` breaks any teardown test that shims `console.warn` to throw: exactly one test failed under the instrumented run, for that reason, which is a second live demonstration of the hazard that file's own comment records and a warning that the probe's own failure is data too.
- A surviving mutant has two very different meanings, and a differential settles which. When deleting an operand keeps the suite green, ask whether that operand can be the *first true* term for any input, then prove it: run one fixed input vector against the original and against the mutant and compare the accept/drop results. `environment.ts`'s storage-event envelope guard has two such operands — `typeof parsed !== 'object'`, which is never first because the next test rejects every non-null primitive, and `!parsed`, which *is* first for `null` but whose deletion is absorbed by the surrounding `catch` returning identically — and 23 stored values produced a byte-identical vector for all three sources. Two obligations follow. Name which of the three verdicts each operand has (pinned / cannot decide / live and unpinned), because only the last one is a gap; and when you keep a cannot-decide leg, write the *future edit* that would make it load-bearing — the two above are held against the parse and the property reads being split across `try` boundaries, and against the `seq` test ever being relaxed, not against an input — because 'defensive' without such a sentence is indistinguishable from noise. That counterfactual is itself testable, and worth running: make the swap you are guarding against (for `validation.ts`'s three `typeof value !== 'number'` operands, writing the coercing global `isFinite(value)` in place of `Number.isFinite(value)`) and run it **both** with and without the kept operand. It survives with the operand and dies without it, which is the only form of evidence that a cannot-decide leg is holding anything. A cannot-decide leg whose whole justification is a forbidden edit should end up with a gate in front of the edit, not only a comment: `eslint.config.js` now configures `no-restricted-globals` for `isFinite`/`isNaN`, added the cycle after that measurement and verified by introducing the swap and watching eslint report it. The operand stays anyway, and the comment says which removal each of the two defences covers — deleting the operand alone changes nothing today, while deleting the rule re-opens the swap without touching the source.
- A third reason a mutation survives: the reconcile loop *repairs* it before an end-state assertion can look. `reconcileAssignedTopics()` drops any assignment whose durable route no longer names this worker, so deleting the frame-level guards (`handleControlMessage`'s route check, the route write in `subscribe()`) still ends in a correct quiescent state. That is a finding, not a failure — record which mutants survive and why in the harness header, and keep the mutation that kills the repair itself (`empty the sweep`) as the proof that the leg is load-bearing.
- A mutant that does not compile is not a caught mutant, and on this repository it fails *loudly in the wrong place*. `scripts/build.mjs` calls `rmSync(dist)` before `tsc`, so a mutation the type checker rejects (`if (false && guard)` → `TS7027: Unreachable code detected`) leaves `dist/` holding declarations only, and the next E2E run dies inside `openDemoTab` waiting for the connected badge — a missing artifact reported as a test failure. Write the mutant in a form `tsc` accepts (an empty consequent block, or a comparison it cannot fold), and chain the build's exit status ahead of the test command so a broken build can never masquerade as a killed mutant.
- Vitest transpiles `tests/**` with **no type checking**, so a test that constructs a deliberately illegal value is red only under `pnpm check`. `postMessage({ type: 42 })` into a `ClusterChannel` (whose signature takes `WorkerClusterMessage`) ran green through `npx vitest run` and failed CI's `verify` at `tsc --noEmit` with two `TS2322`s — a failure that names no behavior, so it reads like infrastructure until the log is opened. When a test forges a value, widen at the **call site** (`const postForged = (channel.postMessage as (message: unknown) => void).bind(channel);`), not per frame: `Object.create(null)` is `any`, so a per-frame cast leaves one forged post looking exactly like a legal one and the cast stops being the marker. Same asymmetry on the mutation side — `unknown.type as string | null` compiles from `unknown`, so a broken *pin* can typecheck; only the assertion catching it proves the file was checked at all.
- A guard in a *synchronous* `catch` is not automatically dead. The throw usually comes from application code that ran just before it, and that code can re-enter the library: `credentialProvider.getToken()` calling `transport.stop()` and then throwing leaves `resolveTokenRequest`'s catch addressing a backend that no longer exists, where `post()` throws `start() must be called first` out of the Worker message listener. Build that re-entry before classifying such a leg as unreachable — `tests/centrifuge.test.ts`'s throwing-provider cases are the shape. Contrast with genuinely unreachable legs (`selectWorkerBackend` cannot return a Worker backend when the global is absent): say which kind each is, because the difference decides whether deleting it is a cleanup or a regression.
- A zero-count arm verdicted “no caller can bring the premise” has to be re-checked against the consumer code *the enclosing method itself* calls. Tiering `cluster.ts`'s fourteen zero arms found two live defects that way, and in both the caller enumeration was correct and useless because the re-entering code was inside the method being audited: `activate()` writes `started = true` and only then reaches consumer code (`environment.createChannel`, and `handlers.onControl` on the storage-less self-SUBSCRIBE) before arming its heartbeat, so a `stop()` landing there had already run `pause()` and the rest of activation rebuilt what it removed — a live channel with a listener, a worker record no peer would ever prune, an interval no later `stop()` could clear, and a stopped tab accepting frames addressed to it. The same teardown arriving from `handlers.onResume` was dropped by `stop()`'s own entry guard, and the next visibility toggle revived the runtime. Two rules: ask what runs on this stack *between* a flag write and the side effect that depends on it, not only who calls the function; and read the census rows that do occur as the control on the rows that do not — an absent row establishes only that no test reached it, which is evidence at all if the probe is visibly reading those flags false somewhere else.
- To prove an absorbed rejection (`` await p.catch(() => undefined) ``) is dead, enumerate the assignments to that variable and show each chain ends in a terminal `.catch(...)`: `performStop()`'s `pendingStop` is set in exactly two places, `createStopPromise()` and `suspendTransport()`'s chained stop, and both end in `.catch(error => this.reportError(error))`, so the absorb can never run and the failure is already recorded where it is produced. Name the assignment sites in the comment, because that enumeration *is* the proof, and check what deletion would do instead: dropping this absorb routes a future rejection to a second `reportError`, not to an unhandled rejection, which is a different (and survivable) mistake — whereas the neighbouring `startPromise?.catch(() => undefined)` must stay, because `startPromise` holds the opening a failing `ready()` reports to its caller.
- An enumeration proof is only as strong as its last step, and that step is often a claim about a *handler* rather than about assignments. The `0.21.2`/`0.21.3` deletions above were correct in form and wrong in fact: `.catch(handler)` produces a **rejected** promise when `handler` throws, and `reportError` threw on any rejection reason without a primitive conversion. `0.21.4` fixed the coercion (`describeFailure()`); write the dependency next to the enumeration, not only the enumeration. Prefer a proof that needs no such assumption — `opening.then(f, g)` registers `g` as a handler of `opening` itself, so a trailing `void opening.catch(...)` on that same promise is redundant by Promise semantics alone, and that argument survives any future change to the handler.
- Three different verdicts get confused under "uncovered", so name which one a leg is before touching it: **dominated** (provably cannot run — delete it, or keep it if the failure modes are asymmetric, see below), **the only handler** (an uncovered *function* that is load-bearing — `reopenTransport()`'s early-return arm gets no `.then(f, g)`, so its swallow is the guard, not a gap), and **executing but redundant** (covered, runs, changes nothing — deleting it moves *no* coverage number). Check which tier before claiming one: the trailing `reopenTransport()` swallow removed as Unreleased work was not among the module's three uncovered functions, so that change was justified by its proof with zero coverage movement, and reporting it as a coverage win would have been false.
- When a dominated leg has no assertion behind it, whether to delete it is a failure-mode question, not a purity question. Force the premise false (make the promise reject) and run the suite **twice**, with and without the guard: if the same single test fails both ways, nothing protects the premise, so decide what deletion costs. In `reopenTransport()`, keeping the absorb lets a superseded resume still reopen, while deleting it leaves the bus silently closed after a pageshow — a guard whose deletion converts "degraded" into "stuck" earns its uncovered function. Record the measured pair in the comment; "it is dominated, and here is what breaking it would cost" is the statement a later reader can act on.
- A comment asserting that a promise "never rejects" is a claim about every path *through the handler it runs*, not about the happy path. `performStop()`'s catch reports through `reportError()` → `notifyError()` → `invokeHandlers(errorHandlers, …, ERROR_HANDLER)`, which absorbs a throwing subscriber only by writing to `console.warn`, so a throwing subscriber **plus** a throwing `console.warn` makes `performStop()` reject — and on that path the rejection arm of `createStopPromise()`'s settle `.then(f, g)` is the only thing that ever settles `await bus.stop()`, because the transport shutdown is already finished and nothing else can wake the caller. That arm had never executed, and the comment saying it could not had made it read as defensive filler. When an impossibility claim guards against a *hang*, construct the throw rather than trusting the prose, and make the failure legible: race the promise against a short real-timer watchdog so deleting the leg fails as `expected 'hung' to be 'settled'` in 250ms, not as a test timeout that names nothing.
- **A gate that reads repository history has to hold at every moment a tree can be built, and the tag build is an environment no PR CI reproduces.** `tests/documentation.test.ts`'s roadmap-preamble case was written to require "the newest tag" and passed on its own PR — then killed release run 159 at the Release job's own `pnpm check`, because on `v0.21.39` the newest tag *is* `0.21.39` while the preamble legitimately still says `0.21.38`: the preamble is advanced by the record commit, which necessarily lands after the tag. A branch build never has the tag its own release will create, and main's push CI passed for a third reason (it ran before the tag was pushed), so three green runs said nothing. Enumerate the lifecycle moments before choosing the oracle — prep, tag build, record commit, next prep — and prefer a **set** that is true at all of them ({ greatest tag strictly below `package.json`, that version once tagged }) over any single "latest X". A single tree cannot decide "which release is last" during the tag→record window, so a rule that pretends it can will be false on some real commit. Note that `scripts/verify-public-types.mjs` and `verify-version-compat.mjs` already solve a *nearby* question with "newest tag not pointing at HEAD", and copying that answer here would have broken the merge→tag-push window instead — a baseline for diffing tolerates any consistent choice, a prose claim about the last release does not. When a rule like this fails, fixing it is not enough: the just-pushed tag has produced no artifact and no release, so delete the ref and re-point it at the fixed commit — leaving an unpublished version in `CHANGELOG.md` blocks every later tag, because `scripts/verify-release-version.mjs` refuses a tag while the changelog names a release the registry never recorded.

- A file whose tests spawn subprocesses inherits the **global** `testTimeout` and will flake with host load, so give it its own and prove the override binds. `tests/version-compat.test.ts` builds a throwaway git repo per case (`git init/add/commit/tag` plus one `node` spawn), so its cost is five process starts plus temp-tree I/O, not the object comparison it asserts on; at load average 400+ on 8 cores one case died at the global 15 s under `pnpm test:coverage`, and on a second reading two did, with the survivors reporting 3–19 s. Which case crossed the line moved between runs — the scheduling signature, not a hang — and the same file alone with the contention gone ran all 18 in 23.7 s total (~1.3 s each), so the pin is for the loaded case and does not claim the file is slow by construction. Say exactly that in the comment, including which reading was confounded by a build of your own running concurrently. Then falsify the mechanism rather than trusting it: `vi.setConfig({ testTimeout: 1 })` makes 17 of 18 cases fail with `Test timed out in 1ms` and the run exit 1, which is the only evidence that the call reaches this file's tests at all. Prefer the per-file ceiling over raising the global one — every in-process test depends on the global staying tight.
- A test that passes can still break the gate: attach a rejection handler **before** the await that lets the rejection land. `tests/replay-persistence.test.ts`’s stale-signal case created three loads whose aborts fire on real 0/15/40 ms timers and asserted the third one *after* a healthy reopen round trip, so on a runner where that round trip exceeds 40 ms a promise rejects with nothing attached to it — every test green, and `pnpm test:coverage` exiting 1 on `Unhandled Rejection: Error: transaction aborted`, attributed to whichever test happened to be last. Measured both ways on the real file with the round trip forced to 200 ms: attached at the assertion it fails the run with 34/34 passing, attached up front the same run exits 0. Vitest reports this as a separate “Unhandled Errors” block plus `Errors 1 error` rather than as a failed test, so read the exit code, not the checkmarks. The general shape: any assertion whose `await` is separated from the promise’s creation by another await is a race with the scheduler.
- When auditing shipped prose against the code, start from the sentence and end at the consumer, not the validator. Three defects of this shape reached a release (`0.21.7` wildcard replay teardown, `0.21.8` trace redaction, `0.21.10` a heartbeat value that meant "never" to the sender and "malformed" to the receiver), so the sweep was run across the whole option surface and the parts that came out **correct** are recorded here so nobody re-derives them: every default in `docs/configuration.md`’s tables matches its `??` site (`maxActiveWorkers` 3, cluster heartbeat 3000 / TTL 10000, replay `maxPerTopic` 100, trace `metricsIntervalMs` 5000, dedup 1000 / 60000, `persistenceRetry` 1 / 50, `recovery` 1000 / `Infinity`, Centrifuge heartbeat 10000); the `pruneStrategy` sentences match `replay-pruning.ts` exactly, including that `age` leaves timestamped entries bounded only by the retention window while timestamp-less legacy entries stay capped, and that `age` without `retentionMs` falls back to the count cap; `retentionSweepMs` really does require `retentionMs` plus a `clearBefore` adapter (`replay-manager.ts:288`); `replay: n` delivery is capped by `maxPerTopic` in every strategy and `docs/api.md` says so; the en and zh option tables carry identical types and defaults (only prose differs); and the numbers in the *prose* mostly agree too — comparing the normalized digit multiset of each public doc against its `docs/zh/` mirror, `README.md`, `api.md`, `architecture.md`, `configuration.md` and `getting-started.md` are identical, and of the five pairs that differ, the three small ones (`capabilities.md`, `transports.md`, `benchmarks.md`) were read line by line and are form-only: 最多 3 个 against "At most three active Workers", 实现 5 个 against "implements five methods", 2026 年 9 月 against "early September". `release-checklist.md` and `roadmap.md` are now read to exhaustion by the block-paired sweep (align each language's Nth release section, compare the digit multiset per section, and then look **only** at digits present on one side — a token appearing twice in English and once in Chinese is sentence structure, not a claim). Every one-sided digit was read: the shapes are numeral-versus-word (`Twenty-two`/`22`, `five times`/`5 个`, `pre-1.0`/`1.0.0`, `September 25`/`9 月 25 日`) and one-sided specificity inside dated release blocks (an English `(337 unit tests)` with no Chinese figure; a Chinese `85/80/90/85 → 96/92/96/97` floor list against an English sentence that says "a few points under the measured values"), and **no pair disagrees on a value**. That is a closed check; re-run it with the sweep's own one-sided filter, and check the filter reports a non-zero count in *each* direction before believing an empty list; and the two other `Infinity`-accepting options honour it on the consumer side — `attempt > recoveryMaxAttempts` (`data-bus.ts:1432`) and `Number.isFinite(timeoutMs) && timeoutMs > 0` (`websocket.ts:145`). What the sweep did *not* cover: prose outside the option tables (architecture, capabilities, getting-started) and the `docs/zh/` narrative sections. Nor did it catch a *behaviour* claim in the prose of a section it did cover: `docs/configuration.md` said the persistence retry delays "grow exponentially and are capped", and only the doubling is capped — `backoffMs` is waited verbatim as the first delay, so a `backoffMs` of 5000 waits 5000 ms and then 1600 ms. Both languages now say which of the two the ceiling bounds. Take the lesson from 0.21.11 rather than re-deriving it: a table row and the paragraph under the same heading are separate claims, and when the claim is about a number the pin has to reach that number — the one test whose name said "capped by the retry ceiling" advanced 50 ms then 100 ms and never went near it. That gap has since been swept (`0.21.11`, `0.21.12`): ~15 more unsupported sentences came out of `architecture.md`, `capabilities.md`, `getting-started.md`, `transports.md`, `api.md` and `README.md`, in both languages. Two lessons from doing it. (1) "I audited the option surface" describes the *tables*; the paragraph under the same heading, and the README's summary bullets, are separate claims — `README.md` promised "the persistence layer does not store connection addresses, raw Topic text, or message content" two bullets below the line where it advertises the IndexedDB adapter that does exactly that. (2) The highest-yield shape is an absolute about storage (`never`, `only`, `does not persist`), because one opt-in contradicts it; `channelFallback: 'storage-event'` (frame written whole, plaintext topic and payload) and `replay.persistence` (object store keyed by the plaintext topic, payloads in the rows) are the two opt-ins that broke such sentences six times over. Verify each candidate against `src/` yourself before editing — both audits produced items that read as defects and were demoted on inspection (the `route && route.workerId !== …` tolerance is deliberate, and the storage-writer's "exponential backoff" prose is accurate because its initial delay is a constant, not an option).
- A claim about *how prose reaches consumers* needs a control that could have failed. Concluding "this src string appears 0 times in `dist/index.js`, therefore src comments ship only through `dist/**/*.d.ts`" was wrong, and wrong for a checkable reason: the string tested came from `src/core/types.ts`, a type-only module that emits no runtime JS at all, so the probe could not have produced a positive result whatever the truth. Measured properly, esbuild preserves comments in the bundles too — `dist/cjs/index.cjs` carries 563 comment lines, a shared chunk 525, `dist/centrifuge.js` 84, `dist/index.js` 34 — and all of those are in `package.json` `files`, so **the shipped comment surface is the declarations *and* the JS**, roughly double what an audit scoped to `.d.ts` covers. That "and the JS" has since been given its position rule, and it is narrower than the count suggests: esbuild keeps a comment only in **expression** position — inside a parenthesized initializer, or before a returned expression — and drops it from a declaration header and from a statement slot (measured on a three-case probe file, one `export const` with both). So a JSDoc block on an exported symbol reaches consumers through `.d.ts` alone, while a body comment in an expression slot ships twice. Before concluding "X does not ship", name an artifact where X provably *does* ship and show the same probe finds it there; if the probe cannot distinguish, it is not evidence. **And state which position each sentence sits in, because two probes on the same release disagree by design.** Re-measured on `0.21.26`/`0.21.27` against one built tree: a **class-member JSDoc** (`/** … */ stop(): void` in `replay-manager.ts`) appears in `dist/core/replay-manager.d.ts` *and* in `dist/chunk-*.js` *and* in both CJS bundles; a **statement-slot comment** inside a method body — this cycle's three in `cluster.ts`/`data-bus.ts`, plus `0.21.26`'s own inline comment inside `withPersistenceRetry()`, plus the long-standing "Order matters: release local subscriptions…" note that predates all of them — reaches the published tree **only through the `.js.map` files' embedded sources**, which `files: ["dist"]` does publish. **`private` does not exempt a member from the declaration doc.** Measured on the next cycle's edit: `src/centrifuge.ts`'s JSDoc on `private handleOutput()` appears in five artifacts — `dist/centrifuge.d.ts`, `dist/centrifuge.js`, both CJS bundles and their maps — because `tsc` emits the member as `private handleOutput;` and carries its doc with it. So the assumption "nobody consumes a private method's comment" is false in the only direction that matters, and a wrong structural claim there is a *shipped* wrong claim even though no consumer can call the method. Two corollaries from the same pass. (a) The statement-slot half re-confirmed itself on fresh instances from `cluster.ts` and `trace.ts` — each appeared in exactly the three `.js.map` files and in no `.d.ts` — so the classification is stable across files, not a property of the one module it was first measured in. (b) **A phrase that wraps inside a comment reports as absent.** The first run of this check concluded that `cluster.ts`'s "one of three fallbacks rather than a pair" shipped *nowhere*, because the string breaks across two `//` lines; re-grepped with a single-line fragment, it is in the same three maps as its neighbours. So before writing "does not ship", shorten the probe to something that cannot cross a line break, or grep the emitted form rather than the source form — the same failure as an exact-match citation scan that misses a wrapped title, one level up. So "the comment ships" is not one question: ask what a consumer's editor, sourcemap step, or bundle read would surface, and grep one artifact per answer rather than generalizing from the first hit. The pre-existing third case is the control that keeps this from being read as a regression: if the probe cannot find a comment that has been in the tree for releases, it is reporting esbuild's placement rule, not a change of yours. The same rule bites the cheap "no code moved" proof: `npx esbuild --format=esm <file>` at both revisions and `cmp` **fails** on a comment-only edit whose comment sits in an expression slot, and passes on one that does not, so the comparison has to strip comments from both outputs (or require every differing emitted line to begin with `//`) before either verdict means anything.
- A comment explaining *why a call sits where it sits* is a behavior claim about a counterfactual, so the probe has to take the read at the earlier point rather than argue from the mechanism. Two shipped comments explained themselves with "the route write has not flushed yet, so a reader in this task sees nothing", which the code refutes directly (`getItem` returns the pending value, `storage-batch.ts:79-82`; `keys()` is persisted ∪ pending minus pending-deletes, `:143-155`) — but "refuted by reading" is not "measured", and the real question was whether the *snapshot* taken pre-open differs. It does not: two buses on one `MemoryStorage` + `ChannelHub`, snapshot before and after `ready()`, byte-identical. Check **which write** that run exercised, though — the subscribing bus won the election, so the route it listed was its own still-pending `writeRoute`; had the peer kept the topic, the probe would have read a flushed record and proved nothing about coalescing while looking like it proved everything. A setup with a second participant satisfies one participant's premise before the other's read. Same discipline as the bad control above: name the artifact where the claim *would* have shown up, and confirm the probe reaches it.
- **A line-number citation inside a comment decays on every edit above it, and decays silently.** Measured on this tree: of the `:NNN` references inside `src/` comments, nine pointed somewhere other than what they described — the `assertPublicTopic` trio in `centrifuge-session.ts` had drifted ~27 lines onto a blank line and a JSDoc opener, the two citations into `withPersistenceRetry` in `replay-manager.ts` landed *inside another comment*, and `resetBuffers()`'s epoch bump was cited at its own docstring three lines early. Reading the sentence cannot detect any of it and `tsc` never will. Cite the symbol or the condition: "`getItem` returns the pending value first" survives any refactor, "`storage-batch.ts:79-82`" does not. Keep a number only where no name exists — a line in the installed dependency's `build/index.js` — and pin it to the dependency version there, since that file changes without us asking. To audit, print each citation beside its target line (`cites :N -> <line N>`, ~ten lines of script) and read the pairs; that pass finds the whole class in one go, and it is the same rule as citing coverage conditions instead of arm indices.
- **A *name* citation decays the same way, and one that names a fixture instead of a title decays twice.** `src/core/routing.ts` pointed at a case it named `sticky-existing-routes` and `src/core/trace.ts` at one it named `no-trace`; neither string is an `it()` title, both are `clusterKey` literals sitting *inside* a test body, so each pointer landed mid-body rather than at the assertion, and the first rename of either arbitrary string would have landed nowhere. They were both in shipped JSDoc, so the wrong pointer reaches consumers through the published `.d.ts` — verified the other way, by finding the corrected strings in `dist/core/routing.d.ts` and `dist/core/trace.d.ts` after the build. The class is gated now (`tests/documentation.test.ts`'s "resolve to a case that exists in the file they name"), over every living surface: `src/**`, `AGENTS.md`, `README.md` and the docs, with `CHANGELOG.md` and `docs/progress.md` out for the reason a release note is left standing — they date what was found. That exclusion is measured rather than tidy, and so are its numbers — which is why they are gone: take any tally from the gate's own scope array with its two regexes in a scratch script, because the gated set is a list someone edits while the exempt pair grows with every phase entry, and the last reading had those two files holding two-thirds of the scanned scope's total between them. `README.zh.md` and then `CONTRIBUTING.md` joined the scope in the two cycles that noticed its comment promised *every* living prose surface while the array omitted a shipped file; both report zero citations, so those entries are prospective and change nothing a reader can see. What the hand re-run over `progress.md` and `CHANGELOG.md` establishes is the *kind* of each non-resolver, and each one found so far is reported output rather than a pointer — with one exception, and it was this rule failing on its own author: a log entry put a fixture's *method* name into possessive citation form, which is the exemplum shape the same bullet forbids a few sentences later. It was rewritten into bare form rather than exempted, and the gate's comment now says in words what the hand pass is for, so the next reader does not need a count to know the pass is still needed. **The first version of this sentence was wrong in three of its four numbers, on the tree that introduced it:** it read "`docs/` carries 8 citations, and the only 4 in the tree that do not resolve are all in `progress.md`, 2 of them the very fixture-token forms this removes from `src`", where the 8 had quietly counted `docs/progress.md` (which the gate does not scan — the public docs hold 1), the non-resolving set was 5 there plus 1 in `CHANGELOG.md`, and the gated total was already 18, not 17. Two of those six then got fixed: `progress.md`'s two fixture-token pointers became their real titles, and a third pointer that looked fine was a *truncation without the ellipsis* (`"keeps a live owner sticky"` for a case named "keeps a live owner sticky before considering load") — the form the scan's exact-match arm exists to catch and the exempt file cannot. So re-derive these counts from the test's own two regexes pointed at the exempt pair, and treat an exemption from a gate as a *scope* claim, never as a licence to stop looking. Five mutants, each with its own failure named: a dead citation kills 1 test; a prefix that matches nine cases is reported as `(9 matches)`, because a citation that decides nothing decides nothing; neutering the citation pattern trips the `examined` control and neutering the title collector trips the title floor (both halves of this scan can go empty, and an empty "no unresolved citations" is what each produces); and shortening the `docs/architecture.md` citation kills one test, which is the proof that the widened scope has teeth the `src/`-only version did not. **Two things the construction got wrong first, and both are the lesson.** The destructure took capture group 2 (the quote) rather than 3 (the name), so the new gate failed twelve legs on a tree that a standalone `node` run of the *identical* regex had just cleared — when a gate you just wrote contradicts a direct measurement, diff the two instruments before believing the gate. And the first title-collector mutation dropped the backslash before `\(`, so the file died at parse time with `Invalid regular expression … Unterminated group` and `Tests no tests`; that is not a killed mutant, it is an uncompiled one, and only reading the log separates the two. **And the gate reddened the bullet that describes it.** The first draft of this sentence named those two tokens in citation form as examples, and the scan reported both unresolved — which is the rule, not an exception to it: an exemplum written in the possessive form (`tests/…`'s `token`) is indistinguishable from a live pointer by the scanner *and* by the reader who greps it a year from now, so name the token bare when the sentence is about the defect rather than pointing at a case. **A third decay mode is a pointer that resolved for nobody but its author.** Both `docs/release-checklist.md` and its zh mirror told the reader to re-derive the publication series from `.gate-logs/publication-pair.mjs`, and that directory is in `.gitignore` — the instruction was live on the one machine that wrote it and dead in every clone and in the published tarball's own docs. No gate can see this: the citation gate resolves *test titles*, and a shell command is neither a title nor a symbol. Fixed by promoting the tool to `scripts/publication-lag.mjs` and re-pointing all three mentions — and note which half that fixes, because `scripts/` is not in `files` either, so the tarball still cannot run it: the reader of a release checklist is a maintainer in a clone, and the pointer now resolves *there*, which is the only place the command was ever meant to run. The check is one line — `grep -c '\.gate-logs/' $(git ls-files) | grep -v ':0$'` — and take the tally from that run, never from this sentence. The version written with the promotion enumerated its hits per file, and re-reading the same command on the next tree had moved three of those five counts and found hits in three files the list never named; that is the whole argument for deleting the values. The command also has two readings, so a claim should say which one it used: `-c` counts **lines**, meaning one sentence that names three paths reports 1, while `grep -o '\.gate-logs/[A-Za-z0-9._/-]*' | sort | uniq -c` counts occurrences. Then classify each hit by reading its own sentence rather than by exempting a whole file: one that tells the reader to *run* something is a living instruction, and its path has to be tracked or the sentence rewritten to describe the scan; a record of what a past session ran may keep naming the scratch, because its job is to say where that log lived. Leave the records standing; promoting a throwaway probe into `scripts/` to satisfy a pointer in a dated entry trades a dead reference for a maintained file nobody maintains.
- **An inserted sentence can break the sentence it lands in, and nothing compiles prose to tell you.** Correcting a quoted number means splicing a clause into running text, and two copies of the `75 minutes` retraction landed mid-sentence, producing `…not an elapsed time in this story. so the gate exited 1 with …` and `…) and the gate exited 1 with …`. The failure is invisible while reading — every word is in the right order, the clause just starts after a full stop with a lowercase conjunction — and no test, lint rule or type checker can see it. The shape to look for is a sentence boundary followed by a function word, and the scan is a twenty-line scratch script: over `git ls-files '*.md' 'src/**/*.ts' 'scripts/*.mjs' '.github/workflows/*.yml' 'tests/**/*.ts'`, strip inline code spans, match `(\w|[)\]"”’]\.|digits)\.` + optional closing bracket + whitespace + one of `so|and|but|the|it|this|that|which|then|thus|was|were|because|also|still|only|after|before|instead|leaving|making`, and drop matches where the character before the dot is a lone letter (an abbreviation). Run over the 70 files it reported **one** real hit — the sentence in `docs/progress.md` reading `…a shape-based scan is a claim, not evidence. and the websocket timer guard's…`, since capitalized to `And` and so a re-run reports none, which is the state the next reader will find and the reason the control fixtures below are not optional. (The hit is named by its phrase rather than by its line number, because this file already carries that rule and a `:8891` here would have decayed under the first edit above it — as it happens the line is still 8891, and that is luck, not a reason to write it.) The class is rare, which is exactly why the grep rather than a re-read is the check. **Two instrument failures inside that one-liner, both of them the standing lesson.** The first pattern required `\.\s`, so it missed the bracketed form `story.) and` — found by pointing the scan at `git show <the-commit-before-the-fix>:CHANGELOG.md`, which it cleared while the defect was sitting in it; a scan whose only output is an empty list has not been shown to work. The second was the *exclusion* filter, which has to drop `e.g.` and `i.e.`: written as `(?:i|e)\.$` it matches the `e` of `evidence.` too, and the run reported clean over a tree that held `…is a claim, not evidence. and the websocket timer guard's …`. So a false-positive filter is judged by the control it still reports, not by the noise it removes — print the kept hits against a fixture containing a known defect, and re-read the sentence around anything the scan reports, since the conjunction list is a heuristic that also flags `21 e2e incl. the`. **The scan caught its author writing a second instance while recording the rule** — the edit that replaced the decayed `355 lines / 111 bullets` with a lookup spliced the tail of the sentence it replaced, leaving `…records a count decaying. and #308's citation gate passes…` in the same file, one line above the bullet describing the defect. That is the strongest available evidence for the check: the person who has just written the rule about not splicing sentences still splices one, on the next keystroke, in the act of documenting it. (Same measurement, second consequence: that quoted pair was *already* wrong at its line half — `wc -l < AGENTS.md` read 354 at `37d98b7`, the commit that wrote "355 lines / 111 bullets", while its bullet half was correct — so it is gone rather than corrected, because nothing in the entry argued from the file's length.)
- When a prose defect is retired, grep the claim rather than the file. The `0.21.11`/`0.21.12`/132/133 sweeps each fixed a sentence in one place and left restatements standing; the coordination-trace fiction lived in the declaration doc, the `start()` call-site comment, and `docs/api.md:299` + `docs/zh/api.md:299` — five copies of two wrong sentences. "Audited" means the string no longer appears anywhere in the repo, so the last step of every correction is a repo-wide grep for its distinguishing phrase (`grep -rn "see no routes\|still coalesced" docs/ src/`), with both languages in scope, and a per-copy decision rather than a global replace: some copies are the same claim in different words, and some are a different claim that merely looks alike. **The mirror is the copy that most often survives, and a numeric edit into it leaves its own artifact.** The same sweep rewrote the English staleness figure (`落后一个小时` → 3,418 s) and left the Chinese `docs/zh/release-checklist.md` sentence standing, and both zh copies of the measurement had become `3,207 s 秒` — the unit written twice, because the number went in as digits into a sentence whose unit was already spelled out. Grepping the *figure* across languages (`grep -rn "3,207\|3,418" docs/`) closes what a phrase-grep cannot: a paraphrase shares no substring with the text that was fixed.
- **A phrase-grep finds the copies that paraphrase hides.** The pass after that rule ran the grep above and called the claim closed — and `src/core/trace.ts`'s declaration of the same event still said "after the transport has opened and the just-issued subscriptions have flushed, so the route list is populated rather than empty". Same fiction, different words, so the distinguishing-phrase grep could not see it. Grep the *vocabulary of the mechanism* (`flush|coalesc|pending|populated|empty routes`), and then grep the corrected symbol's own doc: when you fix a behavior claim, every declaration that names the same function or field is a candidate restatement, and the symbol name is the term that survives paraphrase. **A table cell is prose too, and hides better.** `docs/architecture.md`'s identifier table kept a storage absolute (`topic` → "No; kept in Runtime memory and control messages") through four passes that corrected the paragraph, the option row, and in fact the later row of the *same table* — the storage-event row that states the exception the `topic` cell denies — because a cell that short matches none of the vocabulary those greps used. When an absolute is retired, re-read every table in every file whose rows share the subject, and check the cells against the rows that sit next to them — this cycle's other new find was the same shape: an enumeration of `rememberTopic()`'s call sites that named three of six, because the sentence had been re-read for the *guards* around it and never for its own list. **And a numeric audit does not certify the enumeration sharing the sentence.** `src/centrifuge-session.ts`'s note about centrifuge's `emit('error')` sites was checked against the installed 5.7.4 for its three figures — 12 sites, `BaseSubscription` at 667-2673, 23 in the file — and all three held, so the sweep reported the sentence as verified. What it never counted was "the one entry into `_getSubscriptionToken` with no state test of its own": there are four entries and three have no state test above their call, so the numbers were right and the *only*-claim wrong in one breath. The asymmetry is why the miss is repeatable — a numeral has a lookup and an enumeration has none, so counting the former feels like checking the sentence. Either re-derive the list or drop the quantifier; the corrected note names all four entries and what actually excludes three of them (a token predicate, not a state test).
- **"Undocumented" is a repo-wide claim, so check it repo-wide before writing the rule.** `rememberTopic()`'s eviction skip `candidate === topicKey` was recorded as "undocumented anywhere in `src/` or `docs/`" while `docs/architecture.md` and its zh mirror had stated it since the initial import (`8429d2f`). The reading that produced the claim had looked at the table cell that *under*-stated the rule, not at the bullet list two dozen lines above the same table. So grep the condition **as the code writes it** (`candidate === topicKey`) plus its vocabulary form (`topicKey` near `evict`) across `docs/` and `src/`, and read each hit's position in its own section; the failure mode here is not a missing doc but a *third* copy of a rule that already had two — which is exactly how `src/` came to state the same eviction rule twice inside one comment block. Reading that section properly turned up two more defects in prose that looked like the correct half: the bullets named a variable that does not exist (`oldest !== topicKey`, where the loop iterates `candidate` and `continue`s on **equality**) — a fabricated identifier decays like a drifted line number and, worse, sends a verifier to code that is not there — and the section's cap sentence claimed a security property the mechanism refuses. That one needed a run, not a read: a forged `CONTROL/SUBSCRIBE` is *adopted*, adoption makes its key unevictable, and the loop then declines to drop anything, so 1,200 frames grew `knownTopics` to 1,200 entries along with `assignedTopics` and one reconcile tick took both back to `0`. **When the claim is about a cap, flood the structure and count the case its own skip protects**: a bound that holds "unless everything in it is owned" is not evidence about the owned case, and the sentence that names the protection is usually the one that assumed it applied.
- **A scan keyed on a *derived* field reports a paraphrase as an absence.** "The Chinese roadmap has no `0.20.68` delivered-scope block" was produced by diffing headings matched by a version-plus-suffix pattern — 141 zh blocks end in `已完成范围`, that one says `已交付`, so the comparison of two heading *sets* came out asymmetric and read exactly like a missing section. The block exists, with the same bullet the English copy carries. The mirror of the bullet above, and worse, because a scan's output gets turned into a task list: a set diff on a key you derived (a regex, a suffix, a normalization) cannot distinguish *absent* from *worded differently*, and it never prints the near-miss. So run the scan in the other direction too — list what each file's pattern *failed* to match and read those lines — and when a diff says "missing", grep the bare identifier (`grep -n "0\.20\.68" <file>`) and look at what is actually there before filing it. Two consequences landed here: the real defect was a title-convention break plus a placement break (of 144 release-scope blocks in each language, exactly one was out of the section's descending order — `0.20.68` sitting after `0.11.0` where an appended block had landed), and both are now gated by title-set, strict-descending-order, and cross-language-version-set assertions in `tests/documentation.test.ts`, mutation-checked as three separate mutants (one kills only the new gate, one kills only the new gate, one kills the new gate plus the two pre-existing equal-count gates — which is how the third leg's blind spot is known: equal `## ` counts and equal list-item counts passed while one pair disagreed on which releases it *named* by suffix).
- **A conclusion drawn from a short run of consecutive samples is the least durable thing in the file, and it reads as the most.** Three releases recorded at 76.1, 75.5 and 75.8 s — a 0.6 s spread — and `0.21.35`'s prep wrote, in four copies, that the record lag "has been stable to a half-second", which "locates the variance in the reader's blindness rather than in the registry's write". The release then took **127.3 s** to record: 67% above the widest of those three and the third-largest of the nine readings, behind only 310.0 and 248.7. So the run of three was three samples in a wide distribution, and the strong sentence built on it. Note the asymmetry that made this hard to see: the *count* was in the same sentence and was correct, and the *inference* was the part nobody could check without the next release. Two rules come out of it. (1) When a series is quoted to support "stable", write the spread's own bound rather than the run's — what those nine values support is "the gap lives in 74.8–310.0 s and nothing has beaten the 310 s the budget is built from", which is weaker and is the only version the ceiling needs. (2) Name the retracted inference where it stood rather than deleting it, and keep the dated copy's wording: the published `0.21.35` release note still says the three-sample thing, because that is what was known before its own publish existed. The same trap is one level down in the bench gate, where a *median* over an alternating sample is stable only while the modes stay mixed — see the `bench:compare` rule below — and a run of like-mode reports makes the baseline itself move.
- **A count is a claim about one set, and the nearest same-shaped list is a source of contamination.** "the second-smallest of the **seven** now listed" described the ack→registry-record gaps, of which six were listed — the seven belonged to the *verify-duration* series two sentences away, and the sentence's own parenthesis enumerated six, so no external lookup was needed to catch it. Same shape one line later: three copies called 0.21.30's observed **364 s** span its "budget", while the budget was **360 s** (48 × 7.5 s) and 364 s is only first-poll-to-last-`ETARGET`. When a sentence carries a number that belongs to a series, name the series in the words attached to the number ("six gap samples", "seven duration samples"), and if the sentence enumerates, re-derive the count from its own enumeration — a mismatch there is decidable locally, and it is the only check that fires before the text ships.
- **When a comment names another library's behavior, `src/` is not the verification target.** Four of this pass's defects were sentences about Centrifuge ("the two function-valued hooks Centrifuge invokes", "internal listeners preserved", `subscribe('')` throws, "can never drift apart"), and none of them is checkable by reading our code. Open the installed package: `getToken` is the only client-level credential hook in `centrifuge@5.7.4` (in its `build/types.d.ts`, beside the comment naming it as the renewal hook too) and `getChannelToken` exists nowhere in its build; the no-op `error` listener that our `removeAllListeners('error')` deletes is installed by the **`BaseSubscription`** constructor every subscription reaches through `super` — `Subscription`'s own class body contains no `on('error')` call at all (measured), so naming the subclass as the installer sends a verifier to a range where the code is not — the dependency's comment reads `Avoid unhandled exception in EventEmitter for non-set error handler`, and that emitter throws on an `error` emit with no listener (`throw er; // Unhandled 'error' event`). **Pin the version in the corrected text**, because the claim is now about a dependency, not about us — and say plainly which half is an open question rather than asserting safety the probe did not establish. **A version pin does not rescue a line number: read the number off the installed file when you write it.** This bullet itself carried `build/index.js:762` for the no-op listener, and `0.21.29` checked it — the guard is at 763-764 in the pinned 5.7.4 and at 748 in the 5.7.0 copy still in the store, so the number matched no installed file and its origin is unrecoverable. Prefer the dependency's own quoted comment string as the locator: it is searchable with `grep -n`, and a reflow upstream does not silently invalidate it the way `:762` did while looking entirely authoritative.
- A count quoted in a shipped comment decays **silently**, because nothing contradicts it: its sentence is usually "this leg is hot while these arms stay cold", and that reading survives any drift in the number. `reopenTransport()`'s note carried "0 of 5041 calls" from `b0354e4` while the same lookup on a later tree read 5046 — five extra calls, no signal, and a reader who trusts the constant is now calibrating on a run that no longer exists. So write the *lookup* rather than the value ("take it from `coverage/coverage-final.json`'s function count for X"), keep only the pair that carries the argument (hot method, zero-count arms), and treat any number whose re-derivation is not named as decoration that will be mistaken for evidence. **Naming the lookup does not license keeping the value, because a reader with a number in hand does not go and run anything.** `src/core/trace.ts`'s sink-default note was corrected once from "884 tests" to "all 944 tests" *in the same sentence that added* "take the denominator from `pnpm test`" — and it read stale again within two releases, at 954. The count bought nothing there: the claim that leg carries is "2 events → 2 `TypeError`s", and the suite total is not a property of it. So the fix is deletion, not a refresh, and the surviving rule is that a quoted value beside its own lookup is the worst form — it looks self-maintaining to whoever reads it next, which is the person who just edited the number and will edit it again. One sweep decided this: 14 numeric claims over `src/` counted against the tree (the candidates come from a `git ls-files` scan over `.ts/.tsx/.md/.mjs/.js/.yml` for three shapes — `exactly|only|all|both|no|neither` + a numeral, a numeral + a countable noun, and `the|these` + a numeral + a plural noun + `above|below` — matched against the **whole file** rather than line by line because a wrapped claim is invisible to a per-line scan, and kept in a `.ts` file only when its start line is a comment; adjudication is per-copy and mechanical — `confirmRoute`'s 3 call sites, `assignedTopics`' 3 writers, `stopped`'s 2 readers, `pendingStop`'s 4 writes and its 2 non-null writes, `transportReady = false`'s 5 sites, `startPromise`'s 4 installs, the teardown block's 4 field resets, `percentileMs`' 6 calls, the reaper's 3 statements in one `try`, `replay-persistence`'s 4 latches / 5 guarded closures, `validation.ts`'s differential vector — its 25 items enumerated in the sentence that counts them, and `centrifuge@5.7.4`'s 12 `emit('error')` sites inside `BaseSubscription` at lines 667-2673 against 23 in the file, so the range qualifier is what makes that claim true, though the sweep that certified those three figures missed the enumeration sitting beside them, recorded in the bullet above) — **twelve held exactly, one had rotted, and one counted the wrong thing** — `centrifuge.ts`'s `handleOutput` doc said "the Worker message listener, the SharedWorker port listener, and the local-session sink — all three feed into this single dispatcher", and there are **two** call sites: the dedicated Worker and the shared `MessagePort` are attached to the *same* `handleMessage` function (`grep -n "addEventListener('message'"` shows two registrations of one handler), so the sentence counts sources correctly and functions wrongly, and a verifier who goes looking for the third listener lands on the first one again. Twelve of fourteen is a corpus in order, and the fourteenth is the one that shipped a wrong structural claim through a `private` member's `.d.ts`. **Watch what just happened to this sentence:** it said "twelve of thirteen" while the list beside it had grown to fourteen items, because the last claim was adjudicated after the sentence was drafted and the sentence was not re-read. That is the decay this bullet exists to forbid, reproduced inside the bullet that forbids it, one edit after the fact — which is the argument for the rule's own second half: re-derive a count from the enumeration sitting next to it, never from the previous draft of the prose. The other copies of that same "944" survive on purpose in `docs/roadmap.md`, its zh mirror, and `docs/progress.md`: they sit under dated release-scope headings and record what was measured *then*, so the per-copy decision there is "leave it", and a later sweep that greps the number to zero would be rewriting history rather than correcting prose. This is the same failure as quoting a sweep total as a per-seed cost — name what you divided, by what, and where the outlier went. **And say which side of the release a tally was taken on**: "41 of 163 CHANGELOG sections name versions the registry never had" was a true reading twelve minutes before the tag was pushed and a false one after, because the version being prepared was itself in the count; a number whose set includes the thing currently in flight has to state that, or be re-taken after the publish.
- A census **total** is a host measurement, not a measurement of the code, whenever a wall-clock-bounded fuzz supplies most of its volume. Re-running the `stop()`-guard flag-vector census on 2026-09-25 returned 20521 evaluations with 782 reading `(0,0,0,0)`, against the 626-of-7589 the shipped comment claimed — and 18130 of the 20521 come from `tests/coordination-invariants.test.ts` alone. That attribution is a measurement, not an inference: setting that file's `MAX_SEEDS` to 1,000 drops its share to 3648 evaluations and its all-false count from 205 to 36. A count quoted without its depth is therefore unreconstructable twice over, because the tree moved *and* the runner did. So record at the site which rows come from the deterministic files (those are stable, and are the ones a verdict may rest on), name the instrument and the depth the total was taken at, and do not let a share of a fuzz-weighted total read as a property of the guard — this cycle's stale sentence claimed the all-false row was "the common case" when it was 8% then and 3.8% now, while `1001` (a live bus being stopped) held 88%. Check which other surfaces the dead number reached before calling the claim closed: the same 7589 stood in this file's own rule about decaying counts, in `docs/progress.md`, and in both roadmap languages. Published `CHANGELOG.md` sections are the exception — a release note is a dated record of what was measured at that release, so correct the living instruction and let the historical entry stand.
- A **flag vector** quoted in a comment is unreadable, and an **unattributed count** is unrecoverable; both were shipped into `runTransport()`'s guard note and re-dug this cycle. The comment listed census rows `1000`/`0000`/`0100`/`1001`/`1010` and never said the bits are the four operands in source order, so a reader could not re-derive any of them — state the encoding, and say what instrument regenerates the set (one first-sight `console.log` per distinct row over one whole-suite run; the row set came back identical). Same failure in the other direction: `transportReady` "dies to eight named cases (the probe prints eight)" — re-running it, nine tests fail (6 `data-bus`, 3 `centrifuge`), and because the eight were never *named* there is no way to find which case joined short of reading the suite's history. So a kill count carries its list or carries the instruction to re-run the mutant; "at least N" does not make it safe, since the reader cannot tell a grew-from list from a complete one. Related: before endorsing a count as an operand's verdict, check it is still the count — `droppedAfterConnect` was re-measured at exactly its claimed two, which is what makes the nine worth reporting instead of quietly fixing.
- Numeric adjectives in a comment are the cheapest defects to catch and the easiest to leave: "the four record methods", "each time a backend is created", "exactly N places", "no-op", "distinct large primes". Each one is a grep or a five-line script — `metricsActive` had seven readers not four; `generation` is bumped on two of three backend-creation paths and `stop()` moves it *before* the listener removal the comment credits it with following; `defaultWebSocketFactory` "returns null" but throws; `unsubscribe()` "is a no-op" and unconditionally sends a frame; three of six "primes" in `hash.ts` are composite (`929 × 1_719_413` etc.), where the property `Math.imul` mixing actually needs is oddness. When a constant is involved, do not reason about it — factor it, and say in the comment that the values are load-bearing for keys already in localStorage so nobody "fixes" them.
- A hand-written storage record in a test is a claim that a real Worker could have written it. The stale-assignment sweep in `tests/cluster.test.ts` copied a route and changed only `workerId`/`tabId`/`generation`, which left the *previous* owner's `confirmedAt` on the record — a state no worker ever writes (`confirmRoute()` runs in the same task as the `assignedTopics.set` that owns it, at all three call sites), and precisely what `reconcileStrandedSelfRoutes()` now releases. The test's assertion then passed by a mechanism other than the one it named. When a newly added sweep over an existing record shape turns an old test red, check the record's self-consistency before concluding the new code is wrong: fix the premise (that test now stages an *unconfirmed* takeover, which is what a real one looks like from a peer's seat) and state in the comment which field carries the difference.
- Before rewriting a test on the theory that it passes vacuously, measure the theory against the **unmodified** test — one scratch file that reproduces its shape and prints the counters the theory is about. A pass here reads `attempt: 3`, `startCalls: 3`, one `exhausted` event with the environment clock frozen, so the cooldown never swallowed the sequence; the rewrite, and the comment asserting the opposite that came with it, belonged in the bin rather than the branch. A speculative fix costs more than no fix, because the comment it brings along is a claim the next reader will trust.
- **A gate that asserts an empty `offenders` list has to floor its own row count, or it reports clean twice.** Every prose scan in `tests/documentation.test.ts` and `tests/workflows.test.ts` builds its findings inside a loop gated on one regex or one substring test, so the day the convention moves — a heading form, an indented table, a step relocated into a composite action — the list is empty for the same reason it is empty on a healthy tree. Two were closed case by case (the CHANGELOG-date denominator, the phase-order floor) before the rest of the family was swept in one pass and found **twelve more gates**. Two of the shapes generalize. (1) The localized-parity gates assert one count *equals* another, so `0 == 0` passes, and a per-pair floor is unavailable: five of the ten pairs legitimately carry no table rows and one no h2 sections, so the floor belongs on the aggregate. (2) `lists every gate the workflows run in the AGENTS.md quick reference` already floored the document side and not the workflow side, and those are two independent instruments — a full `documented` set against a scan that matched nothing is a perfect pass with nothing compared. Where a second *independent reading of the same artifact* exists, prefer it to a constant: the version-heading gate now asserts its own row count is at least the number of bracketed `## [x.y.z]` sections the release tooling reads, which holds at every moment of the release cycle and needs no maintenance as the file grows. Every constant elsewhere is set well under the measured population and never as a fraction of the set it guards, because a fraction of a shrinking set shrinks with it. Then mutate each pattern alone and read which case dies: all thirteen floors fire, one per aimed-at gate, and the parity legs say more than their own pass did — with the pattern neutered, the equality assertion still reports `0 == 0` and green, so the floor is the only thing those three gates had.
- A probe that returns the safe answer on its first shape has tested that shape, not the question. The first probe for "can a handler change the fan-out it is inside of?" used a named function and measured exactly one invocation per publication — re-subscribing the same reference is a `Set` no-op, so the collection could not grow, and the file looked clean. Rewritten to register a *distinct closure* per invocation, the same probe reached 500 handlers inside a single dispatch (stopping only because the probe imposed a ceiling) and 999 on the next message, which is a non-terminating `dispatch()`. So when the mechanism keys on object identity, vary the identity before recording "not reachable", and say in the comment which of the two shapes was measured — the safe first result is itself a fact worth keeping (same-reference re-subscription really is a no-op), it just is not the fact the question was about.
- A **scoping** probe is a probe, and it needs the same control. A script that decided which zero-count arms still lacked a verdict looked at the single line above each site and handed back 21 unvisited legs — on a ledger where all **46** arms already carry their verdict, because those comments run five to fifteen lines and state the measurement that closes the arm. Reading each reported site with a window instead found one false claim in the whole set: `replay-manager.ts` attributed its `retryGeneration` bump to "suspend/stop", while the field has exactly one writer (`suspend()`) and this class's own `stop()` clears a timer and bumps nothing. The pair was true one level *up* — the bus's `beginStop()` calls `replayManager.suspend()`, and `tests/data-bus.test.ts`'s "cancels a pending persistence retry when the bus stops" exercises that path, which is why no test ever contradicted the shorthand. Two rules come out of it. (1) Before a scan becomes a task list, run it on a site you already know is answered and check it reports "present"; a scan that cannot distinguish *absent* from *not looked at* schedules rework over closed ground, and that is the expensive failure because the rework looks like progress. (2) When a comment names two methods as doing the same thing, check each name in the class that owns the thing, and say which level each half lives at — a pair can be accurate about callers and wrong about callees while every test passes.
- The mirror case is harder to see, because the test looks like it is asserting and the mutation check is the only thing that notices: `caps automatic recovery attempts…` counted the `exhausted` diagnostics a second time after the cap was spent, and deleting the one-shot guard kept the whole suite green. The second count never reached the branch — after a failed reopen the handlers installed on the transport capture a superseded `lifecycleEpoch`, so `isCurrentLifecycle()` drops a further `setStatus('error')` before the attempt counter (measured with a spy: `updateStatus` ran 0 times). When a re-issued *transport* event is meant to exercise something, check the event still arrives at all before believing the assertion; and remember the demand path (`bus.publish()` → reopen) is what reinstalls a live closure, which is also what an application does after exhaustion. Two related dead ends: the recovery cooldown is `this.now = dedup?.now ?? Date.now`, **not** the cluster/environment clock, so moving `createFakeEnvironment({ now })` changes nothing there; and a post-retry `startCalls` count is scheduler-dependent (5, not 4) — assert it relative to a captured baseline so the contract survives.
- `tests/coordination-invariants.test.ts` is the multi-tab fuzz harness: three buses on one `MemoryStorage` + `ChannelHub`, randomized sub/unsub/publish/hide/show/stop/start/tick/dropped-and-forged control frames, then quiescence, then "exactly one owner, exactly one transport holder, the same tab", "a departed topic leaves no owner, holder or route", and "one publication fans out to each live subscriber exactly once". Note `stop()` clears the instance's handlers, so the harness must forget its own subscriptions on a stop op — an end-state harness is only valid against a model of what each API actually retains.
- Measure a candidate harness's marginal kill rate before adding it, and delete it when the answer is zero. A seeded `PortReaper` interleaving sweep was written for exactly that check (four invariants over register/touch/configure/remove/advance, injected clock, 2,000 seeds green in 331 ms — ~6,000 seeds/s, so cost was never the question) and then run against twelve mutants. Nine die to the 19 existing unit tests *and* the sweep, one dies only to the unit tests (`schedule()`'s idempotency guard, which the `expect(sets).toEqual([])` assertion pins directly), and two survive both. So the sweep killed nothing the file did not already kill and was deleted; the surviving two were the useful part, because they say what those legs actually are: `touch()`'s early return is a map-hygiene invariant, not the resurrection guard its comment claimed, and `reap()`'s `Array.from` snapshot is defensive, since `Map` iteration is already deletion-safe. Both comments now carry the measurement. The general trap is that an interleaving harness *feels* like more coverage because it explores more states; the only question that decides it is whether any mutant dies to it alone.
- Some guards protect **progress**, not state, and no bounded assertion can pin them. `queueStartAfterStop()` chains a restart behind `stopPromise`, and its continuation calls `start()`, which re-queues whenever `stopping` is still true. Replacing that chain with `Promise.resolve()` leaves all 194 `data-bus` tests green — measured — because `start()` *separately* chains the reopen behind `pendingStop`, so every ordering assertion survives; what actually changes is that one deferral becomes a microtask busy-wait that starves the macrotask the transport stop needs, and `tests/lifecycle-invariants.test.ts` dies tens of seconds later with (the interval is host-dependent: ~40 s first, 26.8 s on the 0.21.29 re-run) `Ineffective mark-compacts near heap limit` inside `Builtins_RunMicrotasks`, reported as `Errors 1 error` plus a worker `SIGABRT` and **zero failed tests**. Two rules come out of that. (1) Before writing a guard off as unpinnable, ask whether deleting it changes *when* work happens rather than *what* it produces; if it is the former, an await-N-then-assert test passes either way, so name the fuzz as the witness at the site instead of adding a test that looks like coverage. (2) A candidate test that survives every single-leg mutation in its vicinity (four were tried against this one: the `pendingStop` gate, the `stopPromise` read, their combination, and routing `start()` on the gate instead of the flag) is characterization, not a pin — drop it, and put the measurement where the next reader will look.
- Seeded fuzzers share `mulberry32()` and `flushMicrotasks()` from `tests/fakes.ts`; do not re-implement them per file (identical sequences, so extracting them cannot change what a seed explores).
- `ChannelHub` delivers **synchronously** by default, and that default is load-bearing: 16 tests in `cluster`, `data-bus` and `stability` post a frame and assert without awaiting (re-measured on the current suite by flipping the default in `tests/fakes.ts`: **18** fail — 9 `cluster`, 6 `data-bus`, 1 `stability`, plus the hub's two own pins in `coordination-invariants`). **This file carried both the old pair (15 / reddens 17, here) and the new one (18, in the deferral bullet below) at the same time**, which is what a number that names a measurement rather than a lookup does when the suite grows under it; the pair is now the same in both places and the recipe is written beside it. `hub.setAsyncDelivery(true)` opts one hub into the browser's ordering (a post lands on a later microtask) and is used by the coordination sweep — do not make it the default. Its value is a measured control, not a hunch: with asynchronous delivery, the pre-`0.21.16` code fails six sweep seeds and the synchronous hub passes them.
- A guard that protects a *protocol invariant* belongs to the protocol, not to the handler where the first violation was found. The 0.21.5 `topicKey === createOpaqueKey(topic)` check was written into `handleControlMessage` and the rule was stated in the section above as if it covered the channel — it covered one of the two handlers that read that pair, and `handleRouteReleasedMessage` stayed open for a release. When pinning an invariant, enumerate every reader of the fields it constrains (`handleMessage`'s branches are the list) and say at each one whether it needs the check and why.
- A deferred operation must carry the **current desired state**, not a captured call, and every place that resumes on a later task needs its own check. `runTransport()` parked a closure, so a transport subscribe issued while the connection was down could flush *after* the unsubscribe that cancelled it, leaving the connection holding a channel no handler, assignment, or route owns — and it is unrecoverable, because the dedupe that prevents double-subscribes reads the very set that carries the stale entry (`0.21.16`). The two resume points, the recovery-gate waiter and the opening flush, are independent: deleting one alone fails 1 of 191 tests, deleting the other fails 2, so neither subsumes the other and "I put the check where it reproduced" would have left the second hole open. When a fix repeats a guard across resume points, mutate each one alone and record its own kill set.
- A seeded sweep is not a deterministic run. The operation sequence is fixed by `mulberry32()`, but each awaited step interleaves with the host's own queues, so a failure that appears only under load can be genuine and locally unreproducible. The assertion message is the only datum, so never pipe the run you intend to diagnose — `| grep`/`| tail` on the failing invocation has lost this file's failure once. When it is already lost, bound the failure rate by re-running under manufactured load, and record the instance as unexplained rather than as a flake.
- A long fuzz must bound depth by **wall clock**, never by seed count alone: the same sweep swings by an order of magnitude between an idle desktop and a shared CI runner, so a count that passes locally can blow the per-test ceiling there. Pattern: `MAX_SEEDS` cap + a budget check at the top of each iteration + `expect(completed).toBeGreaterThanOrEqual(MIN_SEEDS)` so a slow runner cannot "pass" on a handful of interleavings, plus a `console.log` when the sweep was truncated (that line is what makes a runner-depth regression diagnosable from a CI log instead of a mystery). Take the floor from measured mutant **kill depth**, not from how many seeds you like — the coordination harness's heaviest mutant dies at seed 12, so 100 leaves 8x the depth that detects a regression. Keep the floor an *assertion*, never a precondition on the budget check: `if (completed >= MIN_SEEDS && elapsed > BUDGET) break` is inert on exactly the runner the budget exists for, and it let a starved CI box run 1,046 seeds for 485s against a 60s fuse while reporting only a bare timeout. Demonstrated locally — with `BUDGET = 0` the gated form still ran 100 seeds and **passed**, the unconditional form stops at once and fails with `explored only 0 seeds`. Also log the **slowest seed** beside the truncation line: a fuse sampled between iterations cannot bound a seed that wedges on an `await` needing a timer the fake clock never advances, so that case is diagnosable only from the per-seed number.
- Print sweep progress *during* a long fuzz, not only when it ends. The post-loop truncation log cannot fire on the one failure it exists to explain: a host that kills the test on its own ceiling reports `Error: Test timed out in 120000ms` and nothing else. `if (seed <= 5 || seed % 50 === 0) console.log(...)` costs one line per 50 seeds and separates "this runner is slow" (depth climbing, elapsed growing) from "a seed wedged" (last line names the wedge) — different diagnoses, different fixes. Note the failure mode that produced the log line above: Vitest marks a test failed at its ceiling but the async loop keeps running, so a truncation line *can* appear in the log after the `Test timed out` line, and reading the pair in timestamp order is what identifies a single-seed wedge.
- A deadline placed at `await` boundaries bounds only **queue-yielding** waits, so never write it up as bounding a seed. Measured: forcing the coordination sweep's per-await cap to 12 ms over a full 5,000-seed sweep trips it **zero** times, because every locally reproducible slow seed is *synchronous* cost inside the first tab's `stop()` (`stops=[a=109..132 b=0 c=0]` within a 111-135 ms seed, six such seeds in the instrumented run to 112,399) — spent before a promise exists, and nothing on the same thread can preempt it. Keep the guard for the class it does cover (a timer-queue blowup in `advanceTimersByTimeAsync` is exactly that shape) and name both halves in the comment. Two consequences: the guard cannot be shown to fire *from the sweep*, so pin the mechanism directly and in both directions — never-settling await hits a 25 ms and a 0 ms cap, a microtask-settling chain beats even 0 ms — then mutation check three forms (never expires / expires eagerly / reports settled work as expired), which die at three separate assertions. And "slow" and "cut short" must be different counters: a `[CUT]` line keyed on `seedMs > cap` fired 19 times per sweep at the 12 ms setting while the guard itself fired 0, and that conflation is what wrote a false control into a CHANGELOG. Report the summary on `completed < MAX_SEEDS || cutShort > 0`, and let the depth floor count only seeds that were actually asserted.
- When a per-await deadline reports `0 cut short` on a run that still blew its ceiling, the loop is not waiting — bound it at the message pump. Two `verify` runs of markdown-only PRs failed with one seed burning 463s and `(0 cut short by the 2000ms await cap)`, and instrumentation said why: 2,642 transport `subscribe` / 2,641 `unsubscribe` calls on one tab inside ~110 ms, 18,379–26,719 `getItem` calls in a single awaited `stop()`, and **no** `:route:` writes in the same window. No time-based guard on the same thread can reach that; `ChannelHub.setDeliveryBudget(n)` can, because a coordination loop needs both halves to keep posting. Set the limit from a measured distribution (over a full 5,000-seed sweep: p50 = 28 posts, p99 = 90, p99.9 = 5,429, max = 16,763, 18 seeds over 500), never from the size of the wedge, and make a cut seed **unasserted** — check the flag at every await boundary and set the same `aborted` the cap uses, because a starved end state otherwise reports as a product failure — and **counted**: print it by name, and make the sweep-summary condition include the churn count, since a sweep that completes all its seeds while cutting some looks exactly like a clean pass. Pin it in both directions like any other limit: dropping the `return` dies at "posts past the budget must not be delivered", tripping one post early dies at "a budget must not drop anything under the limit" — two different assertions, so a guard that can never fire cannot pass unnoticed. Last: the budget is immunity, not a fix — so record *what the loop it cuts actually is*, with the measurement that says so, or the next reader will either find a green suite and conclude there was nothing there, or hunt a product bug that is not there.
- Before calling a coordination loop a product defect, check whether the test channel delivers synchronously. The loop the budget above cuts was recorded as "a real behavior and not fixed", on evidence that a tab re-elected itself thousands of times with no route writes behind it. Counting the decision points instead of inferring them: 11,542 accepts / 11,541 re-sends / **11,538 `confirmRoute` skips** / 6 confirmations, and 27,571 route reads against 6 writes at the store. That is not re-election — it is a one-step visibility gap (the owner's confirmation sits in *its own* `BatchingStorageWriter.pending` while the peer reads the flushed, still-unconfirmed record), and its only exit is a microtask boundary that `ChannelHub.send()` never yields to, because it delivers every frame on the caller's stack. Defer delivery by one `queueMicrotask` and the storm is gone: 0 seeds over 500 posts out of 1,050, against ~18 per 5,000 before. A browser cannot reproduce it — `BroadcastChannel` posts to a later task, so the flush always lands first. Two rules from that: attribute a loop by **counting the legs**, not by reading the code that looks responsible; and treat "the harness delivered synchronously" as a first-class hypothesis whenever a fake channel and a batching writer share a test, because it converts a one-shot correction into an unbounded cascade and will otherwise be filed as a product bug. Raising the harness's fidelity is not free either: async delivery makes six injected-frame seeds fail an end-state invariant (every one opens with `a:forge:<topic>`; `no subscriber left but owners=[] transportSubscriptions=[a]`), and making *every* hub asynchronous breaks **18** tests as re-measured on the current suite (9 `cluster`, 6 `data-bus`, 1 `stability` — 16 that post a frame and assert in the same stack — plus the 2 hub pins in `coordination-invariants` itself, one of which asserts the synchronous default by name), so the deferral has to be opt-in and scoped to the fuzz, and the forged frames' own `channel.close()` must be deferred by a microtask or the injection silently stops landing and the sweep goes green by losing coverage.
- Do not read a sweep total as a per-seed cost. `stopped at 1046/5000 seeds after 158476ms (slowest seed 151850ms)` divided out reads as "~151 ms per seed" and is a lie in the direction that matters: one seed ate 96% of the wall clock and the other 1,045 sum to ~6.3 ms each, which is what tells you the failure is a wedge and not a slow runner. When a number in a comment came from a division, state the numerator, the denominator and the outlier separately.
- Budget on `realNowMs()` from `tests/fakes.ts`, never on a clock fake timers can move, and keep `tests/setup.ts` restoring real timers after every test. Measured on the pinned Vitest 5 inside a window advanced 60 simulated seconds: global `performance.now()` read 60000, `process.hrtime.bigint()` moved 60000, and `node:perf_hooks`' `performance` — a *different* object in that context, which is why shadowing the global leaves it alone — read 216ms of real time. **That immunity is contextual, not inherent**: Node makes the global and the `perf_hooks` export the same object in a worker thread, so a `toFake` list containing `performance` patches both, and a budget clock that gets *frozen* reads 0 elapsed no matter how slow the host is. `realNowMs()` therefore takes the max of `node:perf_hooks` and `process.uptime()` — the latter is in no fake-timer toolbox's list, and measured a real 260ms across a 250ms `Atomics.wait` inside a fake window where `Date`, global `performance` and `hrtime` all read 0. Do not collapse it to one source. A live `performance.now()` is only safe because a seed loop reads it after the previous seed's `vi.useRealTimers()`: the same budget moved inside the window (per-operation check) reported "out of budget" after 48ms of real time and truncated the sweep at 3 seeds. Do not encode that placement dependency in a new call site — take the helper. Restoring real timers still matters, because a leaked fake `Date` travels with a reused worker and poisons the *cluster* clock: the same coordination file measured 16.4s in one full run and 60.2s alone. Reading `Date.now()` "in a real-timers window" is not enough — the *baseline* can already be fake. The sweep's own `Date`-driven cluster time stays fake on purpose.
- Pin a clock guard in **both** directions, or you have only pinned half of it. "60 simulated seconds must not cost 60 measured ones" passes trivially at 0, so a frozen clock sails through it; the assertion that catches the dangerous direction is "N ms of *real* blocked time inside a fake window must still measure ≥ N". Verified: a mutant that makes the helper return a constant passes the advance check and fails only the freeze check. Block real time with `blockRealTimeMs()` (an `Atomics.wait`) rather than `while (Date.now() < stop)` — `Date` is frozen inside the window, so a busy-wait on it never terminates; an early version of this pin hung the run that way.
- `pnpm test:coverage` only instruments `src/**` (the `coverage.include` in `vitest.config.ts`), so a leg whose *only* caller is a built artifact reads as uncovered. `tests/dual-format.test.ts`, `tests/verify-packed-consumer.test.ts` and `tests/release-version.test.ts` import from `dist/` and the packed tarball; `src/centrifuge.ts`'s two `catch` arms around `new URL('./centrifuge.worker.js', import.meta.url)` are the canonical example — esbuild shims `import.meta` to `{}` in the CJS bundle, so the CJS half of that test drives the throw while src-level coverage never sees it. Before hunting a zero count, check whether a dist-level test already drives it; record "pinned through dist" at the site (with the test name) so the next pass reads the classification instead of re-running the experiment.
- Distinguish the two tiers of the ledger when reporting it: an uncovered **line** and a line carrying zero-count **branch arms** are different claims. `cluster.ts` reached 100% lines while still holding 19 zero arms (`?? fallbacks`, conditional spreads, `typeof` guards), so "module closed" needs the tier named.
- A third tier exists, and it is **uncloseable rather than unfixed**: with `noUncheckedIndexedAccess` on, an index read is `T | undefined` whether or not a hole is reachable, so the `?? fallback` that satisfies an assignment can never run *and* can never be deleted. Two legs in `src/core/trace.ts` are this shape (`(latencyBuckets[i] ?? 0) + 1` and `seen += buckets[index] ?? 0`), as is `websocket.ts`'s `return this.connectPromise ?? undefined` (`MaybePromise<void>` is `void | Promise<void>`, which admits `undefined` and not `null`). Check for it before hunting an arm: write the bare field and run `pnpm typecheck` — TS2532/TS2322 means the counter is reporting a type artifact, so say that at the site and spend the effort where the type is blind. What the type could not see in that same function was the real defect: `Math.max(0, delay)` clamps a *negative* delay and passes `NaN` straight through, so one non-finite reading of the public `trace.now()` clock blanked the window's average and pinned its p95 and max at the histogram ceiling. `exactOptionalPropertyTypes` is on too, and it produces the same tier from the other side: `publishBatch()`'s conditional spreads (`...(id !== undefined ? { id } : {})`) cannot be written as always-present keys, because `{ id: string | undefined }` is not assignable to `{ id?: string }` — that form fails TS2769 while the whole test suite stays green, since vitest does not type check. So for these legs the compiler is the only check that exists, and `tsc` failing is the evidence the leg is load-bearing rather than a gap to close.
- A zero on an arm whose right operand is an **arrow function** is a statement about that closure's invocations, not about who omitted the argument. `this.sink = options?.sink ?? (() => undefined)` in `src/core/trace.ts` reports 0 while `tests/property.test.ts` selects that default 300 times per run — the branch region spans the arrow's *body*, and a selected-but-never-called no-op reads as unexercised. So never report that shape as "no caller uses the fallback"; check whether the fallback is ever *invoked*, and if it cannot be under the declared types, record at the site what deleting it costs (there: one `trace sink threw` warning per event, suite green either way) rather than lighting the arm up with a cast.
- A timing gate must sample the code, not the scheduler. Assert the **fastest of N repeats** (`bestOfMs` in `tests/perf-gate.test.ts`) because a real regression is slow on every repeat, and give absolute-millisecond gates their own sequential step (`pnpm test:perf`, excluded from `pnpm test`/`pnpm test:coverage` in `vitest.config.ts`) — 38 isolated workers on a runner's few cores still beat a 20x ceiling, which is what took two of the five gates down in one coverage run. Keep verifying a ceiling has teeth by mutating the hot loop, and record which weaker mutants pass so the gate's real sensitivity is known rather than assumed.
- E2E tests use Playwright with Chrome, launching a real demo server.
- A background experiment's red is not a product signal until it has been re-established on a committed tree, and it is not *evidence* at all unless the observation was written down when it happened. Two sweep reds in one session turned out to be unreproducible and unrecoverable: they arrived as completion notices for commands started before a context break, their output files lived in the previous session's temp directory, and the tree they ran on carried uncommitted harness knobs (a raised `MAX_SEEDS` and a changed per-await cap) — so nothing survives about what they actually measured. Before filing such a red, copy the seed list and the failing assertion text out of the log and re-run the file at committed settings; if it does not reproduce, say exactly that (attempts, timings, and that the configuration was never committed) rather than leaving an open defect claim that no later reader can adjudicate. Corollary: the sweep must print its effective caps in its own summary line, because that print is the only thing that makes a run's configuration reconstructable.
- When a red cluster E2E run lands on a loaded host, adjudicate it with CI's `browser` job on the same commit rather than by re-running locally, and check whether the changed code is even reachable from that path before believing the correlation — a release that touched only `recordDispatched()` failed 4 of 37 local Playwright specs at 90 s/150 s budgets while the demo under test never constructs a trace reporter (`enabled` false → `metricsActive` false → the method returns on its first statement). Record the local red with its load numbers; do not waive it, and do not treat it as a product failure either.
- E2E topic names must come from `uniqueTopic()` in `e2e/topics.ts`, never from `Date.now()` alone. The name doubles as a Centrifugo channel and `serverSubscribers()` counts connections to it **across the whole server**, so the "exactly one tab holds this topic" assertion is only meaningful if no other test shares the name. The same trap one level up: a *total* connection count from `/debug/connections` is server-wide, so it is not an observable for any single test — `playwright.config.ts` runs specs in parallel and every live tab is in the number. That mistake read as runner load for months until `--repeat-each=3` of the offending spec failed 3/3 at its own precondition. Address a socket by the channel it holds (and remember that in a shared cluster only the *owner's* transport subscribes a channel, so two tabs of one topic show one channel-carrying socket and one with none). `playwright.config.ts` runs `fullyParallel: true` with the platform default worker count off CI (and `--repeat-each` multiplies it), so two instances of one case can start in the same millisecond: that collision made a correct two-cluster pair look like a stuck 2-subscriber channel and burned a 30s poll to prove it.
- A handoff/teardown E2E must make the *sender's* cluster view a precondition it waits for, not a race it hopes to win. Delivery is at-most-once, so a publication written while the sending tab still addresses a departed worker is gone for good, and polling for it after the fact cannot distinguish "slow" from "unrescuable" — it just fails at the timeout. Read `getClusterSnapshot().routes` from the sending page (both example pages expose the bus) and wait for the dead worker id to disappear before publishing.
- An E2E gate cannot depend on which thread the browser puts a SharedWorker's timers on. Blocking a tab's main thread past the port session timeout does reap the port on desktop Chrome, but on headless Chromium the reaper's clock rides the blocked thread: no tick lands during the stall, and the queued PING is processed before the next one, so the precondition never occurs at all. Measured three ways — the same spec passed locally in 36.7 s and failed 3 of 3 on CI, each attempt ending with the channel still held by the *original* socket and the demo's health line reading `健康 · transport 就绪 · 恢复 0/Infinity · 无失败记录`. Test reaper behaviour at unit level with injected timers and clocks (`tests/port-reaper.test.ts`), and record the browser consequence as a measurement rather than as a check that only fires on the machine it was written on.
- An option whose effect is to *stop sending* something has to be checked against what the peer does when it stops receiving it. `heartbeatIntervalMs: Infinity` is accepted by `assertHeartbeatInterval`, documented in `docs/configuration.md` as the way to switch the SharedWorker PING heartbeat off, and pinned on the sending side by a test that no `PING` ever arrives — while `PortReaper.setTimeout()` filed `Infinity` under "malformed payload" and fell back to the 10 s default, i.e. a 30 s session timeout. So the one configuration whose purpose is "this port will never prove liveness" guaranteed the port would be reclaimed 30 s after connecting, in shared mode, and each half had a green test. When you pin a value's meaning on one side of a boundary, read the peer's guard for the *same* value in the same pass: a `Number.isFinite` check written to reject `NaN`/`0`/negatives also swallows `Infinity`, and only the caller's contract says those are different categories. The stale premise was visible in the old test's own comment, which listed `Infinity` among malformed payloads — a test that asserts a fallback cannot also be evidence about which inputs deserve one.
- Headless Chrome cannot be used to study background throttling, so a probe that measures "what a clamped heartbeat does" has to assert its own precondition. Two pages in one context plus `blocker.bringToFront()` left the first page at `document.visibilityState === 'visible'`, and a 100 ms in-page `setInterval` counted 2405 of a possible 2405 ticks over a 240 s window — no clamp at all, because Playwright passes `--disable-background-timer-throttling`, `--disable-backgrounding-occluded-windows` and `--disable-renderer-backgrounding` in its default Chromium switches (`chromiumSwitches` in `playwright-core/lib/coreBundle.js`). Dropping them with `ignoreDefaultArgs` is necessary and still not sufficient headless: measured, the tab never became hidden. Put the precondition read (visibility *and* tick count) inside the probe, because the result worth having is the one this instrument cannot produce, and a run that measures nothing prints like a clean pass. Headful Playwright fails the same way, and for a different reason worth knowing before a third attempt: a context gives each page its own window, so nothing is ever occluded (`bringToFront()` left the first page at `visible` with `hasFocus()` still true), `Target.createTarget` over a browser-level CDP session creates a target without hiding the first page, and this Chrome build exposes no `Emulation.setPageVisibilityOverride` or `Emulation.setFocusOverride` at all — only `Page.setWebLifecycleState`, which *freezes* the page (the starvation case above, not the clamp). The only route left runs through the desktop, which is not worth taking for one sentence: what survives as the open question is the arithmetic — 3 × the default 10 s heartbeat is below Chrome’s published ~1-minute floor for a long-hidden page, so a *sustained* clamp is expected to reclaim and rebuild once per period — and that stays recorded here as arithmetic on our constant plus upstream policy, not as a measurement taken in this repository.
- `await expect(promise).rejects.toThrow('message')` **also passes when the rejection reason is `null` or `undefined`** (verified on the pinned Vitest 5). That silently voids the assertion for exactly the bug the `reason ?? new Error(...)` fallbacks prevent, so those fallbacks cannot be pinned with `rejects.toThrow`. Use `expectRejectionMessage()` from `tests/fakes.ts` instead whenever the message itself is the contract; plain `rejects.toThrow` stays fine for rejections whose value the test itself constructed.
- Same trap one level up: `toThrow(TypeError)` cannot tell a validator rejecting an option from the validator's own message builder throwing while naming it, because both are a `TypeError`. Where the message is the contract, assert the message. `0.21.4`'s bug is the instance: `String(Object.create(null))` throws `TypeError: Cannot convert object to primitive value`, so any `String(error)` on a rejection reason — in a recorder, a message template, or an enum membership test — is a failure-reporting path that can fail. Render such values with `describeFailure()` from `src/utils/error-utils.ts`, and give the fake a way to set the rejection's **value** (`FakeTransport.stopRejection`), not just the fact of failure, when a test needs the distinction.
- `pnpm verify:types` (`scripts/verify-public-types.mjs`) is the only gate that can see a **type-only** export, because a declaration emits no runtime key: `tests/dual-format.test.ts` freezes `Object.keys()` of the built barrel and `verify:compat` compares package.json fields, so both are blind to it. Its two checks have **measured, disjoint** sensitivity, so neither is redundant: deleting `WorkerAvailability` from the barrel dies to the closure walk alone (with the walk neutered the same mutation exits 0, so the baseline leg cannot see it), and deleting `DEFAULT_MAX_ACTIVE_WORKERS` — which no public *type* references — dies to the baseline comparison alone (with that leg neutered the same mutation exits 0). Do not simplify one leg away to "dedupe" them. Do not simplify one leg away to "dedupe" them. The closure rule requires the name to be **exported by its own module**, and that filter is a decision with an instance behind it: `WorkerUnsafeOption` in `src/centrifuge-protocol.ts` is reachable from `./centrifuge` through `Omit<Partial<Options>, WorkerUnsafeOption>` and a mapped-type key, and stays module-local on purpose — `tsc` resolves it away, and a consumer never inhabits that position, so exporting it would add a public name nobody can use. Deleting the filter makes the gate report it again (measured). Design note, because the alternative looks natural and is not usable: walking *types* (`getPropertiesOfType` + type arguments + call signatures) needs a depth bound, and loosening that bound from 7 to 14 exhausted a 4 GB heap on this surface, so the bound was load-bearing for cost rather than safety; the syntax reference walk is bounded by the repository's own finite type symbol set instead. Its one real limit: a name that `tsc` inlines away rather than emits is invisible here.
- When a limit is raised because something exceeded it, check that the gate asserting it would have **rejected** the old value. `tests/workflows.test.ts` pinned the release workflow's published-consumer retry budget at `totalMs >= 5 * 60 * 1000`, and the 360 s budget that had just turned a good release red satisfied it — a floor below the value that broke is decoration, and it would have kept blessing lower ones. The fix is to write the floor as its **derivation** with one named constant per term and the lookup beside it: 0.21.31's is one packument cache lifetime (`max-age=300`, read from `curl -I https://registry.npmjs.org/<pkg>`) plus the measured worst-case `npm publish`-ack → `time[<version>]` gap (310 s; subtract the `Publish to npm` step's completion from that stamp via `gh api repos/<owner>/<repo>/actions/runs/<id>/attempts/<n>/jobs`, taking `<n>` from the run's `run_attempt` — see the bullet below about which attempt to read). Then mutate: set the workflow value back to 48 and confirm that one test reddens with a message naming both terms. Two side effects of doing it. Deriving the number explained a regularity that had been filed as noise — every green release whose consumer step actually waited consumes **301–309 s** of the ceiling (re-derived over the whole series at `0.21.36`: 42 of 45 measurements inside 300–310 s, `0.21.30`'s 364 s budget exhaustion the only reading above, and every sub-2 s row belonging to a release before `0.20.92`, where nothing had to propagate), which is what one cache lifetime looks like from the outside. That band replaced a 304–308 s figure this bullet had copied out of a ten-run subset — the full sweep widens it three seconds at the bottom and one at the top, which is the same error one level up: a range named from the samples nearest the cursor. Take any of these from each run's `Verify published npm consumers` step (`steps[].started_at` / `completed_at`, the same `gh api` call the attempt selection uses), never from a sentence that is one release behind by construction — and it exposed that the *story* written hours earlier about that same failure was wrong in a checkable way: a note claiming "the document lagged the artifact, both statements true in the same minute" put the stale-document read at 21:43:15Z and the registry's own record of the version at 21:44:12Z, so the read was correct and nothing was lagging an artifact that existed. Before restating a failure narrative, read its own timestamps; the sequence `ack → record → HEAD 200 → local pack OK → runner still ETARGET` is what distinguishes "the publish didn't happen" from "your read path is five minutes old", and it is why you probe from the machine that is failing. **And when an observation is cited as evidence *against* a model, check both of its halves came from the same machine.** That same note argued against a purely-local-cache account by joining "a fresh read succeeded at 21:44:35Z" with "the retry resolved in 1 s at 21:47:10Z" — but the first was a laptop's `npm pack` and the second a *new runner job* whose cache starts empty, so the pair is what the local model predicts, not what refutes it; 0.21.32 then produced the model's cleanest sample (record 76.1 s in, a laptop reading `latest` at 151 s, the runner taking 304 s — one lifetime after its own fetch), and 0.21.33/0.21.34 repeated it at 75.5 s and 75.8 s: three consecutive records inside half a second while the step stayed at 304 s, which locates the variance in the reader rather than in the write. Retract the joined sentence where it stands, and name the one reading the surviving account genuinely cannot explain — here the runner's own `ETARGET` ~60 s after that copy must have expired — so the verdict stays "not established" instead of resting on a cross-machine comparison.
- **A recipe that names a parameter but not how to choose it reads as complete, and its default answer is a different quantity.** The lookup above is `…/runs/<id>/attempts/<n>/jobs`, and three surfaces carried the `<n>` unfilled (one of them glossed it as "attempt 1 is the failed one", which is true of exactly one tag in the series and of none of the other eight, whose runs have a single attempt). `/jobs` with **no** attempt segment returns the *latest* attempt, so the obvious short form answers a different question than the recipe asks: measured on `v0.21.30` (the one re-run release, `run_attempt` 2) the default read gives a **-177.0 s** gap and a **1.0 s** verify step, because that attempt republished nothing and the consumer check found the version already in the registry — while attempt 1 holds the real 310.0 s and the 364 s failure the prose describes. Negative is the lucky case: a re-run whose step also succeeds prints plausible numbers, so the series silently changes membership while keeping its shape, and "the range over nine releases is 74.8–310.0 s" stops being comparable to the previous reading. Two rules come out of it. (1) Say at the recipe **which attempt holds the event being measured** and take `<n>` from `run_attempt` rather than from a guess. (2) The control for a re-derivation is the number you already believe: when a fresh measurement disagrees with shipped prose, decide *which of the two is broken* before editing either, and have the instrument print the attempt it read plus one line per tag it could not resolve, because an instrument that loses members reports a shorter, cleaner-looking range as a result. **Run it wide and the same trap moves one level down.** Re-derived over every release tag from `0.20.85` on (take the denominator from the sweep's own `measured=N of M tags` line rather than from here — it grows every release, and this sentence has already been stale twice) the sweep needed a `gap < 0` branch — and three releases genuinely land there (`0.20.88`, `0.20.90`, `0.20.91` at -0.6/-0.3/-0.1 s, the registry stamping the version *before* the ack returned), with `0.20.86` and `0.20.87` at +0.2/+0.3 s immediately beside them; all five have a 1.0 s consumer step, while every release in the sweep's modern window took at least the floor that sweep prints — its own `modern floor:` line, 74.8 s on the latest run — to be recorded. The first version of the script treated a negative as a failed read and printed those three as `NO-PUBLISH` — real readings reported as absences, which is precisely the "gap that was never there" bias this file has recorded twice. Two lessons in the output: an unexpected **sign** is a regime until proven otherwise, so classify it and print it rather than filtering it; and the datum that separates a real reading from an artifact is the one already in the same response — here the `Publish to npm` step's own wall time (13–36 s for a publish, 1 s for the `already published; skipping` branch, which is what made `v0.20.85`'s re-run read -529.2 s).
- **A verdict read off a successful fetch is only as good as the system that was fetched, and a test that injects the value cannot see which one it was.** `scripts/verify-release-version.mjs` refuses a tag when a CHANGELOG section names a version the registry never recorded, and its header reasoned that the inference is safe because "`time[]` records never expire, so a version missing from a read that succeeded was never published". Measured while preparing `0.21.35` on a host whose `npm` is configured to `registry.npmmirror.com`: that read *succeeded* and answered `CHANGELOG names 1 release(s) the registry has never recorded: 0.21.34` about a version npmjs had recorded 3,207 s (53 min) earlier — the mirror's own copy still reported `modified` at the `0.21.33` publish, and did not learn of the version until 3,418 s after the record. (This bullet first said “75 minutes”, which is the gap between two previous stamps.) Both halves of the sentence were true except the word "the": a mirror that has not learned of a version is not a record of its absence. **So when a check's conclusion is an absence, name the authority in the call** (`--registry https://registry.npmjs.org`), and note that `cache-control: no-cache` does not help — a sync lag is not HTTP caching, and a no-store request returns the same stale document. `verify-published-consumer.mjs` already pinned it on both of its reads; this was the last npm-based registry read in the repository that did not, and both checklist languages carried the unpinned form as the *re-derive this number* instruction. The other half is why it survived: every case in `tests/release-version.test.ts` injects `RELEASE_REGISTRY_TIME`, so none of them executes the path that chooses a registry — a gate whose tests all stub the value under test is silent about how the real one is obtained. Pin that path by running it: the new case puts a stub `npm` on `PATH`, lets the script call it un-injected, and asserts the flag and the URL arrive **as a pair** (three mutants — drop both, wrong URL, URL without the flag — die at three different assertions).
- A percentage gate on a metric with a multi-modal history needs an **absolute** leg, or the summary statistic becomes the thing that fails. `bench:compare` baselines on the median of the last five reports; a median is stable only while the modes stay mixed, so when several consecutive reports land in the same mode the median itself jumps and a comment-only tree read +121% against one baseline and −56.7% against another. The fix (`findRegressions` now also requires beating the highest baseline sample) has a stated cost — a *sustained* shift that stays inside the observed range is invisible — so the suppressed rows are printed under `within-baseline, not gated` rather than passing silently: an excuse the reader cannot see is indistinguishable from a clean bill. Measured on the live archive at `--fail-above-pct 5`, three of the seven metrics (`publish/dedicated`, `publish/shared`, `publishBatch1000Ms`) were already inside their own five-report range, which is the honest statement of that gate's resolution. Mutation-check both legs: dropping the ceiling condition fails only the new multi-modal case, and hard-coding it to `false` fails four tests including the pre-existing explicit-pair path — that asymmetry is what says the null-ceiling leg is real behavior, not decoration.
- `docs/benchmarks.md` and `docs/zh/benchmarks.md` are **generated** by `scripts/bench-trend.mjs`'s `buildDocs()` (`pnpm bench:trend`); the prose paragraphs naming the gate live in that function's `en`/`zh` arrays. Editing the markdown loses the next regeneration and leaves the two languages out of step — change the generator, re-run it, and commit the tables it refreshed in the same commit.
- When a comment proves a promise resolves by pointing at a terminal `.catch(handler)`, the proof is only as good as `handler` being total. Two released versions deleted absorbers on exactly that argument while assuming the handler (`reportError`) could not throw; it could, and the assumption was load-bearing. State the assumption next to the enumeration, and pin the chain's observable end with a test.
- A prose-audit candidate is a claim, not evidence — including when it arrives as a confident report naming files and line numbers. One such pass quoted two sentences ("the five dispatch events", "the five internal modules") that do not exist in either language of the file it cited, reported `scripts/verify-packed-consumer.mjs` as unable to fail when every check in it `throw`s and `await main()` therefore exits 1, and claimed two names were missing from its consumer script that sit at its lines 96-125. Adjudicate a collected list with a *second* reader whose brief is to assume the first is wrong, and require of each item a verbatim quote found by searching for the phrase (`grep -n "五个 dispatch\|five dispatch"`) rather than a line number — a count claim's sentence may be fabricated, and only the search distinguishes that from a drift. Note the same pass was reliably right about *code-behaviour* claims it read at the cited lines and wrong about *existence* claims about prose it did not quote.
- A number in a *commit message* is the same class of claim as one in a shipped comment, and it gets checked less because it reads like narration. One release's branch commit asserted "first browser samples since 0.21.16" and "twelve releases of recorded deferrals"; both were wrong and both were checkable from the repository before pushing — `git show <tag>:docs/benchmarks.md` carries a "Data through … / N archived reports" line that pins the doc's real vintage, and the local archive gives the count. Two consequences. (1) Squash-merge discards branch commit bodies, so false text never reaches `main`; the correction has to go where it survives (the PR body and `docs/progress.md`), and the log entry should name which artifact carried the error rather than quietly rewording it. (2) A state claim copied forward through successive log entries inherits the first copy's error: "0.21.12-era sample" appeared three times while the file had in fact been regenerated at `v0.21.13` — 71 reports and a `55.44 → 50.61 ms` dedicated row at 0.21.12, against 87 and `38.47 → 36.59` from 0.21.13 on. Re-derive such a phrase from the artifact once, then fix every copy by grepping its distinguishing substring rather than re-reading the files that might contain it.
- When a correction *rewrites* an enumeration, the new list is a fresh claim about a type, not a safe default. Rewriting `docs/configuration.md`'s storage-boundary section removed the false absolute ("Storage does NOT hold publication data") and replaced a three-bullet list of what storage does hold — which the follow-up check found was not exhaustive against `WorkerRecord`: `protocolVersion`, `role`, `registeredAt` and the optional `throughput` sample are all persisted. Enumerate from the interface (`grep -n "" src/core/types.ts` over the record's own block), prefer naming the records over naming their fields, and re-read your own inserted list before believing the correction is finished.

- The Quick reference above was assembled from the CI workflow's steps, and the reason it now lists `pnpm lint` and the bench trio is that they were missing: `pnpm check` is `typecheck && build && test && test:perf` and **nothing in it runs eslint**, while `ci.yml` runs `check` and `lint` as two separate blocking steps in the verify job. So an agent that trusts the command list can be locally green on every gate it knows about and still redden CI on a style rule. When a gate is added to the workflow, the fix that keeps this from recurring is to re-read the table against `grep -n "run: pnpm" .github/workflows/ci.yml` rather than against memory — the command list and the workflow are two artifacts, and only one of them is the source of truth. (`test:e2e` was already there; `verify:published` and the `bench:*` trio were not, and the last two are the only way to check the published artifact and the browser-baseline gate at all.) Writing that instruction did not close the class: `pnpm audit` was *already* a blocking step in the same job and stayed out of the table for another cycle anyway, because a rule that asks someone to re-run a comparison is a rule that gets skipped under load. The comparison is a test now — `tests/workflows.test.ts`'s 'lists every gate the workflows run in the AGENTS.md quick reference' reads every `run: pnpm …` in `ci.yml` and `release.yml`, except the two provisioning commands (`install`, `exec`), and requires each to appear in a table row here. It went red on the tree that added it, naming `ci.yml runs pnpm audit`, and three mutants die at three distinguishable values: delete the row and it names `audit`, drop the provisioning exclusion and it names `exec` (which is what proves that exclusion is a decision rather than a dodge), invert the table-scan filter and it names the whole set while its own `documented.size` floor catches a scan that read nothing.

- **A gate's scope comment is a claim about its own array, and the two drift in opposite directions.** The citation gate's comment says it walks "every living prose surface", and the array below it named `AGENTS.md`, `README.md`, `docs/**` and `src/**` — not `README.zh.md`, the other shipped root README. Measured by running the gate's two regexes over both readings: the missing file carries **0** citations today, so the omission is invisible to the gate itself and **cannot be pinned by any test** — a zero-citation file fails nothing whether it is scanned or removed. That is the half worth writing down: adding it produced a comment that no longer overstates the scope and a next-zh-citation that gets checked, and it must be reported as exactly that, because "widen the gate" reads to a reviewer like "the class was covered". The mirror case is the same sentence: its numbers (`18 citations in 9 of the 53 files`; `src/**` 13, `AGENTS.md` 4, `docs/architecture.md` 1) were right and one of them, `52 files`, was wrong the moment the array grew — so re-derive the scope's *size* from the array after touching it, not from the previous measurement. **That re-derivation went stale in the next cycle too**, to `54`, when `CONTRIBUTING.md` joined the same array: the citation *content* figures (18 in 9 files) held exactly, and only the denominator moved, because a scope size is a property of a list someone edits and the citations are a property of prose that nobody rewrites. So the size is the figure to replace with a lookup, and the counted content is the figure worth keeping — which is the opposite of what this file's rule about coverage totals implies for any number that names an array length. And a corpus total that the suite moves on its own schedule is not evidence at all: this cycle's new test case made the comment's own `977 titles / 937 from tests/` stale on arrival, which is why those are gone in favour of printing the reduced total, under the same rule that retired the coverage counts. **Finally: when a widening touches several lists, probe each one, because they do not all have the same teeth.** `CONTRIBUTING.md` was added to three gates of this file in one commit — the citation scope, the empty-section sweep, and the relative-link check — and only the first is unpinnable. Measured by a two-way probe (`.gate-logs/probe-contributing-teeth.mjs`): doctor the file with one violation of each kind, run the gate with the array entry, then run it again with *only* that entry removed. Both new legs came back `FAILED(1)` naming `CONTRIBUTING.md` with the entry and `PASSED(21)` without it, so those two additions are preventive coverage that provably fires; the citation leg has 0 citations to fire on and stays exactly what the sentence above calls it. The rule is not "a widening is decoration" — it is that *one* number (the citation count) decides whether a scope entry is observable, so ask it per list rather than characterizing a commit by its weakest member. Same probe shape answers "does this guard I just added reach anything" for any file-list-scoped gate whose new member may be empty.
- **A directory walk is not a scope, and an unfiltered one silently reads generated output.** `listDocumentationFiles()` in `tests/documentation.test.ts` returns *every* file under a directory — no extension filter — and the privacy gate walks it over `examples/`, where `pnpm build:examples` writes `examples/react/vendor/react.esm.js` (1.13 MB) and a 1.75 MB map, both in `.gitignore`. So that gate was regex-scanning ~2.9 MB of somebody else's bundle on every run, on the machines that had built it: CI's `verify` job runs `pnpm check` (which never builds examples) and the `browser` job that does build them runs no vitest. The direction that matters is the failure, not the cost — a `@scope/…` specifier or an absolute path appearing inside a vendored artifact reddens a docs-only PR **at home while the merge gate stays green**. That is the *mirror* of the understated-threshold error fixed in `CONTRIBUTING.md` in the same cycle (a doc claiming a lower bar than the config enforces is green at home, red in CI), and both are one defect: a gate or a sentence that reads different bytes than the thing it describes. Measured before deciding what to call it: all three forbidden patterns hit **0** times in both artifacts, so this was scope instability, not a leak, and the fix is to express the scope against the repository's own index (`trackedFiles()` = `git ls-files`, the same source the layout gate uses) rather than against disk. The predicate gets its own case on **fabricated input** — `keepTracked(['README.md', 'examples/react/vendor/react.esm.js'], …)` and `keepTracked(['examples/demo/demo.js'], …)` — because a case that sampled the working tree would mean something different on a clean checkout than on a built one, which is the exact defect. Mutation record (`.gate-logs/probe-tracked-scope.mjs`, three legs with distinguishable messages): drop the filter → `expected [ 'README.md', …(1) ] to deeply equal [ 'README.md' ]`; invert it → `expected [ Array(1) ] to deeply equal [ 'README.md' ]`; over-eager (`&& !file.startsWith('examples/')`) → `expected [] to deeply equal [ 'examples/demo/demo.js' ]` plus the privacy gate's own `toBeGreaterThan(0)` floor, which is the second defence and the only one that lives in the gate itself. Note that the first two mutants die to the *same* leg — an inverted filter looks like a second direction until you read which assertion produced it — so the retention leg needed its own third mutant rather than credit for two. And the probe needed its own control twice over: its summary regexes matched nothing until the ANSI escapes were stripped (piped output, so `\x1b[31m` sits *inside* `Tests  N failed`), and the `× <test name>` line names only the test, which is why attribution here goes through the expected-value text.
- **The map section is a gate, not a courtesy.** `AGENTS.md`'s `## Directory layout` had drifted to naming 16 of the 43 files under `tests/` while keeping a one-line-per-file shape, so every harness, prose gate, release gate and bench file added after the block was written was invisible to the session that reads it first. It is now complete over `src`/`tests`/`e2e` and pinned by a case in `tests/documentation.test.ts` whose three legs each fail a different assertion (measured): a deleted entry fails the `toEqual([])`; a filter that can never report dies to the doctored-block control; and **moving the section anchor fails an edge guard rather than passing** — which is the leg that matters, because a fence extraction that slips one position makes "every name appears in the block" true of a document that merely *discusses* those files elsewhere. When a check reads a substring out of a prose file, pin the substring's edges, not only its contents.

## Common tasks

**Add a new topic owner selection strategy**: Add a pure function in `routing.ts`, test it in `tests/routing.test.ts`, then wire it in `cluster.ts` `subscribe()` / `reconcileSubscriptions()`.

**Add a new control message type**: Add the union member in `types.ts` `WorkerClusterMessage`, handle it in `cluster.ts` `handleMessage()`, add a test.

**Adjust heartbeat/worker TTL**: Pass `heartbeatIntervalMs` / `workerTtlMs` in `WorkerClusterOptions`. Defaults: 3s heartbeat, 10s TTL. Worst-case dead-owner detection = heartbeatIntervalMs + workerTtlMs ≈ 13s.

**Trace/diagnostics**: Pass `trace: { enabled: true, sink: (event) => ... }` to `CrossTabDataBus`. Events include lifecycle, status, subscription, coordination snapshot, and periodic metrics (throughput, dispatch latency percentiles).

## Product Boundaries (thesis → queue, not checklist)

The project is feature-complete: follow-up work is verification and deepening of the existing library surface — never new library capabilities. Queues derive from this boundary; parked ideas are not backlog items unless their entry conditions are met.

### In Scope (library thesis-internal queue)

- 12-arm ledger receiver-trust verification (the batched PUBLISH `message.items` and `ROUTE_RELEASED` key/topic questions are closed as of 0.21.6; what remains is arms whose consequence is a behavior no test names yet)
- Bench baselines and trend-document corrections
- Transport protocol compatibility fixes (Centrifuge / native WebSocket)
- Patch releases

### Out of Scope (parking; every item carries entry conditions)

- Server-side coordination component: entry = repositioning the library as a full-stack solution
- New framework adapters beyond React/Vue 3: entry = community demand + maintenance commitment
- Durable storage backends beyond replay ring buffers: entry = extending the library thesis

### Upstream Blocked

- Centrifuge major-version upgrade: unblocks = peer transport protocol change, with regression verification
- **TypeScript 7.** Measured 2026-09-25: npm's `latest` is `7.0.2`, this repo is on `6.0.3` — which is the
  newest 6.x published (the 6.x line is `6.0.2, 6.0.3`) — and `pnpm outdated` reports **that one row and
  nothing else**, so `typescript` is the project's entire outdated-dependency surface. Unblocks =
  `typescript-eslint` declaring 7: its latest's published peer range is
  `typescript: ">=4.8.4 <6.1.0"`, so 7 is outside what the lint dependency declares, and `pnpm lint` is a
  separate blocking CI step that `pnpm check` never runs. Re-derive both sides before acting —
  `npm view typescript version --registry=https://registry.npmjs.org` and
  `npm view typescript-eslint peerDependencies --registry=https://registry.npmjs.org` — and do not read
  "7 is latest" as "we can move"; this repository's npm is configured to a mirror, so an unqualified
  `npm view` answers a different registry than the one whose release you are timing against.

### Boundary Review Signals

- Centrifuge breaking protocol changes
- An ability gap inside the library thesis that verification/deepening cannot cover
