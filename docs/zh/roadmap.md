# 路线图

0.21.40 已于 2026 年 9 月 25 日发布。项目会先持续完成可靠性发布，再进入 1.0.0 稳定性冻结。

## 0.21.41 已完成范围

- **随包发布的发布清单现在写明了：发布运行死在自己的 tag 上时该怎么办。** `0.21.39` 恰好到达过这个状态——运行在自己的 `pnpm check` 里失败，于是 tag 存在而产物不存在——而清单给的三条路（“针对不变的 tag 重跑”“发布新的 patch 版本”“禁止移动已发布的 tag”）都不覆盖它：按第二条走会把一节从未发布的版本留在 `CHANGELOG.md` 里，随后完整性门禁会拒绝之后的每一个 tag。两种语言都写清了全过程：两次确认读取以及它们各自的撒谎方式、删除重建本质上就是两次普通推送、以及预防性那一步——先在本地建好 tag，**带着 tag 存在**再跑一遍门禁，全绿之后才推送 ref，因为这个构建状态是 PR CI 和 `main` 推送 CI 都永远看不到的。
- **发布门禁打印出来的补救措施，被改写成真正能执行它的形式。** `scripts/verify-release-version.mjs` 会在 `CHANGELOG.md` 点名一个从未发布的版本时拒绝打 tag，并让读者“把该节标成从未发布过的版本”；拿导出的谓词实测、并配一个原样保留的章节作为对照，`## [0.20.91] - never released` 仍然和它想豁免的那个章节被同样拒绝，只有让标题脱离方括号那种形状才会被豁免。报错信息现在点名的就是这个形状，并由三条用例钉住、每个变异体各杀一条断言：加关键字豁免、让正则匹配裸标题、以及把措辞改回原来的模糊说法——前两种是只看行为那两条腿区分不出来的。

## 0.21.40 已完成范围

- **发布说明描述的是它自己的修复已经删掉的那条规则。** `0.21.39` 的第三条要求路线图的 preamble 写出*已打标*的最新版本；而 PR #328 —— `0.21.39` 之所以存在的理由 —— 把它换成了"`package.json` 版本之下最大的那个标签，或者等它打上标签之后就是它自己"，并且只改了测试和 `AGENTS.md`。两种语言的路线图副本都在原处更正；`CHANGELOG.md` 里的那一份照它发布时的样子留着，和 `0.21.35` 那句三个样本的结论同等对待。
- **这条规则现在跨越整个发布周期被检验，而不是停在克隆恰好处于的那一瞬间。** `allowedPreambleVersions()` 从读真实文件的用例里抽出来，铺成十行：准备、标签构建、记录提交、落后两个发布、preamble 写了一个从未打标的版本、克隆里带着一个更新的标签，以及两位数小版本号（按字典序排序会挑错赢家）。前后都是量出来的：只有真实文件用例时，这条规则三条腿里的两条 —— 包括让 Release run 159 失败的那一条 —— 对所有朝它们下的变异都存活。写成表里的行之后，四个变异都会死，而死在哪儿本身就是结论：两个落在"标签构建"那一行，一个落在"两位数小版本号"那一行，而"承认某个已发布版本自己的标签"这一条，在真实文件里**哪儿都不死** —— 在准备阶段的那棵树上，`package.json` 的版本还没打标签，这条腿对任何终态读取都不产生可见贡献，所以它只能在合成出来的瞬间里被判定。
- **滞后扫描打印的中位数，按它本来的名字说明。** 那里的 `p50` 是中间偏上的那一个元素，不是中间两个的平均值，所以往奇数长度的序列里再加一个样本，数字自己就会动：现代窗口从 `97.3` 到 `126.4`，而同样那批值的插值中位数是 `111.85`。钉在 `tests/publication-lag.test.ts` 里 —— 那里 `n=2` 的 fixture 是唯一能把排序后的列表整个暴露出来的形状。

## 0.21.39 已完成范围

- **变更日志里有一个发布被标成了一个什么都没发生的日期。** `## [0.20.10]` 写的是 2026-09-02，而它的标签（`2026-09-03T12:27:29+08:00`）和它的 registry 记录（`2026-09-03T04:28:11Z`）是同一个瞬间，且在两种读法下同一天；它前一个发布的标签打在 09-02 的 21:33，所以那行标题只是没跟着往前走。这条已经改正，而在带标签的 151 个有日期的段落里，它是唯一一个落在自己标签两种读法之外的。
- **新增的检查以标签为准，而不是以 npm 为准，而且这个选择是量出来的。** `tests/documentation.test.ts` 现在要求每个段落日期，是其标签瞬间在 UTC 或 `+08:00` 任一框架下的日历日。把同一条规则改成用 `time[<version>]`，会报出三个错段落而不是一个，因为发布完成的时间跨过 UTC 零点的情况多到不能忽略：`0.4.0` 的标签是 2026-08-30T21:28Z，记录却是 2026-08-31T00:03Z，`0.20.85` 同形。那三个"发现"里有两个是检查自己错了。
- **为什么两种框架都收，以及一次空读取为什么骗不过去。** 只收一种框架会让几十段各自描述真实事件的段落被判错（实测：84 段两种框架都对，46 段只在 `+08:00` 对，20 段只在 UTC 对），所以规则是"标签的某一种读法"，不是"本地那一天"。对被检查段落数的下限，会把一个没有标签的克隆变成失败而不是干净通过；而要求被检查的行同时覆盖两种标签对象类型，挡住的正是它的静默版本——本仓库两种打标签方式都有，而只有 `%(creatordate)` 对两者都给出答案。 同级用例如从两种语言的路线图中读出同一个说法。那里的日期是写出来的（`September 25, 2026` / `2026 年 9 月 25 日`）而不是 ISO 标题。它所写的版本必须是 `package.json` 那一个之前最新的已打标发布，或者在它的标签已经存在之后，就是 `package.json` 自己 —— 因为 preamble 由记录提交推进，而那次提交落在标签**之后**，所以在某一个发布自己的标签构建上，最新的标签恰恰是 preamble 还没写到的那个版本。`0.21.40` 更正：这句话最初要求的是"已打标的最新版本，而不是 `package.json` 的那个"，而那正是让 Release run 159 失败、并由同一个版本的修复只在测试里替换掉的规则。

## 0.21.38 已完成范围

- **一个比例在一种语言里被写反，另一种语言写对了。** 本文 `0.21.17` 那段说 `storage-fail` 交错"从 seed 414 起就让 sweep 失败，这比 sweep 的深度下限低四倍"。下限是 `tests/coordination-invariants.test.ts` 里的 `MIN_SEEDS = 100`，而 414 是它的四倍*以上*——这正是那句话的本意：这条发现需要的深度超过了 sweep 自己断言的最小值，所以它是发现器而不是守卫。中文镜像的方向本来就是对的，于是两种语言每个数字都一致、关系却是反的。逐值比对看不见这种错；找到它的按块配对扫描比较的是两种语言的第 N 个发布块，再比块里的每一句。
- **两种语言现在写出同一个工具的同两个数。** 英文 `0.21.37` 那段只说"每次调用带一个超时"，中文那段说 30 秒超时。`scripts/publication-lag.mjs` 里是 `CALL_TIMEOUT_MS = 30_000`，现在英文也带上了这个数值。
- **其余未动。** 库代码、导出、选项、默认值、帧格式与存储键均未改动；零计数分支账本与覆盖率聚合仍是 `0.21.37` 的。

## 0.21.37 已完成范围

- **一份已发布的操作说明指向了一个 git 里从来没有的目录。** 两份发版检查清单都告诉读者用 `.gate-logs/publication-pair.mjs` 重新推导发布延迟序列，而这个路径在 `.gitignore` 里——它只在写下这句话的那台机器上有效，在任何一份克隆里、在已发布压缩包自带的文档里都是死链。这一类问题无法被门禁看见：引用扫描解析的是测试*标题*，而一条 shell 命令既不是标题也不是符号。这个工具现在是 `scripts/publication-lag.mjs`，已被跟踪，仓库名与 Release workflow id 都通过 `gh` 解析而不再硬编码；上一版扫描曾卡在一次没有上限的 `gh api` 上 18 分钟且不打印任何东西，因此每次调用现在都有 30 秒超时、一行 `reading <version>` 进度，以及为分母丢掉的每个成员单独打印的一行。
- **跑一遍它，就改掉了引用它的正文里的三个数字。** 被测量的序列里耗时不足一秒的消费者步骤是两条（`0.20.86`、`0.20.87`），而不是两种语言都写的六条——`0.20.92` 之前另外四个发布以 `NEGATIVE` 打印，因而不在这 45 条之内，这正是工具自己的输出会做、而被抄下来的那句话丢掉的区分。`0.20.92` 起的窗口有 43 个发布，而不是 `AGENTS.md` 仍在计的 42；既然这个数每次发布都会动，它就被换成了查找方法（`measured=N of M tags`）。而"打印每条读数取自第几次 attempt"高估了这个标记——它只在非首次的 attempt 上出现。
- **序列确认了什么，工具本身又教了什么。** 正文引用的每个逐版本样本重读之后完全一致——`0.21.31`–`0.21.36` 的落库延迟 127.2 / 76.1 / 75.5 / 75.8 / 127.3 / 126.4 秒，对应的步骤耗时 304–308 秒，`0.21.30` 独自是 310.0 秒与 364.0 秒，45 条里 42 条落在 300–310 秒内，没有一次间隔越过 310 秒——因此 720 秒预算与钉住它的测试都原样不动。而被提升为正式工具的脚本第一次跑起来是*坏的*：一个作用域在扫描循环内的 `const` 格式化函数，让它在读完全部 45 个 tag 之后死于 `ReferenceError`，只打印了四行并以 1 退出——让这次失败可诊断的只是那个非零退出码，因为 49 个 tag 的扫描只返回四行，读起来像"序列短"而不是"工具坏了"。
- **进度日志里一个被抄来抄去的数，从来没有人量过。** `bench:browser` 的欠账在相邻两条 `Next` 里分别写成"四个发布"和"五个"；而最新的 `bench-results/browser-*.json` 是 `2026-09-24T13:40:52Z`，比重新生成 `docs/benchmarks.md` 的 `0.21.22` 发版提交早八分钟，所以这个数是十四个（自 `0.21.23` 起）。两个错数都不在已发布文件里，因此修正留在日志中，而两条旧 `Next` 现在指向那两份产物本身。

## 0.21.36 已完成范围

- **两条已发布的注释与其描述的代码相互矛盾，而发现它们的方式是计数，不是阅读。** `src/centrifuge.ts` 的分发器注释声称有三个监听器，实际是同一个函数注册了两次——三个来源、两个调用点——而这句话会经由 `dist/centrifuge.d.ts` 发布，因为 `private` 成员会保留其声明文档。`src/core/trace.ts` 引用了一个测试总数，而它已经失效两次，同一句话却同时告诉读者如何重新推导；该数值被删除，因为写在自己查找方法旁边的数字，恰恰是没人会去核对的那一种。找到它们的这次扫描把 `src/` 中十四条数值断言逐一与代码树比对，十二条成立——其中包括 `centrifuge@5.7.4` 里 `BaseSubscription` 内的十二处 `emit('error')`（整个文件为 23 处），让这个断言成立的正是那个范围限定。
- **`CONTRIBUTING.md` 第一次被逐条核对，其中四条断言与仓库相互矛盾。** 覆盖率阈值低于真正的阻塞门禁（写的是 85/80/90/85，而 `vitest.config.ts` 是 98/96/98/99）；`pnpm check` 的释义写了一个测试数量却漏掉 lint；发版一节要求 `npm version` 加 `git push origin main --tags`——即在本地 HEAD 上打 tag 并直接推送受保护分支，正是本仓库禁止的两个操作；`[Unreleased]` 在四处被当作约定，而 `CHANGELOG.md` 中这样的标题数为零，非发版 PR 都直接开版本号小节。四处现在都以配置文件与发版检查清单为事实来源。
- **发布延迟的操作方法写了一个参数却没说怎么选，同时样本序列从九个版本扩展到全部 tag。** 不带 attempt 段的 jobs 接口返回*最新*那一次，因此在那个重跑过的版本上，最直接的简写形式给出 −177.0 秒与 1.0 秒的消费者步骤，而 attempt 1 才是真实的 310.0 秒间隔与 364 秒失败；两份语言清单和 workflow 注释现在都写明 attempt 该从哪里取。在 `0.20.85` 起的全部 48 个 tag 上：44 个非负的 ack→记录间隔，最大 310.0 秒，于是 720 秒的传播预算维持不变——而“注册表在数分钟后才记录”这一前提被限定在 `0.20.92` 之后的窗口，因为更早的五个版本在 ±0.6 秒内就完成了记录，其中三个早于 ack 返回。
- **`AGENTS.md` 的目录地图已经完整并被门禁守住。** 它此前只列出 `tests/` 下 43 个文件中的 16 个，因此在这一块写就之后新增的测试框架、文档门禁、发版门禁与基准文件，对最先读它的会话全部不可见。如今它覆盖 `src`/`tests`/`e2e` 的每个被跟踪文件，外加每个代码目录一行，并且新的用例把这个小节的*边界*也钉住——因为围栏取位若偏移一格，“每个名字都在块里”对一份只是在别处讨论这些文件的文档同样成立。五个变异体，五次不同的失败。
- **两个文档门禁的作用域一直是错的。** 隐私门禁遍历 `examples/` 且没有扩展名过滤，于是每次运行都在正则扫描 2.9 MB 被 gitignore 的打包产物与 sourcemap，而 CI 从不构建它们——这是一条“本机红、CI 绿”的通道；它的作用域现在是仓库自身的索引，并用构造出来的输入钉住，因此在干净树与已构建树上读起来一致。`CONTRIBUTING.md` 进入了引用、空小节与相对链接三种扫描，而每种新增都单独探测：三种里有两种在带该数组项时失败、只去掉它时通过，而引用那一类在该文件里没有可触发的条目，因此只能算预防性覆盖。

## 0.21.35 已完成范围

- **发布门禁曾把一次已经公开的发布判成"从未发布"，而且真的判了。** `scripts/verify-release-version.mjs` 原先用裸的 `npm view <pkg> time --json` 读 packument，于是它读的是*当前配置*的那个 registry。本次发布准备期间在本机量到：这台机器的 `npm` 指向 `registry.npmmirror.com`，它那份文档仍报 `modified: 2026-09-25T00:32:51.767Z`——也就是 `0.21.33` 那次发布——而门禁运行在 `03:41:18Z`，比 npmjs 记下 `0.21.34` 的 `02:47:50.782Z` 晚 3,207 秒（53 分钟）；镜像自己的 `modified` 后来才走到 `03:44:48.778Z`，也就是说它已经有 3,418 秒答不出这个版本。（本条初稿写的是“75 分钟”，那是两个更早时间戳之差，和这件事里的任何一段耗时都对不上。）于是门禁以退出码 1 报出 `CHANGELOG names 1 release(s) the registry has never recorded: 0.21.34`。它自己注释里的推理（"`time[]` 记录不会过期，所以一次成功的读取里缺少的版本就是从未发布过"）错在第二个前提：一份*还不知道*某个版本的镜像，并不是该版本不存在的记录；而镜像的同步滞后不是 HTTP 缓存，所以 `cache-control: no-cache` 取回的还是同一份旧文档。现在这次读取显式点名 `https://registry.npmjs.org`，"registry" 才终于指那个"缺失即证据"的对象；修好后重跑，打印的是 `the 48 earlier CHANGELOG releases from 0.20.85 on are all in the registry's time map`，退出码 0。`verify-published-consumer.mjs` 早就为它的两次读取钉住了 `--registry`，理由相同——这是仓库里最后一处没钉的 npm 读取，而两种语言的检查清单都把未钉版本的那条命令当作"自己重算"的指引。这道门禁既有用例全都注入 `RELEASE_REGISTRY_TIME`，这正是没有任何一条能看见"读的是哪个 registry"的原因；新用例走未注入的真实路径，对着一个放在 `PATH` 上的 `npm` 桩运行，并断言那个参数与 URL 是成对到达的。
- **指向测试的引用，现在要拿去和测试套件对账。** `tests/documentation.test.ts` 新增"resolve to a case that exists in the file they name"：`src/**`、`AGENTS.md`、`README.md` 与公开文档里每一处 `tests/<file>`'s `<name>`，都必须在被点名的那个文件里恰好匹配一条 `it()`/`test()` 标题（当前是 52 个文件里的 18 处引用；结尾带 `…` 的允许前缀匹配，但也只能命中一条）。扫描的两半各自都有对照，因为它们都可能空掉，而"没有未解析项"在两种情况下看起来都是一次干净通过——标题收集器必须找到套件的用例（40 个文件共 977 条，下限写 500 而不是写当前总数）、引用模式必须至少检查一处，而 `e2e` 语料单独断言，因为那 977 条里有 937 条来自 `tests/`，总数那一项根本看不见 `e2e` 抓取整个空掉。两处范围决定是量出来的而不是定的：两处豁免文本加起来另有 11 处引用（`docs/progress.md` 8 处、`CHANGELOG.md` 3 处），而公开文档里只有 1 处——本句更早的版本写的是"`docs/` 里有 8 处"，那是把闸门并不扫描的 `progress.md` 也算了进去；那 11 处里恰好只有 1 处解析不出，因为它是逐字引用了这道闸门自己打印的失败文案，而不是一枚指针；更宽松的 `文件名 …（'a name'）` 窗口试过也被排除，因为整个范围内它只产生一个候选，而那个候选正是"文档注释点了这个测试的那个函数"里抛出的 `new Error(…)` 文案。五个变异体，各自报出自己的失败；更早的一个变异体因编译不过而被丢弃（少了一个反斜杠，正则变成未闭合，报告的是套件收集失败，而不是行为被杀掉）。这道闸门把写来描述它自己的那条 `AGENTS.md` 规则判红了——因为初稿按引用格式写出了那两个坏 token；于是规则现在写明：句子讲的是缺陷本身时，要把 token 裸写，不要写成指向用例的样子。
- **两句已发布的注释写的是 fixture，不是用例名，而且两者都会发出去。** `src/core/routing.ts` 引用了 `sticky-existing-routes`，`src/core/trace.ts` 引用了 `no-trace`；两者都不是 `it()` 标题，而是写在测试*体内*的 `clusterKey` 字面量，所以指针落在用例中间，而任意一个 fixture 字符串一旦改名就会指向虚无——构建、类型检查、测试套件都不会提出反对，这和漂移的 `:NNN` 是同一种沉默。两句都在声明的 JSDoc 里，也就是说错指针是发出去的：构建之后，用例标题出现在 `dist/core/routing.d.ts` 与 `dist/core/trace.d.ts` 里，而 fixture 串不在。`AGENTS.md` 在行号那一条旁边，补上了这一类引用的衰减规则。
- **`knownTopics` 上限的最后一份绝对说法退役了。** `AGENTS.md`——决定每一次后续会话怎么读这段代码的那一份——在 `src/` 和两种架构文档都已修正两个发布之后，仍然写着"max 500 entries"和"never evicts a key the worker still owns"。现在是量出来的说法：500 这条界覆盖的是本 worker *未持有*的条目，淘汰扫描跳过两个候选（持有的 key，以及本次正在记忆的 key——新写入的 `Map.set` 落在末尾，所以只有更早的条目全被持有时，从头扫描才会走到它），而在那个状态下上限会失守。1,200 条被收养的 peer `CONTROL/SUBSCRIBE` 让 `knownTopics` 和 `assignedTopics` 一起涨到 1,200，一轮 reconcile 把两者带回 `0`——所以对洪水而言界是清扫。两个跳过各自保留自己的具名用例。
- **一个承重的测试工装计数朝两个方向同时腐坏了，而同一个文件里同时存着新旧两个值。** `ChannelHub` 同步投递默认值的代价是一个实测数字：把 `asyncDelivery` 翻过去就会让套件变红。`tests/fakes.ts`、`tests/coordination-invariants.test.ts` 和 `AGENTS.md` 的一条项目符号写的是"15 条同栈断言、红 17 条"；而同一份 `AGENTS.md` 的另一条写的是 18。把字段改掉后重跑套件实测：**18** 条失败——`cluster` 9、`data-bus` 6、`stability` 1（也就是同栈投递并断言的 16 条），加上 `coordination-invariants` 里两条 hub 自身的对照（其中一条就是按名字钉住这个同步默认值）。三处文本现在一致，并且每一处都写出了能把它重新算出来的那次改动。**值得记下的不是那个差一，而是同一个文件可以同时持有过期计数和它的更正**——两条相隔一个周期写下的项目符号，谁也不会反驳谁；这是漂移的 `:NNN` 往上提一层的版本。
- **一句修补本身可以把它落进去的那句话打断，而中文镜像会留着英文已经换掉的数字。** 那次把"75 分钟"换成四个时间戳的清扫留下了三处幸存：`docs/zh/release-checklist.md` 仍写镜像"落后一个小时"，而英文对应处已经改成 3,418 秒；两处中文的实测都变成了 `3,207 s 秒`——单位写了两遍，因为往一句已经把单位 spelled out 的话里插数字就是这个残留；而两处更正本身被插进了正在流动的句子中间——英文的 `CHANGELOG.md` 与 `docs/roadmap.md` 因此分别留下了 `…not an elapsed time in this story. so the gate exited 1 with …` 和 `…) and the gate exited 1 …`。散文不参与编译，所以最后一类没有任何闸门能看见，而第一类也没有任何短语 grep 能抓到——改述后的文本与被改掉的原文没有共同子串，这也是为什么收尾的那一步是按*数字*跨语言 grep。`AGENTS.md` 补上了这条规则与扫描配方（在 70 个存活文件上找"句号后接小写虚词"，先剥掉行内代码，紧邻句号之前是单个字母则视为缩写）；它在全仓库报出**一处**真实命中（就是本分支更早的 `7168810` 里修掉的 `docs/progress.md` 那一句，那次之后重跑就是零），而这个扫描是按它仍然能抓到的那两个"修复前提交"来判分的——第一版要求句号之后紧跟空白，于是漏掉了带右括号的那一处；第一版缩写过滤把 `evidence.` 结尾的 `e` 也当成了缩写，于是在确有缺陷的树上报告"干净"。随后这个扫描抓到：本次会话在写下"不要打断句子"这条规则的同时，又打断了一句——这正是它该是扫描而不是重读的理由。
- **发布延迟样本扩到八段间隔、九个耗时，而上限的推导没动。** `0.21.34`：`Publish to npm` 在 `02:46:35Z` 完成，registry 在 `02:47:50.782Z` 记下这个版本（**75.8 秒**），`Verify published npm consumers` 用了 **304 秒**。最近三段落库延迟——76.1、75.5、75.8 秒——跨三次连续发布只差半秒，而那一步每次仍然吃掉约 304 秒，也就是说方差在读者看不见，而不在 registry 写得慢；这个读法已经写进四份文本，旁边就是能把它重新算出来的 `gh api` 取法。75.8 秒没有超过决定 720 秒预算的 310 秒，所以 `maxMeasuredAckToRecordMs` 原样保留，下限未作修改仍然通过。
- **没有变动的部分。** 库代码、导出、选项、默认值、帧格式与存储键均未改动；`verify:compat`、`verify:types`、`verify:pack` 针对 `v0.21.34` 运行，零计数分支账本与覆盖率聚合仍是 `0.21.34` 的。测试规模是 37 个文件 / 952 条。`docs/benchmarks.md` 与其中文镜像被重新生成后与提交版本逐字节相同（91 份归档报告），也就是生成的表格和仓库里的副本没有漂移——报告归档本身不入库，所以这项检查是一次运行而不是闸门。把存活文档里所有 `name()` 形态的 token（共 955 个）扫了一遍，没有虚构标识符：三处在全仓库符号表里找不到的，一个是 `e2e/` 里的辅助函数，另两个是 Playwright 的方法。依赖巡检：对 `registry.npmjs.org` 无已知漏洞，而 `typescript@7.0.2` 仍被上游挡住——`typescript-eslint@8.70.1` 的 peer 声明是 `>=4.8.4 <6.1.0`。发布记录完整性闸门现在报 48 条受检发布而不是 47 条，因为 `0.21.34` 已经进入了它自己审计的集合。

## 0.21.34 已完成范围

- **发布记录里点名了一个 npm 从未有过的版本时，打 tag 现在会被拒绝。** `scripts/verify-release-version.mjs` 在原有检查旁边读入 packument 的 `time` 映射，只要 `firstGatedRelease = 0.20.85` 及以上有某段 CHANGELOG 版本在 registry 里没有记录就失败。正在发布的那个版本按必要豁免，重复的小节只报一次，而读取 registry 失败时**告警并跳过**，不去把一次好发布判红——这是设计里刻意的那一半。下限来自计数而非口味：CHANGELOG 的 165 段里有 40 段点名了未发布的版本（总数自己重算：`grep -c '^## \[' CHANGELOG.md`；每发布一次就加一，所以这个总数不是论证的那一半）（`0.9.0`、`0.12.0` 到 `0.18.0` 每个 minor 的第一个 patch、`0.19.x` 全部十条、`0.20.0`–`0.20.5`、`0.20.54`、`0.20.55`、`0.20.69`、`0.20.72`–`0.20.84`；1 + 7 + 10 + 6 + 2 + 1 + 13 = 40）——把正在发布的那一条也算进去就是 41 段，而闸门豁免它正是这个原因——而它们全都在下限之下；从 `0.20.85` 一直到 `0.21.33` 的 47 条则一条不缺（这个数是本次 tag 自己的读数，每发布一次就加一，要取的是脚本打印的那一行）。四个变异体，四条具名测试，每次只红一条。
- **有一个淘汰跳过项没有任何测试能看见。** `rememberTopic()` 会跳过持有的 key *以及*本次正在记忆的 key；删掉后一项时 944 条测试全绿。它只在一种状态里可达：新写入的 `Map.set` 落在插入有序 Map 的**末尾**，所以只有更早的条目全部被持有时，从头扫描才会走到它——而在那里变异体丢掉的正是这次调用本要建立的映射。新用例摆出 500 条被收养的 peer SUBSCRIBE 帧加一条 PUBLISH，失败信息是 `expected [ 'owned.0', …(498) ] to include 'evict.extra'`；持有项仍由那条既有用例钉住，于是两个跳过各自有各自的击杀集。先前两种设计写不进去，原因记在用例注释里，因为它们看着合理却碰不到这条腿：入站 SUBSCRIBE 产生不出"未持有"的条目，而重复记忆一个已在表里的 key 撑不爆上限。
- **三句关于上限的既定文字，描述的是代码并不提供的保护。** `MAX_KNOWN_TOPICS = 500` 曾被写成"防止恶意 peer 借控制消息耗尽内存"；实测 1,200 条 peer `CONTROL/SUBSCRIBE` 让 `knownTopics` 和 `assignedTopics` 一起涨到 1,200——每个帧都会被收养，收养使它的 key 变为持有，而扫描随即拒绝删除任何东西；一轮 reconcile 把两者带回 `0`，所以洪水的界是清扫，上限的界只覆盖未持有的条目。`trace.ts` 把它的两个上限都说成在防"单个失控 topic"，而 `MAX_PENDING_TOPICS` 其实是对*不同* topic 的准入闸门（1,001 个 topic 得到 1,000 条队列，而一个已准入的 topic 仍会长到 256），`topics` 这张集合两个上限都不加，是刻意为之，由窗口刷新增界。同一份架构文档里的标识符表仍按修正前的说法复述淘汰规则，而它旁边那行漏掉了 `reconcileAssignedTopics`——唯一真正结束洪水的通路。两种语言全部改正，`src/` 的注释改为指向那个循环和那些测试，而不是把规则复述两遍。
- **与既有用例重复的候选测试应当删掉，而不是留下。** trace 的两个上限看起来没人钉，是因为检索用的是常量名，而那两条真正钉住它们的用例把数字写在注释里、通篇没提这两个标识符。两条新用例都写好了，随后变异检测报告：删掉准入守卫会红**两**条——新写的那条，以及输入规模更大的 `caps the number of topics tracked for pending receives`。于是新用例被删除，注释改为引用既有用例的名字。本次发布因此只新增一条测试（淘汰跳过项），不删除任何测试。
- **发布延迟样本扩到七段间隔、八个耗时，而上限的推导没动。** `0.21.33`：确认 `00:31:20Z`，registry 记录 `00:32:35.519Z`（**75.5 秒**），verify 步骤 **304 秒**——304–308 秒区间里的第八个绿色样本，也是从该 runner 自己首次抓取算起的一个 300 秒生命周期加命令时间。75.5 秒没有超过决定 720 秒预算的 310 秒，所以 `maxMeasuredAckToRecordMs` 原样保留，`tests/workflows.test.ts` 未作修改仍然通过。
- **没有变动的部分。** 库代码、导出、选项、默认值、帧格式与存储键均未改动；`verify:compat`、`verify:types`、`verify:pack` 针对 `v0.21.33` 运行，零计数分支账本与覆盖率聚合仍是 `0.21.33` 的。测试规模是 37 个文件 / 950 条。唯一新增的阻断行为在发布工作流自己的版本闸门里，而它在 registry 读取失败时自行跳过。

## 0.21.33 已完成范围

- **一条被当作"反证"的观测，其实把两台机器拼在了一起。** 关于发布延迟的正文曾用"重跑成功的时间早于 21:44 那次新鲜读取的 300 秒有效期到期"来反驳"纯 npm 本地缓存"的说法。对着运行记录看：那次新鲜读取是 21:44:35Z 笔记本上的 `npm pack`，而 1 秒就绿的那次是 21:47:10Z 的**全新 runner 任务**——新任务的缓存本来是空的，这正是本地模型预测的结果，不是反例。该论断撤回；本地模型仍然解释不了的，现在明确写成它本来的样子：只剩一个读数——runner 自己在 21:45:07Z 仍报 `ETARGET`，距轮询开启（21:39:03Z）已 364 秒，比那份副本理应过期的时刻还晚约 60 秒。结论依旧保持"未确立"。
- **写下这段的同时，`0.21.32` 送来了这个模型最干净的样本。** 发布确认 23:34:46Z，registry 记录 23:36:02.091Z（**76.1 秒**），笔记本在 151 秒时用 `no-cache` 已经读到新的 `latest`，而 runner 的阻塞步骤一直到 **304 秒** 才通过——从它自己首次抓取算起正好一个生命周期再加命令时间。于是 verify 耗时区间扩为七个绿色样本、304–308 秒；两份样本清单在两种语言、工作流注释、以及钉住下限的那条测试里都补齐了。
- **其中三个数字说的是另一个量，而且都在打 tag 之前改掉了。** "七个里第二小的"把耗时清单的个数借给了延迟清单——后者只有六个值（七个的那份是步骤*耗时*：307 / 304 / 305 / 304 / 304 / 308 / 304 秒），而且同一句话自己的括号里已经列了六个。另有三处写 0.21.30"把整整 **364 秒预算**花在 `ETARGET` 上"，可那一次的上限是 360 秒（48 × 7.5 秒），364 秒只是从它首次轮询到最后一个 `ETARGET` 的间隔——预算和读数被写成了同一个数。而 `docs/progress.md` 还留着那句的第一稿"比 21:44:05Z 本该失效的那次抓取晚大约 5 分 40 秒"，既不等于 364 秒的间隔，也不等于其它所有副本都写的"过期后再约 60 秒"。为了核对第一条，延迟清单声明区间之下的两个发布第一次被同样取样：0.21.25 是 **157.8 秒**（确认 17:56:53Z、记录 17:59:30.781Z），0.21.26 是 **96.1 秒**（确认 18:41:40Z、记录 18:43:16.084Z）——而它们的 verify 步骤耗时分别回到 307 秒和 304 秒，正是耗时清单里已有的那两个值，于是"哪次运行属于哪个版本"是被一个早就在案的数字确认的，不是靠新数字自证。现在共有八个延迟样本，没有一个超过 310.0 秒；这两个新读数写在这里和 `CHANGELOG.md`，而不是补进清单的每一处复述——多一份副本就多一处需要同步。
- **没有改的东西，以及证明它没改的检查。** 76.1 秒没有超过决定 720 秒上限的 310 秒，所以 `maxMeasuredAckToRecordMs` 原样不动，`tests/workflows.test.ts` 未作修改仍 6/6 通过——扩大样本而不必重算推导，正是这次发布想要的结果，而不是再调预算的理由。
- **历史记录按指针修正，而不是改写。** `CHANGELOG.md` 的 `0.21.31` 条目和两份 `docs/roadmap.md` 仍然保留当时写错的那句话，各自补上指明由 `0.21.33` 取代的说明，让记录读起来就是它本来的先后顺序。`AGENTS.md` 在"要从正在失败的那台机器去探"这条既有规则旁边，添了更一般的那条：把一次观测当作反证引用之前，先确认它的两半来自同一台机器、同一条读取路径。
- **没有动的东西。** 库代码、导出、配置项、默认值、帧格式与存储键都未变化；`verify:compat`、`verify:types`、`verify:pack` 针对 `v0.21.32` 运行，分支账本与覆盖率沿用 `0.21.32` 的（1951 支里 46 支零计数；99.02 / 97.64 / 99.27 / 99.69）。

## 0.21.32 已完成范围

- **一次发布会被丢，而且只丢在第一次。** `publish()` 与 `publishBatch()` 各自持有一条快速路径：当某个 topic 还没有 `wildcardPublishCache` 条目时，它先扫描本地通配模式，一旦匹配就把控制帧寄给*自己*，不去查持久路由；而同一 topic 之后的每一次发布都会去查。实测：一个 runtime 持有 `chat.*`，另一个 peer 具体持有 `chat.room.1`（两边 `isAssigned` 都为真，路由记录指向 peer），通配方发出的三次发布落在发送方 `1,0,0`、落在 owner `0,1,1`——第一次从没离开这个标签页，而投递是至多一次的。两条目的批次也是同一形状，接收方是按条目把它拆开处理的。
- **为什么修法是删掉，不是设界。** 这是 `0.20.58` 的反面：那次是被缓存的 `null` 短路了 owner 查询，这次是被缓存*未命中*短路。两种取值其实什么都没决定——`pattern` 和 `null` 都只被当作"这个 topic 我扫过了"来读——所以任何逐出规则都无法既给这张表设界、又修好顺序。现在所有非本机持有的发布都走 `resolvePublishTarget()`；通配归属依然成立，因为没有存活的具体路由时这个调用会返回本 worker，而接收侧的归属门会匹配模式。顺带地，0.21.31 记下的那条无界增长也就此关闭：表没了，它当初需要的那条"允许忘掉哪一半"的规则，已经没有可忘的东西了。
- **按发布逐条钉住，旧的那一对重新标注。** 两条新回归（每个入口一条）各自只对单次发布做断言，于是回归发生时报出的是"哪一次发布丢了"，而不是"总数差一"；不带修复时它们分别失败于 `expected +0 to be 1` 和 `expected [] to deeply equal [1, 2]`。三条标题里点了这张已删缓存的旧用例，现在改成它们实际断言的东西，其中单 runtime 的那一对明确标为特征化测试——只有一个 runtime 时它总会选出自己，因此分不清这两条路径。
- **发布闸门的说明文字补上了自己的样本。** 0.21.31 是第一个跑在提高后的 720 秒已发布消费者上限下的 tag 运行：第一次尝试即全绿，用了 308 秒，registry 记录落在发布确认之后 127.2 秒，剩下的约 181 秒是对着缓存生命周期等的——也就是说定这个上限所依据的 310 秒最大值仍未被超过，上限不必再调。校正这句话时还暴露出一个同形状的漏计：清单和 `AGENTS.md` 写的是"连续三次绿色 tag 运行都是 304–305 秒"，而 0.21.25 到 0.21.29 那五次实际是 307、304、305、304、304 秒，算上 0.21.31 的 308 秒就是六个样本落在 5 秒区间里。现在延迟清单、每次运行的耗时以及重取这些数字的命令都写了出来，两种语言、工作流注释和钉住下限的那条测试里都有。`trace.ts` 的 flush 活动守卫注释也补上了每个"杀掉哪些测试"来自哪个文件——在这棵树上把四项删除各重跑了一遍（3 / 2 / 1 / 1，四次还原都逐字节相同）。
- **动了什么，什么没动。** 这是自 `0.21.25` 以来第一次改动库的*行为*——0.21.26 到 0.21.31 对 `src/` 的改动只在注释里——`src/core/cluster.ts` 少了一个字段和两条快速路径。公共面未变：`verify:compat`、`verify:types`、`verify:pack` 针对 `v0.21.31`，而被删掉的 `wildcardPublishCache` 是私有字段，本就不出现在产出的声明文件里。

## 0.21.31 已完成范围

- **那次"发布成功但闸门失败"的排查：预算改成测量出来的和，而不是凭感觉。** `0.21.30` 的 tag 运行把 48 × 7.5 秒的已发布消费者预算整个耗在 `ETARGET` 上，而针对同一个 tag 重跑时这一步只用了 1 秒。这个上限其实是两段延迟之和，两段现在都测过：`npm publish` 返回确认早于 registry 自己给这条版本盖的 `time[<version>]`——0.21.29 是 74.8 秒，0.21.28 是 96.8 秒，0.21.27 是 248.7 秒，0.21.30 是 310.0 秒；而 `npm pack` 用来解析的 packument 是以 `cache-control: public, max-age=300` 提供的，也就是说路径上任何一份副本按设计都可以旧一整个缓存生命周期。第二段也正是前三次绿色运行那可疑一致的 305 / 304 / 304 秒在报告的东西——一个在没人解释之前只会被读成噪声的规律。于是预算改为 96 × 7.5 秒 = 720 秒，覆盖 610 秒。究竟是哪一份副本提供了过期视图，这里是故意留空的：纯 npm 本地缓存的解释会预测重跑同样要等满 300 秒，而它没有。（这一句已被 `0.21.33` 撤回：它把笔记本上的一次 `npm pack` 读取，和一个全新 runner 任务 1 秒就绿拼成了一句话，也就是两台机器；新任务的缓存本来是空的，"读到新鲜"正是纯本地模型预测的结果而不是反例。它真正解释不了的只剩一个读数——runner 在自己的副本理应过期约 60 秒之后仍然报 `ETARGET`——而 0.21.32 那次 304 秒的 verify 是这个模型最干净的一个样本，区间也因此扩到七个样本、304–308 秒。）
- **一道放过了"刚刚失败的那个值"的下限。** `tests/workflows.test.ts` 断言的是 `totalMs >= 5 * 60 * 1000`，也就是说把一次好发布判红的那个 360 秒预算，恰好满足这道专为阻止此事而存在的闸门，而且再往下调也不会有人拦。现在下限换成上面那两项之和，写成两个具名常量，并且把每一项怎么重新取数写在旁边。双向都做过核对：把工作流改回 48 会让这一条测试精确失败，报错信息同时点出两段延迟；同一条下限也会拒绝这里失败的 360 秒和 0.20.89 当年排空的 120 秒。
- **一条关于该失败的说明，被它自己的时间戳推翻。** 同一次发布期间写进发布清单的那段说，包文档端点与 tarball URL"在同一分钟里"给出了相反答案。而它自己引用的读数把那次过期文档读取放在 21:43:15Z，把 registry 对该版本的记录放在 21:44:12.987Z——所以那次读取根本不是在拒绝一个已经存在的产物，它是在如实报告一次 registry 还没写完的发布，`no-cache` 也救不了它。真正的分歧是：一台机器最迟在 21:44:35Z 已经能取到 tarball，而 runner 到 21:45:07Z 还在 `ETARGET`。因此正文现在要求**从正在失败的那台机器**去探，并且把"发布步骤全绿之后几分钟里 `versions`/`latest` 仍然没有它"写成一次成功发布的**预期**状态。
- **`wildcardPublishCache`：那条被文档化的上限并不覆盖的按 topic 映射，以及为什么加个上限不是免费的。** 它把具体 topic 映射到匹配它的本地通配模式，或映射到 `null` 表示"扫过，没匹配"；它没有任何界限，只在两处生命周期拆解里清理，位置与每一次 `routeOwnerCache.clear()` 相邻。实测：对互不相同的 topic 名调用 1,200 次 `publish()` 之后，`knownTopics` 仍停在 500 条上限，而这张表持有 1,200 条。逐出哪一半的代价并不对称——丢一个*模式*会重跑这张表本要阻止的首次本地分发；丢一个 `null` 会重新扫描，而如果这期间分派过一个通配，这次发布就会拿到只有首发才有的那种扇出。所以注释记下的是：要给这张表设界，需要的是一条"允许忘掉哪一半"的规则，不是一个数字。`docs/architecture.md` 在"上限与逐出"那段之后也写了同一件事。
- **把"杀掉哪些测试"按测试名字写出来，而不是写成计数。** `trace.ts` 的 flush 活动守卫原本带着"（`received` 三条、`dispatched` 两条）"。五个具名测试取代了这两个乘数，而其中一个恰好记下计数藏起来的事实：`dispatched` 的那两条在两个不同的文件里。
- **没有动的东西。** 库代码一行未变：`verify:compat`、`verify:types` 与 `verify:pack` 对 `v0.21.30` 报告公共面闭合，分支账本与四个覆盖率数字都沿用 0.21.30 的。有行为改动的只有 `.github/workflows/release.yml`、`tests/workflows.test.ts` 和 `tests/version-compat.test.ts`（后者是一条按文件设置的超时；它确实生效这件事，是通过把它调到 1 毫秒、看着那 18 条里的 17 条失败来证明的），三者都不在发布的包里面。

## 0.21.30 已完成范围

- **两条被代码直接推翻的绝对表述。** `touchRouteOwnerCache()` 的逐出循环给出的理由是"此时缓存至少比上限多一条，所以迭代器总会交出一支待丢的键"——在 `routeOwnerCacheMax: 2`、两个远端 owner 的树上测量：填到上限后再解析其中任意一个，`size` 仍是 2，循环第一轮就 break 而不丢任何东西；因为一次调用最多净增一条，只有"缓存已在上限且这次来的是新键"才会越过上限。那句不等式对每一次重复 touch 和每一次未填满的情况都是反的。另外六条注释说某次删除后"全部 37 个测试文件全绿"——这个计数并不带来句子本身没有的信息，而且它是在有人新增文件的那一刻变假，不是在被描述的那段代码改变时变假，于是六处统一改成"整个套件全绿"。
- **两份无法从自己那句话重建出来的差分向量。** `validation.ts` 写的是"一份 23 个取值的向量（数字字符串、布尔、`±Infinity`……）"——都是复数的类别、没有各自的条数，读者既数不出 23，也分不清 23 和 21。重建成明确列举的 25 个输入，**并且包含 `3000`**，因为一个"没有任何输入被接受"的差分什么也证明不了：每个守卫按原样写、以及只删掉它的 `typeof` 那一项，对全部 25 个输入的回答完全一致，接受/拒绝相同，消息也相同。`environment.ts` 的信封守卫同样处理，并得到一个值得写下来的意外：26 次写入但只有 **23 个不同文档**，因为 `1e999` 就是 `Infinity`，而 `JSON.stringify` 把它输出成 `null`，于是那三行存下的字节和它们的 `null` 双胞胎一模一样——这也正是任何非有限的 `seq` 都到不了 `typeof` 测试的原因，而那一行本来就是为了检查这件事才存在的。
- **重跑 reopen absorb 的四条腿——但先得把施加手段重做。** 那条"51 条测试失败"的数字自文件还是 194 条起就没人重跑；现在是 198 条，回来的仍然是 51 条，另外三条腿也确实全绿。第一版构造是把 `pending` 整个换成一个新 promise，于是两条本应全绿的腿各自失败三条测试（排在 pending stop 之后不重开那一例、反复 hide/show 都排在首次 open 之前那一例、以及异步 stop 恢复那一例）——它们断言的是重开**等的是哪一个** promise，而不是它是否 reject。一个坏掉的变异落在对照腿上时，读起来像发现而不是像仪器坏了；只有把链接**追加**到真正的 `startPromise ?? pendingStop` 上才是可信的施加方式，注释里现在写明了这点。
- **reopen 闸门那四项都活过了删除——`tsc` 也一样。** `runTransport()` 重开闸门里每一次只删一个条件项，整个套件都全绿，与注释所说相同。新测到的事实：这四次删除各自也通过类型检查，这把它和文件里另外那几支只有编译器在守的 `?? 回退` 区分开了。这里既没有测试也没有类型检查站在那四次删除与合并之间，挡住它的只有下文关于被调用者的论证——注释现在写清了是哪一种。
- **一道可能在验证错版本的情况下通过的发布闸门。** `verify-published-consumer.mjs` 在没有 `PUBLISHED_VERSION` 时从 `npm view <pkg> version` 推断目标，而这个读取在本地缓存有货时就直接用缓存：某次发布后的几分钟里，同样的命令连续两次返回被取代的旧版本，而同一 registry 上的 `versions dist-tags --json` 已经报出新版本。于是这道闸门会下载并 import 上一个产物然后退出 0，唯一的见证是它自己打印的那行里的版本号。现在推断出的版本只有在与 `package.json` 一致时才被采信，空读取也会被拒绝，而不是变成 `pkg@`——npm 会把它解析成 `latest`。`Release` 工作流从不走这条路（它显式传入 tag），所以暴露的恰恰是清单里要求维护者手动复跑的那一次。新增五条测试并全部做过变异核对（四条腿分别死于两、一、一、二条测试），脚本主体也移到与兄弟脚本相同的 `main()`/`invokedDirectly` 门后面。
- **没有动的东西。** 库代码一行未变：`verify:compat` 与 `verify:types` 对 `v0.21.29` 报告公共面闭合（6 个 entry、90 个可导入名字），账本依旧是 1963 支里的 46 支零计数（99.02 / 97.65 / 99.27 / 99.69）。测试增至 941 条，文件仍是 37 个——新的五条刻意加进已有文件，因为"37 个测试文件"这种计数正是刚才清掉的那六条注释变假的方式。

## 0.21.29 已完成范围

- **队列本身就是这些断言：把注释里引用的测量结果逐条重跑，一次一个变异。** `src/` 里每句"测量过：删掉 X 套件仍全绿""删掉它会杀掉 N 条测试"都是某一次旧树上的结果，所以这一轮对着当前套件一条一条重做，每次改完都还原并逐字节校验。绝大多数完全成立——`port-reaper` 的 `touch()` 守卫和它的非空回退、`centrifuge-session` 的空 topic 丢弃、`cluster` 的无条件 `clearInterval`、`stop()` 入口守卫里那两个被支配的条件项、`websocket` 的 `handshakeCompleted` 项、`centrifuge` 的重复装配守卫（它的调用方枚举也一并核对：`startHeartbeat()` 只由 `startSharedWorker()` 到达，`clearHeartbeat()` 只在 `stop()` 与 `onWorkerFailed()`），以及 `trace` 的 `metricsActive` 项——删掉它依旧恰好杀掉注释点名的那两条，而 `stopped` 也确实只在注释说的两处被读。四条不成立。
- **由编译器守着的那几支，数字与当初写的一致。** `cluster` 的空 owner 守卫：套件全绿，并且正好五个类型错误落在注释点名的那三个表达式上。三处选举的 `?? this.currentRecord`：分别是 **3、2、7** 个错误，与文中所写相同；三支同时删掉，整个套件仍全绿。`projectedLoads` 那一对：各一个错误，且正是注释描述的两种形式。这些分支的"检查"只有编译器，测试根本看不到——这恰恰是它们的计数值得重取的原因。
- **一处四与五的错位、一个差一，还有一条对不上任何已安装文件的引用。** IndexedDB 结算那段写的是"全部四个 latch"；latch 变量确实四个，但被守卫的闭包是**五个**，因为 `load()` 的 `oncomplete` 与它自己的 `fail` 共用同一把 latch——于是一次性删掉的其实是五处语句（照这样重跑：全绿，连接数不变）。`CentrifugeSession` 的"四个语句之后"列举的是三个，代码里也是三个。而它引用 SDK 的 `build/index.js:762` / `:162` 去对照依赖包：5.7.4 里那个空转 `error` 监听在 763-764，store 里的 5.7.0 副本在 748——这个数字哪个文件都不对，而且无从还原它怎么来的。两处现在都改用依赖自己的注释原文作为定位串：可 grep，而且上游一次重排不会像 `:762` 那样在看起来完全可信的情况下悄悄失效。这条规则也得落到 `AGENTS.md` 自己身上，因为它抄去的同一条引用一样是过期的。
- **烂掉的测试文件计数，和一次与主机相关的耗时。** `queueStartAfterStop()` 的注释写着"194/194 全绿"；该文件现在是 198 条，重跑这个变异仍然复现两半——整文件全绿，以及生命周期 fuzz 死于 `Ineffective mark-compacts near heap limit`。只是这次崩溃落在 worker 存活 26.8 秒处，而非记录里的约 40 秒，于是句子改成：时间是主机相关的，崩溃才是承重的那一半——而同样的两个数字还站在另外两处：这个测试文件自己的头部注释，以及把这一例当作准则引用的 `AGENTS.md` 条目，同一轮里一并改掉了。旁边那个四向 absorb 测量里的三处 `194/194` 改成了"整文件全绿"，唯一带计数的那一支保留数字并写明它是在 194 条的文件上取的，因为这一支还没有重跑。
- **没有动的东西。** 没有一行代码变化：把注释行过滤掉之后，五个文件的 `git diff -U0 -- src/` 返回空；测试仍是 936 条加 5/5 性能闸门；分支账本依旧是 1963 支里的 46 支零计数（99.02 / 97.65 / 99.27 / 99.69），且每个文件的分布与 `0.21.28` 相同。

## 0.21.28 已完成范围

- **"这是这个 opening 拿到的唯一处理器"——它并不是唯一的。** `reopenTransport()` 提前返回那一支上的 `.catch(() => undefined)`，过去的正当性来自一句"没有别人处理它"。实际有五个调用点，其中三个（`startDemandRecovery()`、`updateStatus()` 的恢复定时器分支、`runTransport()`）把自己那份 `opening.then(f, g)` 链在**同一个**对象上，于是按 Promise 语义，`g` 本来就是 `opening` 的处理器，这一行在那三条路径上什么也不决定；`start()` 则把它交给自己的调用方。只有 `resumeTransport()` 的 `void` 调用需要它，注释现在点的是这个调用方，而不是"唯一"。同时记下对称的变异结果：删掉这一行，37 个测试文件全绿且没有任何 unhandled-rejection 报告，删掉方法开头那个 `.catch` 也是同样——把两行区分开的是那份调用方清单，这是一次枚举，不是一枚钉子。
- **一句两次都不对的时钟描述。** `ClusterEnvironment.now` 原来写着"注入的那个才是这张图里唯一的单调时钟"。这张图里根本没有单调时钟：浏览器适配器把该字段直接绑到 `Date.now`，而 `performance.now()`/`hrtime` 在 `src/` 里一次都没有出现；注入的时钟同样不单调——把测试提供的所有时钟普查一遍，找到一个是**故意**往回走的（`tests/trace.test.ts` 那条 dispatch 倒序用例）。替换后的注释说的是真正承重的东西，也就是消费者：`cluster.ts` 是 `src/` 里唯一读这个字段的地方，它的十次读取有七次只盖章，剩下做减法的三处各自都会在一次倒跳上判为"未过期"，于是倒跳之前盖章的记录会一直活到时钟重新爬过它自己的时间戳。
- **没人解得动的普查行，加上一个已经烂掉的杀伤计数。** `runTransport()` 的守卫注释列了 `1000`/`0000`/`0100`/`1001`/`1010`，却从没有说这四个位就是它这四个前提按源码顺序排列——编码现在写明了。行集合用"每种行首次命中打一条日志"跑一遍全套件重新导出：还是那五行；`1001` 的来源也不再是转述，而是栈本身：`handoffAssignedTopics()` → `onControl` → `unsubscribeTransport()`。逐个删操作数重跑之后，`droppedAfterConnect` 仍然恰好死在它点名的两条测试上，而 `transportReady` 杀掉了**九**条，注释里写的是八——而且那八条从未被点名，因此补进来的是哪一条已经无从认定。两个数字现在都写成"去跑这个变异"，不是上限。
- **一条把"端口"说成"机会"的拆卸注释。** `ReapTarget` 原来的"一个即将被关闭的端口，是它的拥有者得知原因的唯一机会"是 `reap()` 那段有测量的推理的坏压缩；现在它点明三个步骤、它们的顺序，以及共用那个 `try`/`catch` 的代价——在 `notify` 里抛出的 target，自己的 `close` 和 `stop` 也一并跳过。
- **没有动的东西。** 没有一行代码变化：把注释行过滤掉之后 `src/` 的 diff 返回空；测试仍是 936 条加 5/5 性能闸门；分支账本依旧是 1963 支里的 46 支零计数（99.02 / 97.65 / 99.27 / 99.69），且每个文件的分布与 `0.21.27` 相同——这才让"仅注释"成为一次测量，而不是一句描述。

## 0.21.27 已完成范围

- **代码是对的，理由却是错的。** `reconcile()` 以 `if (!this.started) return;` 开头，而它的注释把 REGISTRY 那一支解释成"关上的"，理由是：从 `pause()` 清掉 `started` 到 `pause()` 摘掉 message 监听器之间"没有任何东西能把栈交给使用者代码"。那三段语句里正有一段是 `environment.clearInterval(…)`——那是适配器代码，而这个文件自己的 `activate()` 注释恰恰把它当作使用者提供、不可信任的东西。所以这个守卫不是"不可能的窗口"后面的多余装饰：它就是把这个窗口关上的东西。注释现在是在为守卫辩护，而不再是不小心反对它。
- **一个"覆盖到了，但来路是推理"的分支，现在有测量了。** `pause()` 里 `heartbeatHandle === null` 那一支只会从激活窗口进来——这是注释的说法，而覆盖率计数器证明不了它，因为无论如何这一支都是被覆盖的。把这条分支插桩后跑完整个测试套件，恰好打印两行、没有第三行：一行来自适配器 `createChannel` 内部，一行来自无存储 self-SUBSCRIBE 的 `handlers.onControl`，正好一个缝一条，与那两条测试逐字对上。两个名字都写在落点注释里，于是"第三条来路"是一个能看见的差异，而不是一次需要重新推导的结论。
- **一个烂掉了却谁也没反驳的数字。** `reopenTransport()` 的注释说它的两个守卫分支"在覆盖率运行里 0 of 5041 calls"从未触发，而 5041 这个数字写于 `b0354e4`。同一个查找重跑得到 5046。这个数字自始至终只支撑"方法是热的、分支是冷的"这一件事，所以句子改成指向取数入口（`coverage/coverage-final.json` 里的函数计数），而不是留下一个看起来像证据的过期常量。通用规则已收进 `AGENTS.md`。
- **没有动的东西。** 没有一行代码变化：把注释行过滤掉之后 `git diff -U0 src/` 返回空；测试仍是 936 条加 5/5 性能闸门；分支账本仍是 46 个零计数 / 1963（99.02 / 97.65 / 99.27 / 99.69）；而且没有一份面向使用者的文档需要改写，因为这三句话讲的都是内部方法。

## 0.21.26 已完成范围

- **谁取消持久化重试，谁不取消。** `replay-manager.ts` 里有四处注释说取消代际"is bumped on suspend/stop"。这个类两个方法都定义了，而只有 `suspend()` 会写这个字段；`stop()` 清掉的是回收定时器的扫尾计时，什么在途工作都不覆盖。成立的那一半——隐藏的 tab 和停止的 bus 确实都会取消自己排队的持久化写入——属于 DataBus：它从 cluster 的 `onSuspend` 处理和 `beginStop()` 两处走到 `ReplayManager.suspend()`。这个层级错位也正是那句话从来没被反驳过的原因：覆盖这一行为的测试是 bus 层的测试。
- **生下来就是错的，不是变旧的。** 在 `0cb8572`——把这个文件拆出来的那个提交——里，`stop()` 就已经和现在一样只有清空计时器这一件事，而 `suspend()` 也已经是字段的唯一写者，所以这对名字从未描述过任何存在过的代码。这个类是内部的（`ReplayManager` 在 barrel 里没有任何出口），因此这是一次面向维护者的更正，而不是面向使用者的。
- **产出这份任务清单的扫描，本身才是这一遍的缺陷。** 它判断哪些零计数分支还缺少落点结论的方式，是读每个站点头上一行，于是在一份 46 个分支、每个都已带结论的账本上报出 21 个"未访问"——结论注释本来就有五行到十五行长，而它引用的测量正是重点。换成带窗口地读这些站点，一次重审就变成了一个真实发现。`AGENTS.md` 现在收下了这条规则：扫描也是探针，所以要给它探针该有的对照——先拿一个已知有答案的站点跑一遍，确认它回答"存在"。
- **没有可测的东西，这一点是被分类而不是被略过的。** 差异只有注释行，测试仍是 936 条，账本仍是 46 / 1963，聚合仍是 99.02 / 97.65 / 99.27 / 99.69。改过的文字*如何*抵达使用者反倒是量出来的：它出现在 `dist/core/replay-manager.d.ts`、那个 ESM chunk 以及两个 CJS 包里——esbuild 会把注释留在 JS 里，而这些路径全部随包发布。

## 0.21.25 已完成范围

- **一个不会终止的分发。** handler 列表是每个 topic 一个 `Set`，而 `dispatch()` 迭代的是这个实时集合，于是为一个 topic 注册*全新*闭包的 handler，会收到正是它自己引起的那条消息——而那个新 handler 又会再注册一个，如此继续。测量结果：**一条发布跑完了 500 个 handler**（500 是探针自己设的上限，并不是库给出的界限），**下一条投递给了 999 个**，因为这些注册会留下来。背后的写法很常见：每条消息都重新挂一次一次性 handler。
- **同一处变动的两向。** `Set` 迭代同样会跳过游标之前被删除的条目，所以较早的 handler 里调用 `unsubscribe()`，过去会把后面的 handler 从一次已经开始的分发里摘掉（实测这一对只投递出 `['first']`）。现在的契约固定在那个可预期的方向上：一条消息投递给分发开始时存在的订阅者。
- **为什么以前每一遍都没看见。** 用*同一个*函数引用重复订阅，在 `Set` 的相等性下是 no-op，而针对这个问题写下的第一个探针用的就是具名函数，于是量出来恰好一次调用。缺陷只在闭包各不相同的时候才出现——这正是"确认怀疑的测量"与"凭空发明的测量"之间的差别：把它定下来的是测量，不是推理。
- **量过，而不是想当然。** 五个性能闸门没有一个覆盖 dispatch，所以这次为快照单独量了：1-20 个 handler 时每次分发 9-23 ns，一个含两个 handler 的匹配 pattern 再 +15 ns，而每消息预算在几十微秒量级。三条新测试各自以测出来的数字失败（`expected 500 to be 1` 加两个数组比较），原有 933 条测试不受这次改动影响，覆盖率仍停在 46 个零计数分支 / 1963。

## 0.21.24 已完成范围

- **两个生命周期缺陷，都是给覆盖率台账分级时分出来的。** `WorkerClusterRuntime.stop()` 如果落在运行时仍在激活的那段时间里，会把这次 teardown 刚刚清掉的东西全部重建：一个带着监听器的活 channel、一条对等端永远不会按 TTL 清理的 worker 记录、一个之后的 `stop()` 再也清不掉的心跳定时器，以及一个仍然会接收发给自己的控制帧的已停止标签页——原因是 `activate()` 先写了 `started = true`，之后才去调用消费者代码。另一个是从 `handlers.onResume` 里发出的 `stop()` 会被直接丢弃：`handlePageShow()` 恰好在调用那个回调的前一条语句清掉了 `suspended`，而 `started` 只会由被 generation 自增跳过的 `activate()` 来设置——于是下一次可见性切换会在调用方已经停止过的运行时上重新触发 `onResume`。
- **调用方枚举是对的，而且没有用。** 第一个缺陷里，被守卫的那个方法的每一个*调用方*都遵守了不变式；重新进入的代码在它自己内部。正是一个零计数分支台账会交到你手上、却指不出方向的那种形状，这也是为什么两条新测试是从“撑开这个窗口的缝隙”去构造它（一个会停止运行时的 channel 适配器、一个会停止运行时的控制回调），而不是从外部把它驱动出来。
- **台账六个阶段以来第一次动了：零计数分支 47 → 46**（分母 1958 → 1963，分支覆盖 97.59 → 97.65，测试 930 → 933）。其中一条腿是因为这次修复才变得可达并被覆盖的；其余的如今都在现场写下了结论——`writeRoute()` 存储守卫的调用方枚举、发布目标兜底那条由 `routeOwnerIsLive()` 自己的合取项决定、交接处两次投影负载读取由那次 Map 播种决定，而三标志不变式只在 `stop()` 处陈述一次，其余站点指向它。
- **凡是便宜的测量，就不沿用旧结论。** 一次完整套件运行上的标志向量普查给出了可达性行（`pause()` 四种不同向量，`activate()` 入口与监听器移除各一种，`reconcile()` 与 `writeRoute()` 各两种）；每一“类型问题”的腿都单独跑了一次 `tsc --noEmit` 数错误；而交接处那条 owner 守卫注释一直声称的五个错误，也在这棵树上重新删一次量了出来。普查里*出现过的*行是没出现的那些行的对照：每个被观测的标志都在某处读到过 false，所以缺席的一行只意味着“没有测试走到”，而不是“探针是瞎的”。
- **两条被支配的项是有意留下的。** 删掉 `stop()` 原来的任何一个标志整套件仍然全绿，因为它们都被新加的 `lifecycleListening` 项蕴含。留下它们的理由是不对称性：析取式里多一项只花一次布尔判断，少一项则会悄悄吞掉一次 teardown——那正是本次发布修掉的缺陷。

## 0.21.23 已完成范围

- **逐操作数的守卫清查到此收尾。** `data-bus.ts` 的四条四项状态链——`stop()` 的提前返回、恢复定时器的回调闸门、`runTransport()` 的就绪短路，以及它下面的按需重开——都被逐个删掉一个操作数、跑整套件测量过：十六个变异里，四个早已被具名测试钉住，十一个由构造决定不了，只有**一个**是有测试从未命名过的活行为。`sends no unsubscribe to a transport while the bus is stopping` 现在钉住了它，而且只死于它自己那个变异。
- **用普查替代输入差分，用一段栈追出那条测试。** 状态守卫没有可变的调用方输入，于是把操作数的归类改成：在一次完整套件运行里记录守卫每次求值的标志向量（`stop()` 被调用 7589 次、得到七种不同向量；定时器回调 49 次、两种），再拿这些行去比对每个幸存操作数要成为决定者所需的那一次赋值。当某个前提真的出现、而删掉它仍然全绿时，在第一次看见该行时打印一段栈，就点名了走到这个守卫的*生产路径*——`unsubscribeTransport()` ← 一条控制帧 ← `WorkerClusterRuntime.stop()` 的交接。这条测试需要第二个标签页：没有剩余订阅者时交接是删除路由而不是发出控制帧，而单标签页的那版对它自己的变异是通过的。
- **两种此前没见过的“这条操作数决定不了任何东西”。** 一种是被**被调函数**支配：按需重开的四条腿里有三条只是把 `reopenTransport()` 先行执行的闸门重述了一遍，删掉任何一条都只会把同一个 promise 交回调用方。另一种是**没有可观测缝隙**：`stop()` 的 `!started` 这个前提在每个 `start()` 的这段时间里都是真的，可中间没有任何调用方边界能观测到它——而同一条不变式，在 `reopenTransport()` 里已经有了说明它自己那一对的注释。
- **一段用来解释死操作数的注释变成了一道门禁。** 记录在案：`validation.ts` 里那几条 `typeof value !== 'number'` 存在的理由，就是防住有人把 `Number.isFinite(value)` 写成会强制转换的全局 `isFinite(value)`；现在 `no-restricted-globals` 在 lint 阶段就拒绝它，并且是靠真的把那个替换写进去、看规则报错来验证的。原先写着“不存在这条规则”的两处活文本被改写而不是删除，那条操作数现在被记录为受两层保护，且这两层各自防住不同的删除。
- **还有两件没有进套件事，以及原因。** 两个候选测试——一个冲着 `status !== ERROR` 那条腿，一个是最早版本的 unsubscribe 测试——在它们各自为之而写的变异下通过了，于是被删掉。取代它们的结论写的是“没找到路径，防的是这一分支被放宽或重排”，而不是“不可达”，因为前一句才是这次真正建立起来的那一半。

## 0.21.22 已完成范围

- **一套方法，用上一轮扫描排出的每一个多操作数守卫**：只删一个操作数，跑整个测试套件，然后判断这个变异给出了三种答案里的哪一种——被钉住了、根本没有裁决能力、活着却没有任何测试命名它。于是九处从未被测试命名的行为有了名字（`websocket.ts` 里三个判断连接是否过期的项、`trace.ts` 四个活跃/去重计数器中的两个、`storage-event` 信封的两个操作数、`DedupManager.start()` 清扫闸门的两条选项腿），套件从 923 增至 929。
- **账本一次都没有动，而这正是结论。** 六个阶段结束时 `src/` 仍然是 **47** 条零计数分支，与开始时相同，变的只有分母（1962 → 1958，来自删掉的两条被支配操作数）。被探测的每一个操作数都在每次调用里求值，所以没有任何覆盖率总量能区分“执行了”和“裁决了”——这一类问题对分支账本是隐形的，只有整套件变异能看见。
- **反复出现的原因是联合断言，不是缺测试。** `trace.ts` 的活跃性检查看上去早已被钉住，因为那个既有用例在同一个窗口里把*两个*去重计数器都抬了起来，于是留下哪一条腿都照样产出；信封的 `seq` 与 `message` 两条测试也同时被它唯一那条畸形写入触发。让每条腿各自致死的，是把输入拆开，而不是补覆盖率。
- **五条操作数被判定为“无法裁决任何东西”，而且是差分证明的。** 信封里的两条，加上 `validation.ts` 三条 `typeof value !== 'number'`，全部以固定的输入向量证明而非推论（23 个存储载荷；23 个值 × 3 个校验器 × 4 个变体），原写法与每个变异体得到的“接受/拒绝及其消息”结果逐字节相同。被保留的那条 `typeof` 腿随后也被两头检验了自己的保留理由——把它存在的意义就是防住的那个“换成会强制转换的全局函数”的改写构造出来，结果带着这条腿时该改写杀不死任何东西，而把腿也删掉后 `heartbeatIntervalMs: '3000'` 就成了被接受的选项。
- **删掉两条被支配操作数，另外记下一次的满分和一次的薄。** `getMetrics()` 与 `flush()` 各自把同一个表达式刚算过的字段又读了一遍，这种读取不可能裁决任何东西，却正在悄悄吞掉那个本可以证明 getter 契约的变异；已删除，两组击杀集都写在注释里。`ReplayManager.start()` 的四项闸门则被证实每一条腿早已被钉住——这是两个同形管理器之间的对等检查，也正是 `DedupManager` 缺口的所在。而 `validation.ts` 六条致死里有五条来自多个用例、第六条只来自一个，所以代码位置上如实写明，而不是汇成一句“九之六”。

## 0.21.21 已完成范围

- **又一条未覆盖分支被“从来没有测试发出过的调用”关闭了**——而这一次需要的调用方在浏览器里并不存在。`queueStartAfterStop()` 的 `stopPromise ?? Promise.resolve()` 从未走过它的兜底分支，而那正是*首次打开失败*时所处的状态（它的清理由 `pendingStop` 掌管，所以根本不存在那道闩）；那个窗口内唯一会同步回调进应用代码的缝隙，是调用方自己提供的 `ClusterEnvironment` 端口，于是用例把端口包了一层，从一次存储写入里回调进 `start()`。`src/` 的零计数分支从 48 条降到 47 条。
- **那个表达式里真正有后果的是哪一半。** 不是这条测试当初为它而写的那一半。兜底操作数是没有后果的——`start()` 自己就会把重开挂在 `pendingStop` 之后，所以把整个读取删掉，实测的 `start` → `stop` → `start` 顺序依然成立。这条用例真正钉住的是它上面一行的路由：把 `if (this.stopping)` 改成 `if (this.stopPromise)` 会让它变红，而文件里其余 192 个测试对此毫无反应。
- **一个候选测试被写出、被测量、然后被删除。** 第二个用例把重开排在一个 `stop()` 被挂住的传输后面，看起来正是那个闩的显然钉法——可它在四种单腿变异下全部通过。有限次数的 await 微任务看不见一个必须等定时器队列的等待，于是这个测试被删掉，而它的零击杀率写在了代码位置上；“删掉它 194/194 依旧全绿”这个数字本身就是结论，而不是一个等着更好测试来填的空洞。
- **有一条守卫，删除它的表现是崩溃，不是断言失败。** 把那个读取删掉，重开的“醒来—发现还在 stopping—重新排队”就变成一个微任务忙等，饿死传输停止所依赖的那个宏任务：`tests/lifecycle-invariants.test.ts` 在约 40 秒后于 `Builtins_RunMicrotasks` 内因内存耗尽中止自己的 worker，而 `data-bus` 里的每个测试仍然全绿。位置注释现在写明：那条 fuzz 就是这一行的唯一见证——这也是对任何为了提速而删掉它 stop/restart 交错序列的人的警告。
- **三处随包发布的声明被按代码撤回，正好对应声明腐坏的两种方式。** 一个计数漂了（`?? this.currentRecord` 兜底有三处，不是“那相同的一对”，而且第三处压根没有任何位置说明）；一个测量不再复现（重开兜底那句“只失败一个测试”，被现在写在它原地的四个实验取代：194/194、194/194、51/194、194/194）；以及十一处指向本仓库自己文件的 `:NNN` 行号引用里有六处指向了别处——其中两处落进了*另一段注释*。所有位置引用现在都改成符号或条件；保留下来的编号是依赖的（`centrifuge@5.7.4`，连它最后一个 `emit('error')` 都重新核对过）；而整场纯文字改动是靠比对 esbuild 产出的 JavaScript 与上一个 revision 来证明没有碰到代码的。

## 0.21.20 已完成范围

- **六条未覆盖分支，是被“从来没有测试发出过的调用”关闭的，而不是被更难触达的守卫。** `publish(topic, data, messageId: string)` 是导出的 runtime 上一条声明过的重载，而它的归一化逻辑从未执行过——因为总线总是传对象，也没有任何测试以字符串去调用 runtime。单项的 `publishBatch` 同理：每个用例都同时给了 `messageId` 和 `timestamp`，于是“只给前者”“只给后者”“两个都不给”这三种形态从未被委托出去过。
- **其中哪些有后果，哪些只换来一个计数。** 缺失的 metadata 以 `undefined` 抵达 `sendControl`，于是处理器被以**三个**而非五个参数调用——参数的个数，正是监听方区分“根本没有 id”和“id 是 `undefined`”的方式。两个部分填写的形态被覆盖了，却没有任何断言把它们与“总是构造对象”区分开：因为接收侧会用 `metadata?.x === undefined ? {} : …` 再归一化一次，省略一个键与把它的值设为 `undefined`，在线上帧里是同一个东西。
- **`clusterKey: ''` 是一个命名空间别名，而随包发布的那句话说法相反。** 命名空间取自 `options.clusterKey || '__default__'`，所以空键哈希的是那个字面量字符串，会与 `'__default__'` 共用存储和 BroadcastChannel——可这个选项自己的 JSDoc 说的是不同 cluster key“彼此隔离”，而这个绝对化表述会通过类型声明和打包产物到达使用者。两侧现在都被固定下来（别名本身，以及第三个键依旧隔离），例外也写在了选项声明处。这里是把行为记录在案，而不是修改行为：改动 `''` 会悄悄迁走每一个已经在传它的调用方的存储。
- **账本的第三档，这次是逐条以“删掉兜底、跑 `tsc`”确立的。** `noUncheckedIndexedAccess` 让一次索引读取无论空洞是否可达都是 `T | undefined`；`exactOptionalPropertyTypes` 则禁止条件展开写成常驻形态（`TS2769`），而此时整套测试依然全绿——所以这些腿既跑不起来也删不掉，编译器是为它们唯一存在的检查。把这套探针用到路由写入那块代码里，把五条由编译器持有的腿与一条能裸着编译的腿分开了：`(previous?.generation ?? 0)` 不可达，是因为三行之上那个 null 过滤，而不是因为它自己的表达式。在覆盖率报告里，这两者长得一模一样。
- **动了什么，什么没动。** `cluster.ts` 的零计数分支从 20 降到 14，`src/` 从 **1962 中的 54 降到 48**，分支聚合覆盖率 97.55。没有协议帧、默认值、存储键、导出或行为发生变化，混版本对端感知不到任何新东西。

## 0.21.19 已完成范围

- **一个夹不住任何东西的 clamp，以及它毒化的整个指标窗口。** `recordDispatched()` 把每一次有配对 receive 的分发都记成一个延迟样本，而其中的 `Math.max(0, …)` 是为*时钟倒退*准备的——`NaN` 会径直穿过它。于是样本计数上升，而写入桶位的赋值落在了非索引属性 `"NaN"` 上，任何分位数遍历都读不到它。实测：一次对公开的 `trace.now()` 时钟的非有限读数，会让 `dispatchAvgMs` 变成空（`NaN` 经 `JSON.stringify` 序列化为 `null`，字段是消失而不是读错），并把 `dispatchP95Ms` 与 `dispatchMaxMs` 钉在直方图的天花板值上，而 `dispatchP50Ms` 仍然打印出一个看似合理的数字。现在，无法测量的延迟不再记为延迟样本，与“没有配对 receive 的分发”同一条规则；负值依旧被夹成 0，因为那一对测量是真实的，只是顺序错了。
- **两个此前从未被“不完整的持久化适配器”要求执行的能力检查。** `clearAll()` 与 `clearBefore()` 各自把持久化操作放在一个可选方法检查之后，而这两个方法在 `DataBusReplayPersistence` 上都是可选的——可测试套件里的每个用例要么注入完整适配器，要么一个生命周期只发一次 cutoff，于是两处“没有持久化操作”的分支都不曾执行。新增用例之一断言：一次没有持久化之事可做的本地清除会成功，*并且*不上报持久化错误；另一个断言：重复的 `clearBefore` 中最新的截断点获胜——而在内存过滤器是唯一执行手段的地方，这正是调用方已经要求过的那个截断点。
- **覆盖率账本里的第三档：既不可能执行、也不可能删除的分支。** 开启 `noUncheckedIndexedAccess` 之后，一次索引读取的类型是 `T | undefined`，无论空洞是否可达；因此满足赋值所需的 `?? 兜底` 是类型产物，而不是行为缺口。本轮检视的分支里有四条就是这个形状（写下裸字段后 `TS2532`/`TS2322`，实测），`AGENTS.md` 现在把这一档与原有两档并列写明，好让后续的排查先问编译器，再去找测试。
- **六条分支现在在自己的位置上说明了为何计数为 0。** `websocket.ts` 的 `connectPromise ?? undefined`、`replay-persistence.ts` 的 `dbPromise === pending` 顺落分支、`port-reaper.ts` 的空 handle 分支、`version.ts` 的 `''` 分支，以及三个 `invalidate`+`reject` 闩——它们现在指向那段其实已经覆盖全部四处、却只写在 `load()` 里的枚举。每一条都有一段真实的枚举，而此前没有任何一处读者会去看的地方写着它。
- **账本计数告诉不了你的部分。** 本次发布**没有**关闭任何未覆盖分支槽：`src/` 从 1960 中的 54 变为 1962 中的 54，因为这个修复新增了一个 `if`，而它的两个分支都被覆盖了。缺陷住在一行完全被覆盖的代码里，就在三条各自已有解释的零计数分支旁边。账本是问题的队列，不是 bug 的清单。

## 0.21.18 已完成范围

- **五条没有任何测试点名的分支，各自带着变异归因被固定下来。** 分别是：非字符串的帧 `type` 在进入 `getDiagnostics().protocol.lastUnknownMessageType` 之前被归一为 `null`（以及从来没有读取方的 `getUnknownMessageStats()`）；只写了一个字段的 `replay.persistenceRetry` 必须让另一个字段继承文档里的默认值；`failConnect()` 内部取消连接超时定时器的那一行；没有给出 channel 的 `channelToken` 凭据请求；以及在连接被替换之后才失败的 `Blob` 转换。记录里写明了每个变异会被哪一条断言捕获，也点名了那三条**没有任何变异专门死于它**的分支。
- **关于部分填写的配置对象的教训。** 两处取默认值的位置在覆盖率里都是*热的*——完全不配策略的总线走兜底分支，两个字段都给的走传入值——而介于两者之间的形态，只给一个字段，从来没有被读过。计数会变动不等于配置空间被覆盖；固定测试必须说清它看得见的是哪个默认值，这就是两个默认值各需一个用例的原因。
- **关于异步完成的教训。** 五条里有两条在不同文件里是同一个形状：一个丢弃“已被取代的那次尝试”的结果的守卫，而它是否可达由**别处的某次取消**决定——而那次取消正是没有测试的一行。连接超时守卫现在被写明是“按构造不可达”，并且把它所依赖的那个构造按名字固定了下来。
- **一条随包发布的注释被重写，一个数字声明被撤下。** `src/websocket.ts` 超时回调的注释现在列出了三个条件项为何都无法触达，而不再只解释其中一个；并且删掉了它那句“880 个测试全绿”，选择删除而不是刷新一个会在原地腐坏的计数。注释文本是会发布的：它同时通过打包产物和类型声明到达使用者。
- **没有变动的部分。** 行为、公开 API、协议帧、默认值、存储键和导出全部原样，混版本对端感知不到任何变化。`src/` 的零计数分支槽为 1960 中的 56（减少四个），`validation.ts` 首次达到分支全覆盖。

## 0.21.17 已完成范围

- **一条指向"活着但什么都不持有"的 Worker 的已确认路由，不再永久搁置它的 Topic。** `pause()` 无条件清空
  `assignedTopics`，却只重写它交接得出去的路由，所以一次最终 flush 失败的交接会把持久记录留在原 owner 身上——已确认、
  代次不变。当同一个 worker id 再次注册，每个 peer 都对着这个存活且已确认、却什么都不持有的 owner 让路，没有任何 Tab 的
  transport 还订阅着这条通道，入站投递在所有 Tab 上同时停止，而发布看起来仍然正常。被点名的 Worker 现在每轮 reconcile
  都会修它：本地仍有 handler 就认领回来，否则释放这条记录，并且**先**把删除落盘再广播 `REGISTRY`，好让因通知而
  reconcile 的 peer 读到的是修好的状态。
- **两轮 reconcile 都不可能发现它的原因，写在了代码现场。** 一轮走 `subscribedTopics`，另一轮走 `assignedTopics`，
  而这个幽灵两边都不在；随后 `isAssigned()` 又会通过它的持久路由兜底分支对返回 `true`——这正是该状态保持沉默、
  而不是仅仅变慢的原因。
- **一条会删除共享状态的清扫，加了四道闸门**，每一道都分别测过：必须已经确认（还在等 ACK 的路由是一次进行中的选举）、
  必须是被点名的那个 Worker、先查 assignment map（所以"被选出来但本地没有 handler"这一合法情况不受影响），而明文
  topic 只能来自 `knownTopics`——那是一张只由本 Worker 自己填过的 map。逐个删掉这四道闸门分别会让 6、28、7、1 个测试
  失败；reclaim / release / flush / nudge 四条腿各自恰好被一条断言固定。
- **一个既有测试的前提被改正**：它手写的"被接管"路由带着前任 owner 的 `confirmedAt`，而没有任何 Worker 会写出这种
  状态——它恰好就是新清扫会释放的形状——所以那条断言其实是通过另一个机制成立的，而不是它声称的那一个。
- **找到这个问题的 `storage-fail` 交错被测量过，并且刻意没有留在 fuzz 里**：在修复前的代码上，它从 seed 414 起就让
  sweep 失败，而这个深度是 sweep 自身 100-seed 下限的四倍多，所以它是发现器而不是守卫；它需要的开关
  （`MemoryStorage.failNextWrites`）和复现配方已经提交。
- **一条纯注释的归类**：`handoffAssignedTopics()` 里那条走不到的 `if (!owner) continue;` 现在写清了使它不可达的四个
  环节、每个环节各自被哪个测试固定，以及实测的反事实——删掉它整个测试套件的数字不变，而类型检查会报 5 个错误，所以它是
  下方三处调用赖以成立的类型收窄，而不是一个行为分支。

## 0.21.16 已完成范围

- **延后的 transport 操作现在可以被它自己的释放取消。** `subscribeTransport()` 与 `unsubscribeTransport()`
  先把意图写进 `transportSubscribedTopics`，再把一次*固化的调用*交给 `runTransport()`；因此停在 pending opening
  后面的 subscribe，可能在已经取消它的那个 unsubscribe 之后才 flush。连接于是持有一条不属于任何本地 handler、
  集群分配或路由记录的 channel，而且没有任何路径能恢复——因为防止重复订阅的去重判断读的就是这份过期集合。
  `runTransport()` 现在额外接收“当前期望状态”的谓词，并在每个于后续任务中恢复的位置重新读取。三个回归用例，
  并按腿归属变异结果：两个恢复点各有不相交的击杀集（1 个用例与 2 个用例），而把守卫反向后会有 20 个用例失败，
  其中包括原有的“启动期间排队订阅”测试。
- **协同模糊测试的频道投递现在与浏览器一致。** `ChannelHub.setAsyncDelivery(true)` 把每次 post 延后一个
  microtask；它是可选开启的，因为有 15 个测试发完帧就直接同步断言。在 sweep 的 hub 上启用它，正是把上一个版本
  未解决的那个观察变成上面这个缺陷的原因——两个方向都做了对照测量：修复前六个 seed 失败，修复后为零，而同步
  hub 在坏代码上是通过的。
- **提高保真度所*没有*主张的东西，同样写在原位。** 在已修复的代码上，异步 hub 不新增任何常驻变异击杀；延后
  pump 内部重新读取成员集合属于防御性写法，在当前 fake 下不可观察；退订方向的实测代价是多一帧冗余线上消息，
  而不是被搁置的 channel。
- **本次修复使其失效的随包文档**，中英两侧一并订正：两条订阅传递链都把集合写入画在 transport 调用*之后*（代码
  是先写集合），恢复门段落只归功于取消 generation 守卫，而 `api.md` 的 `subscribe()` 一行只承诺了排队、没有承诺
  取消。

## 0.21.15 已完成范围

- 两个点对点寻址守卫现在都被具名钉住了。`handleMessage` 只按 `type` 分发，而集群频道是广播的，所以
  `targetWorkerId !== this.workerId` 这一行是唯一阻止某个 runtime 处理发给别人的帧的东西；逐行删除后测量，
  先后有 369 个和 358 个测试仍然通过，而协调模糊测试无法断言它，因为它的不变式是结束态检查，收敛得慢一点
  依然算通过。
- `ROUTE_RELEASED` 的那颗钉子需要一个它的陈旧性测试无法拒绝的构造：一条确实把本 runtime 写成 owner 的持久
  路由，配上匹配的 `generation` 与 `handoffFromWorkerId`，再把 ACK 投给第三个 worker。这也正是较早那个伪造
  `topicKey` 的用例从来走不到这一行的原因。
- 协调模糊测试增加了第三层限制，位置选在环路两边都不拥有的地方。原有的两层都由时间构成，而把 `verify` 跑红
  的那个失败把时间花在微任务里，同线程上的任何截止时间都碰不到它；`ChannelHub` 现在会在单个 seed 超过 50,000
  次投递后停止投递，阈值取自实测分布（p50 = 28、p99 = 90、最大值 = 16,763）而不是卡死的大小，被切断的 seed
  会按名字打印并且不计入深度下限。
- 那条环路到底是什么，随后是被测量而不是被推断出来的，而它是测试装置而不是库：11,542 次接受对应 11,538 次
  `confirmRoute` 跳过，而落到存储的写只有 6 次——因为 owner 的确认还躺在它自己的合批写入器里，对端读到的却是
  已刷出的记录；而这个循环之所以让不出微任务，是测试用的 hub 把每一帧都在调用栈上同步投递。把投递推迟一个
  微任务，风暴就彻底消失。本文件里此前把它写成"未修复的产品缺陷"的说法在此更正，而更正之后留下的六个注入帧
  seed 作为下一个问题被点名，而不是被搪塞过去。
- `docs/release-checklist.md` 两种语言现在都写明：`pnpm bench:compare` 会被"调查它"这个动作本身关掉。它的
  基线取自同一个滚动归档，而每次 `bench:browser` 都在往那个归档追加，所以规程变成：先看主机负载，把可疑失败
  连同负载记为 deferred，而不是去追一个绿色。
- `docs/progress.md` 有了结构性门禁。同一个阶段的两个 PR 曾留下两个 `## Phase 142` 块，其中一个被挤到所有
  条目都追加在前的那个锚点之下；`tests/documentation.test.ts` 现在要求每个阶段编号既是新的、又晚于上一条、
  并且在常驻章节之上。

没有行为、线格式、存储布局或集群协议变更：本版本 `src/` 未改动，发布义务来自随包发布的
`docs/release-checklist.md` 与 `CHANGELOG.md`。

## 0.21.14 已完成范围

- 中文版公开文档第一次被逐段对照 `src/` 读过。`package.json` 的 `files` 会发布十篇 `docs/*.md` 加上
  它们的 `docs/zh/` 副本和两份 README，而此前的每一轮审计都是按"当下正在读哪一种语言"逐句修正的——
  于是中文段落保留了英文已经撤回的说法，英文也保留了只有它自己的表格修过的说法。这一轮共处理了
  22 类说法，两个方向都有。
- 协调模糊测试现在可以约束单个 seed 了。一次 `verify` 运行报出
  `stopped at 1046/5000 seeds after 158476ms (slowest seed 151850ms)`，随后撞上 120 秒的单测上限：
  其余 1,045 个 seed 合计约每个 6.6 毫秒，而某一个交织吃掉了整个扫描 96% 的墙钟时间，位于 seed 之间的
  保险丝无法伸进它内部。现在每个会等待的步骤都与一个 2 秒期限赛跑；触发期限的 seed 会被计数、按其操作
  列表点名，并且**不再被断言**——它的终态从未到达静默，在那里断言只会制造假失败。
- 这个守卫不能做什么是被测量的，而不是被假设的：把期限压到 12 毫秒跑完整 5,000 seed，它触发**零**次，
  因为本地能复现的慢 seed 全部是第一个 tab 的 `stop()` 里的**同步**开销——同一线程上的期限无法抢占它。
  于是机制被直接钉住且双向断言，三个变异体各自死在不同断言上：永不超时的期限、过早超时的期限，以及把
  已完成的工作误报为超时的期限。
- `docs/architecture.md` 的 `knownTopics` 一节只列出了实际填充该缓存的六个进程内调用点中的三个；
  `publishBatch` 和 `activate()` 在两种语言里都完全没有出现。
- 本周期在别处反复撤回的那类 storage 绝对说法，原来藏在**表格单元格里**：标识符表的 `topic` 行写着
  "否；仅在 Runtime 内存和控制消息中出现"，而同一张表靠后的行说明 storage-event 的每一帧（含 Topic
  明文）都会整体写入 `localStorage`。此前有四轮审计经过这个文件，其中一轮修正的正是那行相矛盾的内容。
  `configuration.md` 的边界清单还把 tab-id 键归在 `localStorage` 记录里——它其实写在 `sessionStorage`。
- 无行为、线格式、存储布局或集群协议变更。唯一可执行的改动在 `tests/`（不随包发布）；`src/` 中的差异
  仅为注释。

## 0.21.13 已完成范围

- 文案审计读完了它此前唯一没覆盖的公开文本：`src/**` 中**函数体内部**的注释。它们会随包分发，因为
  esbuild 会将其保留进 `dist/**/*.js` 与 `dist/cjs/*.cjs`，而这两者都在 `package.json` 的 `files`
  列表里；0.21.11/0.21.12（docs + README）与声明层 JSDoc 的那一轮各自都漏掉了这半边文本。共修正 33 条
  说法——每一条都是先重读它所描述的代码再动手——另有十余条候选经核查后**保留原样**，并连同保留理由一并记录。
- 其中两条改变的是维护者如何读代码，而不只是读注释。一处"链式 `.catch` 必然 resolve"的证明，依赖的前提是
  "`reportError` 不会抛错"，而同一份文件在 500 行之后自相矛盾，且已有测试钉住那个反例；另有三处注释把自己
  的作用写成"阻止某件邻近代码本就已处理的事"——其中影响最大的是 `isAssigned`：它优先使用内存映射的真正原因，
  是一条没有持久路由就被接受的 `CONTROL/SUBSCRIBE`，而这个状态正是 `tests/cluster.test.ts` 已经钉住的。
- 关于*依赖*的说法现在会点出依赖本身。针对 `centrifuge@5.7.4`：客户端级的凭据钩子只有一个，包里根本不存在
  `getChannelToken`，因此那类请求从未在测试替身之外触发过；而 `CentrifugeSession.unsubscribe()` 会移除
  emitter 自己的 no-op `error` 监听，注释却声称"内部监听被保留"。第二项先被记为一个尚未定论的问题——测试
  替身无法呈现该保护，因此现有测试无论正反都看不见它——随后针对同一个钉住的版本被结案：`BaseSubscription`
  里十二处 `emit('error')` 除一处外全部位于 `_isSubscribing()`/`_isSubscribed()` 判断之后，而
  `unsubscribe()` 之后还能回到那一处的三条路径，分别被触发时的状态复检、客户端只驱动 `Subscribing`
  的重连遍历，以及 `_clearSubscribedState()` 取消的 refresh 定时器封死。枚举写在注释里，也因此没有添加
  任何防御式的重新挂载。
- `pnpm bench:compare --fail-above-pct 50` 补上了它的中位数基线无法提供的那条判据：新值还必须超过自身基线窗口
  中的最大样本，因为在中双峰指标上，中位数只有在两档保持混合时才稳定。被这条判据放过的指标会被显式打印出来而不是
  静默通过，其代价也写在旁边——停留在已观测区间内的持续性劣化从此对门禁不可见。两条判据都由变异测试归属：删掉新增
  那条只让 1 个测试失败，而让它恒为"放过"则让 4 个失败（包含既有的双报告路径）。
- 没有行为、线格式、存储布局或集群协议变更。本次发布中唯一的可执行改动位于 `scripts/`，而该目录并不随包分发；
  覆盖率维持在 99.01 / 96.90 / 99.26 / 99.69，共 **897** 个测试。

## 0.21.12 已完成范围

- 文案审计回到了它的起点：README。`README.md` 与 `README.zh.md` 承诺"持久层不存储连接地址、原始
  Topic 文本和消息内容"，而这句话的两条之上正是在宣传那份 durable 回放持久化——它的 IndexedDB 对象仓以
  `keyPath: 'topic'` 创建，行里保存着 publication payload，而工厂函数是公开导出。该说法现在被收敛到它确实
  成立的范围（协调记录），并把两个会写入真实内容的可选开关点名列出。中文特性列表还补上了它此前完全缺失的两条
  feature 条目，于是不再出现"只有隐私承诺、没有对应披露"的读法。
- 随包分发的 `docs/release-checklist.md` 里有三处会在关键时刻误导维护者。发布后消费者验证门禁被描述为"与手动
  执行相同预算"——实际上工作流以自身 env 传入 48 × 7500 ms，而脚本默认是 6 × 5000 ms，于是手动复跑会在一个 CI
  全绿的 tag 上 30 秒后报 `ETARGET`。以 `v0.21.11` 实测：成功的那次运行重试了 48 次中的 39 次，而离线复验首次即
  通过。现在两个预算都已写明，并给出复现 CI 预算的命令。CI 被写成"在每次 push 与 pull request 上运行"，而两个
  触发器都被过滤为 `branches: [main]`；`bench:trend` 也从"仅本地"清单里漏掉了，尽管它会依据本地归档重写
  `docs/benchmarks.md`。
- `ci.yml` 中记录覆盖率下限的注释在 0.21.7 把下限提升到 98/96/98/99 之后仍写着 96/92/96/97；配置、随包的分发
  清单与 `AGENTS.md` 早已一致，只有注释发生了漂移，现已修正。`AGENTS.md` 同时记录了这轮扫描的结论：审计"选项
  面"覆盖的是表格，而同一标题下的段落与 README 的摘要条目是彼此独立的说法——最值得优先搜寻的句式是关于存储的绝对
  表述，因为一个可选开关就足以把它击穿。
- 无行为、wire 格式、存储布局或集群协议变更，且本发布未触碰任何 `src/` 或 `tests/` 文件。覆盖率保持
  99.01 / 96.90 / 99.26 / 99.69，共 **895** 个测试。
## 0.21.11 已完成范围

- 一轮针对此前从未被扫过的四个文档文件（`architecture.md`、`capabilities.md`、`getting-started.md`、
  `transports.md`，以及它们共同依赖的 `api.md`）的文案审计，找出约十五句代码并不支持的表述，中英文一并修正。
  最要紧的都是"从不持久化"这一类。`architecture.md` 用六句话承诺协调流量绝不写入 localStorage，而
  `channelFallback: 'storage-event'` 在构造上就与之相反：信封本身就是那一帧，因此明文 topic 与 publication
  payload 会留在 channel key 下直到通道关闭，页面崩溃后更是无限期。能力矩阵声称 SDK 不持久化业务 payload，
  而它自己导出的 `createIndexedDbReplayPersistence` 就是反例（对象仓以明文 topic 为 key）；矩阵与入门文档还把
  `workerMode: 'shared'` 说成"复用同一条连接"，实际上每个 port 都有自己的 `CentrifugeSession` 和自己的
  WebSocket——shared 模式省的是进程，从来不是 socket。
- 两条写给 transport 实现者的指引指错了回调、也指错了守卫。自动恢复由 `onStatus('error')` 启动、按恢复冷却窗口
  节流；`onError` 只做记录与通知，因此只通过它上报死连接的 backend 永远不会被重开。generation 计数器只有异步凭证桥
  会比较它——被取代的 Worker 根本到不了 transport，因为监听器早已移除。同一文件的接口块、Worker 联合与 wire 草图
  各自漏掉了确实发布的成员（`publish` 的元数据参数、可选的 `publishBatch`、两个 diagnostics 标签、`TOKEN_*` 与
  `SESSION_REAPED` 类型、批量帧）。
- 有一处过度绝对的说法同时出现在三个地方，其中包括 `cluster.ts` 的一条注释：路由校验丢弃的是指向*另一个* Worker 的
  `CONTROL/SUBSCRIBE`，读不到路由的 topic 会被接受。这个容忍是有界的——没有路由可盖章时 `confirmRoute` 什么都不写，
  下一次 reconcile 会收回 assignment——并由新增测试固化。实测：按旧文案描述采用更严格的规则，会让这条新测试和两条本就依赖
  该容忍的负载加权测试一起失败。
- 同一次"把说法拿去测量、而不是重读一遍"的改动也在此发布：`configuration.md` 的重试封顶语句现在说明了 1600 ms
  上限究竟约束哪一半，两条新测试固化完整的翻倍序列与原样生效的首次等待，两条 `PortReaper` 注释改为描述其真实的保护作用。
- 无行为、wire 格式、存储布局或集群协议变更。全树覆盖率保持 99.01 / 96.90 / 99.26 / 99.69，共 **895** 个测试。

## 0.21.10 已完成范围

- `heartbeatIntervalMs: Infinity`——文档中关闭 SharedWorker PING 心跳的方式——此前被回收器当成了一个
  畸形数字：它与 `NaN`、`0`、负数走同一道 `Number.isFinite` 检查，于是回退到默认 10 秒，也就给
  了这个"注定再也不会发消息"的端口安上一个 30 秒的会话超时。这个选项的本意是"不需要回收器"，结果却保证
  连接 30 秒后必被回收。`Infinity` 现在让该端口豁免于回收，而那三个连 transport 构造函数都会拒绝（因此
  根本不可能来自本库）的值仍走回退路径。用一个测试钉住：三个变异体分别死在三条不同断言上；另有一条断言钉住
  `INIT` 确实带着这个值——这个缺陷的两半各有绿灯测试覆盖，正是它活到今天的原因。
- 发布出去的回收器文档不再承诺代码没有做的事（中英双语）：不存在"SharedWorker 关闭时"运行的
  `PortReaper.dispose()` 钩子（没有任何地方调用它——`MessagePort` 没有 close 事件，因此压根没有可供挂靠的
  信号）；而关闭心跳的代价也和它关掉了什么一起写明了（这样配置的 Tab 若崩溃，其会话与 WebSocket 会原地保留）。
- 一个"测试全绿却把门禁弄红"的缺陷：IndexedDB 回放的一条断言在某个 await 之后才挂上 rejection handler，
  而那个 await 可能被真实的 40 毫秒定时器超过，于是在繁忙的 runner 上 `pnpm test:coverage` 会以
  `Unhandled Rejection` 退出 1，而所有测试都显示通过。现在 handler 与 promise 一起创建——把往返强行拉长到
  200 毫秒后两个方向都实测过。
- 没有改变线路格式、存储布局或集群协议。行为变化仅限于 shared 模式下配置 `heartbeatIntervalMs: Infinity`
  的情形，而那种配置此前根本无法按文档所说工作。

## 0.21.9 已完成范围

- 被饿死的 SharedWorker Tab 曾经会静默丢失 transport 并且永不恢复：一个长同步任务，或后台
  Tab 受到的定时器节流，都会让共享线程里的回收器收回该端口；而 `MessagePort` 没有 close
  事件、向已关闭端口发送消息会“成功”却什么都不送达，于是这个 Tab 仍在持有自己的路由、仍在
  报告 `healthy` / `connected`，而它发出的发布全部消失。现在回收器会在关闭端口前先在该端口上发出这次回收的通知，transport 把它当作一次普通的 backend 失效，于是既有的恢复流程会重建会话。
  真实浏览器两个方向都验证过：有通知时 36.7 秒自动重建，去掉通知后 90 秒内始终不恢复。
- 共享模式的所有权迁移此前完全没有浏览器级覆盖。现在三个共享 Tab 会固定住：关闭 owner 时，
  那条唯一的服务端订阅必须落到一条本来就早已打开的连接上，并且交接之后两个方向都仍能投递。
- 没有改动任何线格式、存储布局或集群协议版本：新增的消息只在页面与它自己的 Worker 之间流动，
  不认识该类型的主线程会直接忽略。

## 0.21.8 已完成范围

- 第二个随包分发的句子同样是错的，性质与 0.21.7 那条一致。`docs/api.md` 在中英文里都承诺任何公开 trace 事件都不包含原始 Topic，而 `docs/configuration.md` 在两种语言里写的恰恰相反，还进一步提醒接入方在把 sink 转发给遥测前对 Topic 名称脱敏。事实是：subscription 事件携带它所报告的 Topic，涉及具体路由的 `reliability` 操作也一样。修正后的文字只陈述测量到的东西：任何事件都不携带消息 payload、连接地址或错误正文；Topic 明文恰好只出现在这两种事件里；`coordination` 使用不可逆的 key 来标识路由。现在有一个测试把 payload 哨兵与错误正文哨兵穿过真实的 bus，并断言 sink 两者都看不到——这是这两条声明第一次拥有见证。
- 套件里那个长期被当作共享 runner 抖动的 E2E 失败并不是负载。shared 模式的下线用例断言的是 demo server 的全局连接数，而 specs 是并行跑的，其它存活 tab 都会被算进去；即便通过也是空过：一个 topic 只有 owner 的 transport 会订阅它的频道，所以「2」可能是一个持有频道的 socket 加上一个什么都没订阅的 socket。现在 socket 按各自持有的频道来寻址，用例也同时断言两半：关闭的 tab 的 socket 必须从服务器列表里消失，存活 tab 的那个必须仍在列表里。把该用例重复三次（旧断言下三次全败）现在能通过。
- 两个带种子的不变量 harness 分别跑到出厂深度的 80 倍与 133 倍（400,000 与 200,000 个种子，753.7 秒与 262.6 秒），没有发现任何破坏，因此剩下的零计数腿是分类工作，而不是隐藏的协同缺陷。它们的文件头现在记录了可用深度，以及能结束一次深跑的三道彼此独立的限制。
- 一个一直被读反的覆盖零值现在按它真正计量的东西标注：当 `??` 的兜底是一个箭头函数时，分支区间落在该闭包体内，所以这一臂统计的是被调用，而不是有多少调用方省略了参数。全仓库扫描下来，这是唯一一条这种形状的腿，规则已写进 `AGENTS.md`。
- 本次发布没有行为、线格式或存储布局的变更。

## 0.21.7 已完成范围

- 一处随包发布的说明是错的。`docs/api.md` 承诺「该 topic 最后一个 handler 退订时清空重放缓冲」，同时又说通配订阅会回放所有匹配 pattern 的已缓冲 topic——但 pattern 不是一个 topic：历史是按具体发布 topic 归档的，所以退订 `chat.*` 一条都不会清空，而 durable 清理拿到的是一个没有任何记录对应的键。中英文文档现在都写清了每次清理作用的键、`maxPerTopic` 限制的是每条环的深度而非环的数量，以及真正能回收内存的那几个调用。行为没有改动（按 pattern 剪枝会连带丢掉其它仍在线的订阅者拥有的环），并新增一条测试把这个边界钉住，避免文档再次漂回去。
- 补上一个真实的测试缺口：`ReplayManager` catch 中的 hydration epoch 检查从未执行过。`resetBuffers()` 只抬 epoch 不抬 retry generation，于是重置之后到达的加载失败会替「替换它的那个会话」上报错误，并把完成闩锁置位——那正是会抑制新会话加载的状态：一次幽灵 persistence error，外加该实例整个生命周期内 durable 历史缺失。
- 接收侧的帧校验规则（0.21.5 / 0.21.6）从维护者笔记搬进了 `docs/architecture.md` 的两个语言版本，包含两条刻意不做的检查，以及非 JS 对端要怎样才能被接受——这些守卫是 fail-closed 的，坏帧被静默丢弃且没有任何回应，对接方不看文档就无从知道。
- 五个模块里约十条 zero-count 分支用实验而不是推断定了下来：一条被真正钉住，其余的在原地处标注了「谁覆盖了它」以及删掉它的代价。没有删除任何一条，本次发布也没有声称任何未经测量的覆盖率提升。
- 覆盖率门禁重新变成了门禁：96 / 92 / 96 / 97 的下限已经比实测分支覆盖率低了 4.7 个百分点，意味着丢掉一整个模块的测试仍能通过包括发布门禁在内的所有检查。现在是 98 / 96 / 98 / 99，留出的余量只为唯一一个真正依赖运行机器的因素（模糊测试按墙钟界定深度），并且用「把阈值抬高到超过实测值就会失败」验证过它有牙齿。
- 本次发布没有改变任何行为、线格式或存储结构。


## 0.21.6 已完成范围

- 0.21.5 的协议修复其实只覆盖了读取这组字段的两个处理器之一。`ROUTE_RELEASED` ACK 的授权方式完全相同——依据 `topicKey` 对应的持久路由——然后把它携带的 `topic` 写入 `assignedTopics` 并让传输层订阅它；因此一个同源脚本只要从 localStorage 读到真实的 key、记录在案的前任 owner 和 generation，就能用任意频道名回应一次正在进行的交接。现在两个点对点处理器都会校验 `createOpaqueKey(topic) === topicKey`；`EVENT` 与 `REGISTRY` 不需要这条检查，因为它们不携带“key + 明文”这一对字段。
- 批量 `PUBLISH` 帧的 `items` 此前只做了真值加长度的判断，而 `publishBatch()` 的 `Array.prototype.map` 是唯一的产生者。手工构造的 `{ length: 2 }` 会让 `message.items is not iterable` 从集群自己的 `BroadcastChannel` 监听器里抛出；可迭代的 `"ab"` 会变成两条 `data` 为 `undefined` 的发布，并以接收方 worker 的会话发出；空数组则两半条件都不满足，落到单条发布分支上把 `undefined` 发出去一次。现在，携带 `items` 的帧必须给出非空数组。
- 账目上的一条是通过证明断言本身失效而关闭的：恢复次数耗尽的一次性上报，其第二处“`exhausted` 诊断只有一条”的计数从未进入过对应分支——因为一次失败的重开之后，安装到传输层上的处理器属于已被取代的生命周期。改成用应用在耗尽后真正会走的需求路径去驱动第二次失败，这个钉住才成立——现在删除该保护会让测试套件失败。`data-bus.ts` 未覆盖分支臂 13 → 12。
- `AGENTS.md` 把流程教训与不变式写在了一起：保护*协议*不变式的守卫属于协议，而不是最初发现违规的那个处理器，因此钉住一条不变式意味着枚举它约束的每个读取点——这正是上一版“修复”漏掉的那一步。

## 0.21.5 已完成范围

- 关闭的是协议漏洞，不是覆盖率条目：`handleControlMessage` 依据 `message.topicKey` 对应的持久路由来授权归属，随后却把 `message.topic` 当作明文去记忆、登记并交给 transport。由于 `topicKey` 就是 `createOpaqueKey(topic)`，任何同源脚本都能向集群的 `BroadcastChannel` 投递一帧“真的 key 配任意 topic”的消息：它以 key 为准的校验会通过，而接收方 worker 订阅的频道名被替换。现在除非两者一致，控制帧一律丢弃；本库自己构造帧时就是这样推导的，因此合规对端不受影响。
- 同一次审计对后续问题给出了相反方向的结论并写进文档：`EVENT` 只做形状校验，因为投递仍要求本地存在订阅者，伪造一帧最多只能往本标签已经关注的主题上投递一条发布——这没有超出同源脚本直接调用页面自身 bus 能做到的事。`AGENTS.md` 记录了这一区分，避免有人把它“加固”成破坏跨版本兼容。
- 明确了一条防挂起契约：`createStopPromise()` 的注释曾断言 `performStop()`“永不 reject”，因此 teardown 闸门的 rejection 分支从未被任何测试执行。它确实会 reject——其 `catch` 经由 `reportError()` 上报，而后者会运行错误订阅者，这些订阅者只靠写 `console.warn` 被吸收——在该路径上 `resolveGate()` 是唯一能让 `await bus.stop()` 结束的东西。注释现在写明这条链，测试则用看门狗与 stop 竞速，删掉这条分支会在 250ms 内明确失败，而不是变成一个超时。
- 这条上报路径的最后一环也被钉住：如果 console 上没有 `warn`，未加保护的日志调用会从“用于容纳失败的处理器”内部抛出 `TypeError`，穿过 dispatch 循环逃逸到 transport 的消息回调里。
- 测试框架的可诊断性，源自一次无法诊断的 CI 失败：两个种子化 fuzz 现在会在扫描过程*进行中*报告深度，因为循环结束后的截断日志恰好在它唯一要解释的事件上无法触发（宿主要超时上限杀掉测试时，只会打印 `Test timed out`）。预算与超时上限的调整被刻意推迟，等心跳数据说明是哪一种原因。
- 本次发布的账目变化：`data-bus.ts` 未覆盖分支臂 17 → 13、未覆盖函数 3 → 2；其中三条被重新归类为“由调用点支配”而非再去凑测试，另有一次构造尝试经测量证伪后回退。全量分支覆盖率 96.41 → 96.63，共 876 个测试。

## 0.21.4 已完成范围

- 修掉的是一个真实缺陷，而不是覆盖率数字：失败记录用 `String(error)` 渲染拒绝原因，而这个转换并不是全覆盖的——`Object.create(null)` 根本没有原始值转换，而 `DataBusTransport` 是由应用实现的公开端口，可以用任意值 reject。于是"报告一次失败"会制造出第二次失败；`getRecoveryStats()` 里也有同样的裸转换，所以最后一场失败无法字符串化的总线，其 `getHealthSummary()` 会直接抛异常——本该解释故障的探针在故障期间拒绝回答。
- 可观察的那一条：open 失败、其清理又同时失败之后的重试，永远不会重新打开传输。因为 `createStopPromise()` 以 `.catch(error => this.reportError(error))` 收尾，这个 handler 自己抛了，于是 `pendingStop` 变成 reject，`start()` 串在它后面的 `.then()`（正是 `0.21.3` 删掉吞除分支的那一处）被跳过，`transport.start()` 再也没被调用，调用方只拿到 `TypeError: Cannot convert object to primitive value`——一句关于格式化器上限的话，而不是关于他的传输。`describeFailure()` 让记录器变成全覆盖，而这正是那条链式 await 一直需要的性质。
- 这同时补回了 `0.21.2` 与 `0.21.3` 立论所依赖的那一步。两次删除吞除分支所依据的 `pendingStop` 赋值点枚举是对的；但"以 `.catch(error => this.reportError(error))` 收尾，因此一定 resolve"这个结论，悄悄假设了 `reportError` 不会抛。两处注释现在明确写出这层依赖，并有一个测试端到端驱动"两次都失败的 open"链条，于是那两次删除依据的是验证过的事实而非假设。
- 同样的缺陷形状在配置校验里也被修掉（`assertPositiveSafeInteger`、`assertPruneStrategy`、`assertHeartbeatInterval`）：对调用方传入的值做 `String(value)`，使一条本应说明配置项写错的告警，变成格式化器抛出的 `TypeError`；而在 `assertPruneStrategy` 里，这个转换本身就是枚举值成员判定。
- 顺带得到一条测试经验：无论校验器是在拒绝这个选项，还是在拼接消息时报错，`toThrow(TypeError)` 都会通过，所以新增断言钉的是消息文本；并且因为 `stopShouldFail` 只能以真实 `Error` 失败，`FakeTransport` 增加了 `stopRejection` 来指定 reject 的**原因**。

## 0.21.3 已完成范围

- 再从 `CrossTabDataBus` 删掉两个永远不会触发的拒绝吞除分支，证明方式都是枚举被吞 promise 的赋值点：`stopPromise` 只有一个非空赋值（`stop()` 手工 resolve 的门，两个 settle 分支都会 resolve 它——这正是公开的 `stop()` 不会拒绝的原因），而每个非空 `pendingStop` 链都以终止性的 `.catch(error => this.reportError(error))` 收尾。`data-bus.ts` 函数覆盖率由 115/120 变为 115/118，且没有删除任何测试。
- 该模块的未覆盖处理函数现在只剩三个，且各自带有写明理由：一个是刻意保留的兜底（防止 `performStop()` 出现未处理拒绝），其余的还欠一次调用点枚举，才能判定为被支配或补上断言。

## 0.21.2 已完成范围

- 删掉一个死的吞异常分支，而不是让它永远作为"未覆盖函数"留在账上：`performStop()` 用 `.catch(() => undefined)` 等待待完成的传输停止，而证明这条臂跑不到的依据，就是 `pendingStop` 仅有的两个非空赋值点——每个链条都以终止性的 `.catch(error => this.reportError(error))` 收尾。它上面那行对 `startPromise` 的吞除则保留，因为那个 promise 确实会拒绝：它是失败的 `ready()` 交给调用方的那次 open。
- 两个判断以及得出判断的方法，都写在了下一位读者会看的地方：源码注释，以及与既有"同步 catch 守卫"规则并列的 `AGENTS.md` 约定。整体覆盖率由 98.68 / 96.16 / 98.54 / 99.45 变为 98.71 / 96.16 / 98.72 / 99.45，没有删除任何测试、没有调低任何上限、也没有任何行为变化。

## 0.21.1 已完成范围

- 库行为没有任何变化；本次发布的增量全在示例、测试与文档面。`pnpm build:examples` 从本地安装打包 `react` + `react-dom/client`，产出被 git 忽略的 vendor 模块，取代原先让 `examples/react` 在无网络 CI 上根本加载不出来的 `esm.sh` 引用——并且那个页面跑的是 React 18，而适配层测试跑的是 19。
- 现在同一组四个浏览器用例驱动两个示例页（扇出、响应式换 topic 后旧 channel 在服务端订阅数归零、owner 标签关闭后仍能送达、清空 topic 输入框后的回退），因为 React 页补齐了 Vue 页早已具备的 id、`?topic=` 覆盖与 `__reactBus` 诊断钩子。只命中某一个页面的失败因此能定位到适配层而不是库，而且回退那条断言在两个页面上都经过变异验证。
- `docs/getting-started.md`（中英双语）写清了浏览器套件到底覆盖哪些页面；`0.21.0` 发布说明里提到的 `cross-tab-worker-databus/react` 导出其实并不存在（真实子路径是 `cross-tab-worker-databus/hooks`），已发布的文本不会被改写，因此在 `Unreleased` 中更正。

## 0.21.0 已完成范围

- 项目第一个弃用周期正式结束：`subscribe("")`、`publish("")`、`publishBatch("")` 由 `0.20.96` 的每实例告警一次改为抛出 `TypeError`。守卫与选项校验断言放在一起，且先于其他任何副作用执行，因此被拒绝的调用不会请求 start、不会登记 handler，也不会写入路由记录——`publishBatch("", [])` 同样先于文档承诺的"空数组 no-op"被拒绝。
- 这条边界由两个用例而不是一个撑起：空数组用例的存在，是因为其他任何空 topic 批量调用都会经由单条目委托被 `publish()` 拒掉，所以放在 no-op 之后的守卫会静默返回且无人察觉；每条报错都使用调用方实际使用的方法名，原因也是这个。
- React 与 Vue 示例页获得 demo 页早就有的 topic 回退，而两个新的浏览器用例先记录了"没有回退会发生什么"——`TypeError: CrossTabDataBus.subscribe("")` 以 `pageerror` 冒出来，同时 Vue 标签页仍在渲染它上一个 topic；demo 页则落到 错误 并留下一条事件流错误行。`||` 与 `??`、有无 `trim()` 各被不同的断言臂杀掉，均经变异验证。
- `docs/getting-started.md`（中英双语）不再夸大浏览器测试的覆盖面：只有 Vue 适配页会在真实浏览器里被驱动，因为 React 页的 React 本身来自 `esm.sh`，在没有网络的 CI 里根本加载不起来。该页的 topic 回退因此是本次变更唯一靠手工验证、而非靠门禁覆盖的路径。
- 覆盖率上限保持不变且仍然达标（98.68 / 96.16 / 98.54 / 99.45，对应下限 96 / 92 / 96 / 97）；这个版本向 `pnpm check` 增加一个单元用例，向浏览器作业增加两个用例。

## 0.20.97 已完成范围

- 公开接口与可观察的运行时行为均未改变。发布的 `src/` 变更只涉及 `centrifuge.ts`：删除两处不可达的 `typeof Worker` / `typeof SharedWorker` 抛错，并以注释记录为何一个守卫被保留、另一个被移除。其余全部是示例、测试、CI 与文档工作。
- Vue 适配层终于运行在它真正交付的环境里：`examples/vue/` 用本地安装的 `vue` 包（不依赖 CDN）挂载真实的 `cross-tab-worker-databus/vue` 组合式 API 并连上本地 demo 端点，`e2e/adapters.spec.ts` 在真实 Chromium 标签页中驱动它——两个标签互收消息、响应式切换 topic 后服务端释放旧 channel、owner 标签关闭后仍能送达。在此之前 Vue 入口从未在 jsdom 之外执行过。
- 跨 tab 之间的核心契约由手工排列变成随机模糊：三个 bus 共享一套存储注册表与 BroadcastChannel，随机交织订阅/退订/发布/隐藏/显示/停止/启动/心跳/丢弃帧/伪造 `SUBSCRIBE`，在静默态断言每个存活 topic 恰有一个 owner 与恰一个 transport 持有者、已离开的 topic 不留残留、一次发布对每个存活订阅者恰好扇出一次。五个变异体能杀死这些断言；三个守卫存活，因为 `reconcileAssignedTopics()` 会修复它们——这一点被记录下来，也正是它们属于帧级回归的原因。
- 两个发布门禁的计时失败被追到根因而非重试了事。协调模糊测试的 `Date.now()` 预算读的是本套件自己伪造的时钟——复用 worker 里残留的假 `Date` 让同一个文件一次在 16.4 秒停在下限、另一次在 CI 上烧掉 539 秒——因此 `tests/setup.ts` 现在在每个测试后恢复真实计时器，模糊测试改用 `performance.now()` 计预算。热路径性能门禁移入独立的顺序步骤，因为绝对毫秒上限只有在未被调度的核上才度量代码。
- 一次凭证守卫的调查结果恰是"守卫留下"：一个以不可达为由删除它的 PR 被证明是可达的——入口代码（`getToken()`）可以在抛错前先 `stop()` 传输——于是它获得了经变异验证的回归用例，而区分"可达"与"确实被上层支配"的规则写进了 `AGENTS.md` 与源码。
- 仅 CommonJS 才有的默认 Worker 失败由构建产物钉住：`dist/cjs` 无法经 esbuild 的 `import.meta` 垫片解析 Worker URL，因此 `start()` 必须给出可操作的 "provide workerFactory explicitly" 而非 `TypeError: Invalid URL`；两个工厂函数都被断言，且同一用例同时检查 ESM 产物，从而把失败绑定到模块格式而不是文件名。
- 全量覆盖率从 98.62 / 96.02 / 98.54 / 99.38 提升到 98.68 / 96.17 / 98.54 / 99.46，且未下调任何阈值；`docs/transports.md`（中英）记录了"自带字符串 `topic` 的发布会被重新寻址到该 topic"这一规则——它既是通配符投递指明具体 topic 的方式，也是 `{ "topic": … }` 载荷可能无声丢失的原因。

## 0.20.96 已完成范围

- 开启项目第一个弃用周期：空 topic（`""`）现在会在 `subscribe()`、`publish()`、`publishBatch()` 中每个 bus 实例告警一次，而不再静默登记一个没有任何 transport 能寻址的 channel。行为保持不变；按 pre-1.0 策略，后续小版本会在该边界直接拒绝——`0.21.0` 已完成这一步。契约已写入 API 参考与升级指南（中英双语）。
- 用证明取代假设，清空覆盖率分支台账：十一个"测试根本不可能失败"的行为获得经变异验证的回归用例——离开方 owner 删除无人订阅路由而非交接给存活 peer、最后一个远端订阅者离开时 owner 释放自身 transport 订阅、被取消的持久化清扫保持静默、被取代的水合快照被丢弃而非合并、默认平台 `WebSocket` 构造（含子协议）、恢复预算耗尽后 `ready()` 交出真实传输错误、Vue 适配层吞掉被拒绝的 `ready()`、把 `connectTimeoutMs` 设为 0 或 Infinity 时确实无限等待、私有 topic 在其唯一持有者隐藏时不留路由记录、在没有 `console.warn` 的运行时吞掉抛错的 trace sink，以及无关的已持有通配符 pattern 无法截获发往远端 topic 的批量消息。
- `src` 中其余零计数分支已全部归类：被支配、按构造不可达，或属于缺失的边界校验（正是它引出了上面的弃用项）。`vue.ts` 与 `hooks.ts` 四项指标均达 100%，`cluster.ts` 只剩一行未覆盖。
- `AGENTS.md` 记下让早期草稿变成装饰性测试的陷阱：共享 `ChannelHub` 的 peer 会自愈被观测的状态、缺少一次 microtask flush 的 peer 对其他 Tab 不可见、文本重复的守卫会让非全局替换改到错的那一份。
- `typescript-eslint` 升级到 `8.70.1`；在 lint 工具链的 peer 范围仍排除 7.x 期间，TypeScript 保持 `6.0.3`。

## 0.20.95 已完成范围

- 删掉只用来抬高覆盖率的不可达防御分支：存储写入器"重试已排程"守卫、两处有界映射淘汰循环里的 `undefined` 判断，以及 Centrifuge 传输层的三个 Worker/port generation 检查——后者即使可达也无法分辨是哪个 Worker 报的错。
- 移除 React 适配层的 lifecycle generation ref（React 的 effect 顺序永远不会走到它的任一分支），并修正配置文档中描述该机制的说法。Vue 适配层的守卫确实起作用（其函数体会 await 一次 stop），保持不变。
- 堵上 Vitest 断言漏洞：`rejects.toThrow('message')` 在 reject 原因为 `null` 或 `undefined` 时同样通过，这使 IndexedDB 回放测试里所有 `reason ?? new Error(...)` 兜底消息断言形同虚设。新增 `expectRejectionMessage()` 同时校验 Error 实例与消息，并把该约定写入 `AGENTS.md`。
- 再有六个行为获得经变异验证的回归用例：存储事件通道的畸形载荷守卫、入门指南中描述的无 BroadcastChannel 回退接线、存储写入器的键枚举、去重清扫的过期保留、迟到的凭据失败路径，以及 React hook 在依赖变化后交出最新 bus。`environment.ts`、`storage-batch.ts`、`dedup-manager.ts`、`hooks.ts` 四项指标均已达 100%。

## 0.20.94 已完成范围

- 修复 Vue 订阅 composable 对自身 `handler` 参数注册的永久空转 watcher，并钉住同一目标重绑守卫：bus 与 topic 在同一 tick 变化时不再拆掉刚建立的订阅再重建。
- CI 单元门禁不再被 runner 负载拖垮：为反复拉起子进程的 package/compat 门禁设 15s 上限，为已在覆盖率插桩下逼近自身 30s 预算的生命周期压测器保留显式 120s 预算。
- 覆盖率下限从"装饰性"改为真正生效：此前下限 85/80/90/85 与实测 98.13/94.59/98.17/99.19 相差十余个点，现为 96/92/96/97。
- 为九个此前没有有效测试的行为补上经变异验证的回归用例，其中包括一条在删掉淘汰循环后仍能通过的路由 owner 缓存断言、下一次 `start()` 的重水合、远端批量解包中无 metadata 的条目，以及被替换 client 的状态隔离。

## 0.20.93 已完成范围

- 强化 package 元数据、无条件与 fallback array target、递归嵌套及自定义 export condition 的兼容性门禁。
- storage 能力探测必须完成可读的写入-读取-删除往返；channel 构造失败时安全降级；sessionStorage 不可用时保持稳定的内存 tab identity。
- 在 clear 与 cluster teardown 时取消失败的 storage retry，并用 cluster 级 restart 回归覆盖收敛行为。
- 更新 patch 级开发依赖，完成发布、浏览器、打包、兼容性与安全全量门禁。

## 0.20.92 冻结范围

当前开发线继续验证 predecessor reopen 结算、排队启动就绪与 stop promise 清理等生命周期错误路径。该开发线现已落地十六处修复。第一处让共享 stop gate 先于同步 teardown 前奏安装：从同步 STOP lifecycle trace 事件重入的 `stop()` 会共享同一次 teardown，而不是再调用一次 `transport.stop()`。第二处在 START trace 发出前安装 lifecycle epoch 与 `startPromise`：若同步 trace/status 回调重入 `stop()`，外层 `start()` 会立即停止后续 timer、cluster 与 topic 启动，旧 opening 由 epoch guard 放弃，transport 不会被重新打开。第三处把相同的「先安装 lifecycle、再发同步回调」顺序应用到 `reopenTransport()` 的 CONNECTING 状态通知，使恢复期间重入的 stop 能取消本次 reopen，而不是在 teardown 后重新打开 transport。第四处让同步 RESUME trace 回调内的 stop 同时取消显式 `start()` 与原生 `pageshow` 恢复；`WorkerClusterRuntime` 通过 lifecycle generation 阻止外层 pageshow 在 `onResume` 已停止或暂停 cluster 后再次 `activate()`。此前这些场景都可能留下 `state: stopped` 但 transport 或 cluster 仍活跃的半停止状态。第五处让已经停靠在 recovery gate 上的 transport 操作不再因等待中的自动恢复尝试失败而滞留：`runTransport()` 现在统计停靠操作数，自动尝试失败时若仍有 waiter 会立即发起一次 on-demand reopen，使在 cooldown 期间发出的 `publish()` / `subscribe()` 自身即可驱动恢复，而不必等待之后某个无关操作。demand reopen 自身失败时仍会重新武装 flag 留待后续操作重试，而不会在自身失败上自循环；一旦 reopen 成功，全部 waiter 会按序 flush。cluster-key 隔离保证现在由双 runtime 回归固定：不同租户可独立拥有同一 topic、publication 不会跨命名空间边界、持久化 key 使用不同 opaque hash 且不暴露明文 key。第六处 replay retention 修复会在 suspend/resume 期间旧 cleanup 事务仍在收尾时，把新排队的最新 cutoff 交给新一轮 cleanup，避免它一直滞留到未来某个无关 publication 才被处理。第七处在 `reopenTransport()` 发出同步 CONNECTING 通知前清空 `transportReady`，避免同一 tick 内第二个操作把仍在关闭的旧 transport 误判为可用连接；所有操作都会继续停靠在 opening 上，并在 replacement 打开后按序 flush。第八处让 pagehide suspension 续体具备 epoch 感知：若同步 DISCONNECTED status 回调按公开恢复路径调用 `start()`，旧 hide 续体会被放弃，不会在 replacement 打开后再次停止 transport 并留下错误的 healthy 状态。第九处修复保留 durable hydration 与实时流量的回放顺序：若 `load()` 尚未完成时已有 publication 写入，加载快照会排在实时消息之前，数量裁剪因此保留最新的实时消息，而不会把旧历史误认为更新内容。第十处让 replay hydration 能跨 lifecycle 替换：清除全部、清除单 topic、退订与 cutoff 裁剪会应用到仍在进行的加载快照，避免已清除历史复活；suspend/restart 会取消被取代的 load 并启动新一代 hydration；显式 stop/start 后也会重新加载 durable history，而不是留下空 ring。第十一处补上严格交接的 generation 缺口：`ROUTE_RELEASED` 必须与存储 route 的 generation 精确相等，其他交接轮次的 ACK 不能确认当前 route，也不能提前释放其 `SUBSCRIBE`。第十二处修复排队重启与 BFCache 的竞态：`WorkerClusterRuntime.start()` 会先安装 lifecycle listener 再检查可见性，因此排队在异步 stop 之后的重启能观察到清理期间到达的 `pagehide`，保持挂起并等待 `pageshow`，而不会重新连接隐藏页面。
第十三处把每个入站 `CONTROL/SUBSCRIBE` 绑定到持久化 route：较早分配轮的迟到帧不能再让非 owner 订阅 transport，也不能在匹配的 `ROUTE_RELEASED` 之前确认悬挂交接；同一 route 的合法订阅与正常交接 ACK 保持不变。
第十四处修复 recovery gate 取消竞态：当 pagehide/stop 取代恢复周期时，已停靠在门上的操作会失效；紧随其后的显式 `start()` 可以在 replacement transport 上重建订阅，而旧 waiter 不能再于重启后重放并与该过程竞争出重复订阅。
第十五处修复显式 `start()` 取代自动恢复 timer 时停靠操作被静默丢弃的问题：取消 timer 不再释放 recovery gate；若该显式重开也失败，停靠操作会保留到下一次自动或按需恢复成功后再回放。
第十六处修复让被拒绝的 teardown 同时保留在两个失败账本中：`stop()` 仍会 resolve 并通过 `onError` 上报，但 recovery 账本会与统一的 `lastFailure` 记录保持同一次 stop 失败，直到显式 `start()` 同时重置两者。


## 0.20.91 已完成范围

可靠性开发线继续补强错误路径覆盖并维护发布门禁。IndexedDB replay 清理现在覆盖 `clear()`、`clearTopic()`、`clearBefore()` 的事务级错误，包括连接失效后的恢复，以及浏览器未提供 transaction error 对象时的领域级 fallback 拒绝信息。WebSocket transport 也会在连接失活后 best-effort 关闭 socket，避免自动恢复遗留死连接；已发布包验证的正常路径经审计确认没有固定等待或多余 registry 往返。Centrifuge token bridge provider 现在绑定到创建它的 client lifecycle，已被替换的 client 无法把迟到凭证请求送入新会话。旧 client 的 subscription 回调与 publish rejection 也已有回归约束，不能修改或上报到替代会话。
DataBus 生命周期审计现在还固定了 failed-reopen 的 stop gate 复用、被取代 initial open 的迟到 rejection 隔离、已退休 transport 迟到的 message/status/error 回调 generation 隔离，以及就绪/恢复契约（`stop()` 后迟到的 `pageshow` 不得重启后台工作、自动恢复再次失败必须通过 `ready()` 暴露、被取消的排队启动不得满足替代重启、排队重启在可用前被挂起必须让 `ready()` reject、旧 recovery timer 不得重开 transport）。本轮审计还发现并修复了一处真实定时器泄漏：`WorkerClusterRuntime.pause()` 在无 channel 时不再排定延迟 `channel.close()`。mutation check 已确认移除对应守卫时每条回归都会失败。本阶段还覆盖 failed-open 后 stop gate 复用、pagehide stop rejection 恢复、replay handler 分发隔离，以及 in-flight recovery reopen 复用；重开守卫已有 mutation 覆盖。

排队 transport 操作审计还固定了：在 initial open 尚未完成时排队的 `subscribe()`，其 rejection 必须恰好通过 `onError` 上报一次且不得成为 unhandled rejection；start gate 放行前不得触碰仍在 opening 的 transport。
DataBus 生命周期审计还固定了 initial `transport.start()` 被 `stop()` 取代后、teardown 等待期间才 reject 的 rejection ownership：原 `start()` 调用方看到自己的失败，`stop()` 仍成功，transport 恰好关闭一次，旧失败不会进入新 lifecycle 的 error ledger。
跨 Tab `EVENT` 边界现在会拒绝不是对象、或缺少字符串 `topic` 的 publication payload，因此一条畸形的同源帧不会再从 BroadcastChannel 监听器抛出并破坏后续投递。未知事件类型保持前向兼容；旧版 payload 会继承帧级 `originTabId`，payload 自带归属优先，两项行为均有回归固定。

## 0.20.90 已完成范围

本开发线修复 0.20.89 tag 首发暴露的发布管线问题，并统一文档中的投递语义。

- 发布门禁传播预算：阻塞式已发布包消费者验证从 24 × 5 s（2 分钟）提升为 48 × 7.5 s（6 分钟）。0.20.89 的 tag 首发发布成功之后仍在该门禁失败，因为 `npm pack` 在旧预算内始终返回 `ETARGET`；真正缺失的包仍会耗尽预算而失败。（已被 0.21.31 取代：上限改由两段实测延迟之和决定。）
- 投递语义：API、架构与能力文档现在一致说明有界的本地 fan-out 保证，并明确 SDK 不提供端到端的 at-least-once 或 exactly-once 投递；中文 API 参考已补充按 bus 可选启用的 `messageId` 去重窗口。
- 发布性能证据：基准趋势文档已基于 23 份归档报告刷新；最近两次运行对比未超过 50% 上限，两种 Worker 模式的单消息发布延迟均有改善。

## 0.20.89 已完成范围

本开发线延续异步回调隔离审计：以下每一项修复都把回调、排队微任务或 Promise 续体绑定到创建它的生命周期 generation，使被取代的会话无法写入其替代者。

- 异步 teardown 与重启边界：已 settle 的 `stop()` gate 不再吞掉后续 teardown（`stop → start → stop` 现在以停止态结束）；`getHealthSummary()` 对正在停止的 bus 报告 `state: 'stopped'`，不再与其已经发出的 `publish()` / `subscribe()` / `ready()` 拒绝语义自相矛盾。
- replay 持久化隔离：微任务排队的 batch flush 与被排队的 retention cleanup 会在 `suspend()` / `stop()` 取代其 generation 后被丢弃，已停止会话的历史无法再写入 durable store。
- durable hydration 取消：在 `suspend()` 或 `stop()` 之后才 resolve 的 `load()` 不再向 teardown 已清空的缓冲区追加数据，并按生命周期取消上报，而不是记为持久化失败。
- trace 会话隔离：已停止的 trace reporter 保持惰性——旧会话排队中的 `asyncSink` 事件被丢弃，显式重启会清除停止标记，使其重新发出生命周期 `start`。
- transport 与 Worker 回调隔离：Centrifuge credential-provider 结果绑定到发起请求的确切 Worker/port/session；`CentrifugeSession` 的异步 client/subscription 回调在 `STOP` 或重新初始化后被忽略；来自已替换 WebSocket 连接的 `Blob` 二进制帧不再被当作新连接的帧派发。
- 回归安全网：seeded lifecycle fuzzer 现在覆盖 1_500 种交织，并断言最后一次显式意图为 `stop()` 的序列会以 `state: 'stopped'` 且无 live transport 结束。

## 0.20.88 已完成范围

- 启动失败恢复现在可重入：transport 在初始 `openTransport()` 尚未结算时同步上报 `error`，调用方可以从 `onStatus('error')` 或 `onError` 回调立即重试。失败 open 会先完成清理，重试建立新的生命周期，旧 rejection 不会重新污染已重置的失败账本。
- 初始 transport open 尚在飞行时反复发生 BFCache `pagehide`/`pageshow`，不再让 bus 永久停留在挂起状态。suspend 只在旧 stop gate 仍代表当前生命周期时复用；否则安装新的串行 stop，使下一次 resume 真正重开 transport。
- 显式 `start()` 现在是完整的 BFCache 恢复路径：除 transport 外还会恢复跨 Tab 协调，并重新启动 `pagehide` 暂停的 trace metrics、dedup 过期清扫与 replay retention 定时工作。

## 0.20.87 已完成范围

- transport 恢复与就绪加固：原生 WebSocket 后端现在遵守 `DataBusTransport.start()` 契约（仅在 `open` 后 resolve，握手失败或超过 `connectTimeoutMs` 时 reject），自动恢复会真正创建替代 socket，`getHealthSummary()` 跟随 live transport 状态而不是 `transportReady` 诊断标记。
- 不再把操作写进已经消失的连接：恢复门会把 `subscribe()` / `publish()` 停放在自动与按需重开之后（包括自动尝试失败但仍保留预算的情况）；在真实连接之后出现的干净 `disconnected` 现在会触发一次按需重开，而不是交给已关闭的 socket；而异步上报连接状态的 worker 型后端仍保有其「尚未连接」窗口，不会被动重开。
- 生命周期与 `ready()` 边界修复：BFCache 挂起期间 `ready()` 会 reject；transport 自身 `stop()` reject 或抛错时 `stop()` 仍能 resolve；一次打开失败在两个恢复账本中只打一次时间戳；运行期 transport 错误会进入恢复账本；被取代的异步打开不再拆除更新的 suspend/resume 转换。
- 适配器与工具链：React/Vue `useCrossTabHealth` 在 `intervalMs` 变化时无需重建 bus 即可生效；`vitest` 及其 coverage-v8 provider 升级到 5.0.1 补丁版。

## 0.20.86 已完成范围

- 加固显式 stop/start 边界的生命周期：排队重启与进行中的 stop 串行化，并会被更新的 stop 取消，可通过 `ready()` 观测；被取代的异步开启不再拆除更新的 suspend/resume 转换；stop 期间调用的 `subscribe()` 与非空 `publish()`/`publishBatch()` 改为通过 `onError` 上报，不再修改 teardown 状态或被静默丢弃。
- 显式 `start()` 在自动恢复预算耗尽后，现在会执行文档所述的手动恢复，同时保留 cluster 状态、订阅与回放历史。
- IndexedDB replay 持久化在事务 abort（包括连接丢失导致的 abort）时结算全部 mutation，串行队列不再永久阻塞；replay AGE 裁剪改为内存与持久化历史共用同一套位置无关策略。
- 配置参考在中英文中完整记录 replay/dedup 公共选项，并新增从声明派生的文档守卫；固定种子属性不变量覆盖 active-worker 选择与 rebalance target。

## 0.20.85 已完成范围

- 新增固定种子的属性测试套件（`tests/property.test.ts`）：针对纯热路径函数与有状态管理器，覆盖有限性/全函数性、与顺序无关的选择、循环安全的大小估算、publication topic/元数据有效性、`serializeError` 可克隆性，以及长时间随机操作序列下的 dedup/replay 上界。
- `effectiveWorkerLoad` 对损坏的存储基础负载保持全函数性——非有限值（JSON `1e999` → `Infinity`）不再泄漏进 owner 选择并重新引入数组顺序依赖。
- `approximatePayloadBytes` 增加深度上界，循环 payload（structured clone 会保留循环）不再使回放字节占用或自适应负载采样栈溢出。
- `serializeError` 始终产出可结构化克隆的结果；不可克隆的 context（函数/Symbol）会被丢弃，而不是让错误上报本身抛出 `DataCloneError`。
- 当 `pruneStrategy: 'age'` 未配置 `retentionMs` 时，回放历史重新受上界约束：内存环与 IndexedDB 记录均应用数量上限。

## 0.20.84 已完成范围

- 发布/CI 门禁由“文档约定”变为强制执行：`pnpm test:coverage`、`pnpm verify:compat`、`pnpm verify:pack` 进入 CI verify job，Release 工作流在发布前重跑 lint + compat + pack，两个 checkout 均拉取完整历史与 tag 以便解析 compat 基线。
- 协调恢复加固：交接 ACK 丢失、owner 崩溃或前任 owner 退出后，均由 worker-TTL 门禁的重新选举恢复，采用单写者与投影负载分摊；每次路由确认 / 迁移 / 恢复都会发出有界的 `reliability` trace 事件。
- 三个真实正确性修复：Vue `useCrossTabDataBus` 卸载泄漏（pending start 可能创建无人拥有的 bus）、自适应 dedup TTL 未在热路径生效，以及 `effectiveWorkerLoad` 会把损坏的存储 load 造成的非有限评分泄漏回 owner 选择。
- 产品 demo 可观测性：事件流渲染 reliability / subscription / coordination trace 事件，混沌开关在真实浏览器中演练丢 ACK 与崩溃恢复路径，配置面板显示当前混沌模式。
- 覆盖与工具链：`ReplayManager` / `DedupManager` 直接测试套件、transport 错误隔离与部分元数据覆盖、vitest 5（基准 API 已迁移）与 eslint 10 lint 配置；TypeScript 7 因 typescript-eslint 未支持而继续递延。

## 0.20.83 已完成范围

- 健康钩子的适配器边界用例、可归档对比的浏览器基准、与当前能力对齐的 README 特性清单。
- 一次大规模内部清理：全部运行时字符串字面量集中到 `utils/constants.ts` 并由之派生字面量类型，回放与去重从 `CrossTabDataBus` 拆分为自包含的 `ReplayManager` / `DedupManager`。
- demo 的「批量 10」publishBatch 按钮与 `/debug/wsstats` 帧计数及单帧 E2E，`asyncSink: true` 投递语义文档，以及与阻塞式发布消费者校验对齐的发布检查清单。

## 0.20.82 已完成范围

- E2E 默认断言上限提升至 20 秒；结构化克隆拒收（Symbol）与 cause 保留覆盖；共享模式会话生命周期经示例服务的连接数端点端到端验证。

## 0.20.81 已完成范围

- 浏览器基准新增 data-bus 热路径矩阵，健康摘要纳入 E2E 端到端断言，storage-event 通道与传输层批量进入 API 文档与能力矩阵。

## 0.20.80 已完成范围

- E2E 可靠性治理：失败 trace/视频与更长保留期、reload 类用例的先收敛后发布模式、符合文档保证的错峰突发模式，以及架构文档中的丢失与恢复矩阵。

## 0.20.79 已完成范围

- 已发布包消费自检成为 release 阻塞门禁（重试预算提升至 24 × 5 秒）；丢失 ACK 交接的恢复链（TTL 清理 + 恢复后重选举）由回归固化。

## 0.20.78 已完成范围

- E2E 逐 Tab 断言真实 transport 后端；延迟关闭不变量由回归测试固化并写入架构文档；getting-started 覆盖健康摘要与协调降级。

## 0.20.77 已完成范围

- 修复无 factory 时静默降级本地会话的缺陷（打包的 Worker 现在真正被使用）；补充默认后端与通道丢失恢复覆盖；demo 展示协调通道诊断与降级开关。

## 0.20.76 已完成范围

- 面向无 BroadcastChannel 环境的 opt-in storage-event 协调降级通道，含降级通道上的 owner 选举集成测试，并同步降级文档与能力矩阵。

## 0.20.75 已完成范围

- 单测套件新增热路径性能门禁（宽松阈值防灾难性退化，真实基准仍在 `pnpm bench`）；IndexedDB replay 持久化新增脚本化故障注入，覆盖 invalidate 与恢复错误路径；审计确认 Release workflow 已集成已发布包消费自检。

## 0.20.74 已完成范围

- 可选的 `DataBusTransport.publishBatch`：WebSocket transport 单帧批量发送，demo server 支持批量帧，无批量能力的 transport 自动回退逐条发送；React/Vue 新增 `useCrossTabHealth` 绑定；健康判定纳入 transport 实时状态。

## 0.20.73 已完成范围

- IndexedDB replay 持久化适配器纳入单测（基于 `fake-indexeddb`）：覆盖裁剪策略、批量分组、并发串行化、清理语义与瞬时打开失败恢复。
- 真实浏览器 E2E 新增三 Tab 并发发布突发与整连接重构建（stop/start）重入集群两个场景；architecture 文档新增稳定性不变量参考（中英文）。

## 0.20.72 已完成范围

- 扩展基准矩阵，覆盖 `publishBatch`、wildcard routing、dedup、replay prune、批量持久化与异步 trace sink。
- 长时稳定性加固：补齐 handoff ACK 世代校验、BFCache 往返、恢复耗尽重置、存储写退避恢复与 replay 持久化清理竞态的回归测试；修复反向的 stale-ACK 世代比较与批量 flush 复活清理历史两处缺陷。
- 生产能力：`getHealthSummary()` 就绪判定、`getPersistenceStats()`、diagnostics 中 transport 状态细化（status/suspended），以及构建时注入的 SDK 版本。

## 0.20.71 已完成范围

- 新增可选 `appendBatch` replay 持久化接口；IndexedDB 对发布突发进行事务合并，旧适配器保持兼容。

## 0.20.70 已完成范围

- 在统一 diagnostics 中新增 SDK 版本与 transport/backend 身份，便于支持包与健康面板使用。

## 0.20.69 已完成范围

- 在 cluster snapshot 与 diagnostics 中新增 peer 协议能力发现。当前 runtime 广播协议版本 1；旧 peer 显示为 `null`。

## 0.20.68 已完成范围

- 为集群帧和 worker snapshot 增加协议版本元数据，并保持旧版本缺失字段时的兼容处理。

## 0.20.67 已完成范围

- 新增未知协议消息计数与最近类型诊断，并纳入 `getDiagnostics()`，同时保持旧 runtime 安全忽略未知消息。

## 0.20.66 已完成范围

- IndexedDB replay persistence 新增可选 `pruneStrategy` 与 `retentionMs`，与内存 replay 使用一致的裁剪语义。

## 0.20.65 已完成范围

- 新增可选 dedup 自适应 TTL 与 replay `pruneStrategy`（`count`、`age`、`both`），保留旧默认行为。

## 0.20.64 已完成范围

- 新增 trace `asyncSink` 选项，将 sink 投递合并到 microtask，保持事件顺序与错误隔离，并新增回归测试（339 个单测）。

## 0.20.63 已完成范围

- 新增可选 `onUnknownMessage` 钩子；旧 runtime 遇到未来 cluster message variant 时安全忽略，不抛异常，并增加回归测试（338 个单测）。

## 0.20.62 已完成范围

- 新增 `CrossTabDataBus.getDiagnostics()`，整合 status、transportReady、recovery、dedup、replay 用量与 cluster 快照，便于健康检查与运行时观测。

## 0.20.61 已完成范围

- 在每条 `DataBusMessage` 与 cluster `EVENT` 线帧上加入 `originTabId?: string`，让跨 Tab 的 replay 历史能归属到产出的 Tab。
- `WorkerClusterRuntime.broadcastEvent()` 默认把 `originTabId` 设为当前 runtime 的 `tabId`，`CrossTabDataBus.handleTransportMessage` 在广播前 stamp `originTabId = cluster.tabId`，邻居与 IndexedDB 回放的晚加入订阅者都看到一致的归属信息。
- `onEvent` 处理器签名新增第四个 `originTabId?: string` 参数；既有调用点改用 `toMatchObject` 以避免额外参数破坏严格相等。
- 新增 `CrossTabDataBus cross-tab replay consistency contract` 单元测试，覆盖生产端 stamp、本地 handler 一致性、写后晚加入与本地来源回放路径（336 个单测，11 个 e2e 测试）。

## 0.20.60 已完成范围

- 在 `CrossTabDataBus` 与 `WorkerClusterRuntime` 上新增 `publishBatch(topic, items)`，让调用方把多条 item 合并进单次 BroadcastChannel postMessage。每条 item 的 `messageId` / `timestamp` 在传输后保留，dedup / replay / 顺序仍按 item 维度生效；空 batch 为 no-op，单 item batch 直接走 `publish()`。基准用例 `publishBatch / 1000 messages / 10 per call` 为突发路径提供上限参考（332 个单测）。

## 0.20.59 已完成范围

- 扩展 `CrossTabDataBus.getRecoveryStats()`，新增 `generation` 与 `lastSuccessAt`，用于诊断 transport 的完整开启历史。

## 0.20.58 已完成范围

- 把 publish 路径的 route-owner 缓存加上可配置 LRU 上限（默认 256），并在 `WorkerClusterRuntime.getSnapshot()` 上暴露 size/max/hits/misses 诊断信息。
- 修复了一个远程 owner 的 publish 正确性 bug：此前 `wildcardPublishCache` 的 `null` 项会提前 return，导致没有本地 wildcard 订阅的 topic 不再走 route-owner 查找；现在会正确转发到远程 owner。
- 新增 LRU 淘汰、TTL 失效、owner 迁移和远程 owner 命中缓存的单元测试（319 → 321 个单测）。

## 0.20.57 已完成范围

- 新增 warm/cold route-cache publish 基准，便于衡量 owner 路由的性能。

## 0.20.56 已完成范围

- 为 publish 路由引入带 generation 比对的 route-owner 缓存，并在生命周期关闭时清空。

## 0.20.55 已完成范围

- 新增具备生命周期安全失效机制的 wildcard publish 判定缓存；基准结果仍需进一步稳定后再宣称吞吐提升。

## 0.20.53 已完成范围

- 扩展恢复诊断，新增安全且可序列化的 `errorMessage` 摘要。

## 0.20.52 已完成范围

- 扩展 `getRecoveryStats()`，新增 `hasError`，可观测当前仍保留的 transport 错误状态。

## 0.20.51 已完成范围

- 新增公开恢复状态快照 API：`getRecoveryStats()`。

- 新增 owner handoff 后取消订阅覆盖，验证存活 tab 取消后不会重建路由。

- 新增 reconnect 前取消订阅覆盖，验证已移除 topic 不会在恢复后重放。

- 新增多 topic 恢复覆盖，验证 reconnect 后每个 topic 都只恢复一次。

- 新增长时 reconnect flapping 覆盖，验证重放保持有界且无重复。

- 新增重复 worker 能力探测覆盖，验证 auto backend 选择在多次探测中保持确定性。

## 0.20.45 已完成范围

- 新增 WebSocket 多轮 error/restart 覆盖，验证始终只有最新连接保持活跃。

## 0.20.44 已完成范围

- 新增生命周期契约回归，验证旧 WebSocket 的 close/error 回调不会影响重启后的会话。

## 0.20.43 已完成范围

- 新增 WebSocket 多轮 stop/start 清理覆盖，验证订阅集合和旧回调会在连续生命周期切换中清除。

## 0.20.42 已完成范围

- 新增 Dedicated Worker 多轮 stop/start 清理覆盖，验证 STOP 边界会在连续生命周期切换中隔离旧 worker 投递。

## 0.20.41 已完成范围

- 新增 SharedWorker 多轮 stop/start 资源 soak，验证每轮都会释放监听器和 heartbeat 定时器。

## 0.20.40 已完成范围

- 新增 auto worker mode 多轮失败与恢复覆盖，验证持续优先使用 SharedWorker，且旧 port 的消息不会在连续重开后投递到会话。

## 0.20.39 已完成范围

- 新增 Dedicated Worker 多轮失败与恢复覆盖，验证只有最新 worker 可以投递消息。

## 0.20.38 已完成范围

- 新增 SharedWorker 多轮失败与恢复覆盖，验证只有最新 worker port 可以投递消息。

## 0.20.37 已完成范围

- 新增多轮 WebSocket stop/start 替换覆盖，验证多个旧 socket 的迟到回调都不会泄漏到最新会话。

## 0.20.36 已完成范围

- 新增 1,000 条高频 publish burst 回归，验证本地 owner 快路径保持消息顺序且不读取 storage。

## 0.20.35 已完成范围

- 新增 publish 与 receive/dispatch 热路径基准，和 routing、cluster 协调基准一起建立可重复的吞吐基线。

## 0.20.34 已完成范围

- 新增 transport 故障恢复组合回归，覆盖自动重开/重新订阅、replay 历史、重复抑制、持久化 append 边界和晚加入 handler 顺序。

## 0.20.33 已完成范围

- 新增基于 tag 的发布兼容检查，验证公开 exports、ESM/CJS 条件和类型元数据不会被升级移除。

## 0.20.32 已完成范围

- 新增 replay/dedup 组合覆盖：历史 hydration、恢复后实时消息、重复抑制、持久化 append 边界和晚加入 handler 顺序。

## 0.20.31 已完成范围

- WebSocket 在 stop/start 替换连接后按 socket 身份隔离生命周期和消息回调。
- 新增旧连接迟到事件不会进入新会话的回归覆盖。

## 0.20.30 已完成范围

- 将 IndexedDB replay 持久化 E2E 扩展到 BFCache、stop、reload、异步 hydration、有序历史和 replay 标记的连续生命周期组合。

## 0.20.29 已完成范围

- 新增 Dedicated/Shared/auto worker backend 在全部能力组合下的穷举降级矩阵覆盖。

## 0.20.28 已完成范围

- 将打包消费者验证扩展为发布兼容矩阵，覆盖包元数据、ESM/CJS exports、声明文件和全部公开子路径。

## 0.20.27 已完成范围

- 新增真实 Chromium 连续 BFCache/reload/owner handoff soak，验证 reconnect 就绪状态以及多轮生命周期切换中的跨 Tab exactly-once 投递。

## 0.20.26 已完成范围

- 新增 trace 隐私字段、模式隔离、sink 异常、reliability 事件 schema、有界状态和生命周期 metrics 窗口回归覆盖。
- trace reporter 在 stop 后保持静默，必须显式重新 start 才会开启新窗口。

## 0.20.25 已完成范围

- 新增协议兼容性覆盖：旧版与嵌套 WebSocket/Centrifuge publication 帧、未知字段、非法可选元数据和未知 worker 消息。

## 0.20.24 已完成范围

- 新增 replay/dedup 组合长时回归，覆盖 TTL 过期、静默周期 sweep、durable retention 清理、异步 hydration 与定时器停止。

## 0.20.23 已完成范围

- 新增双 Tab BFCache 与 transport error 接管回归，覆盖 owner 接管、返回恢复和无重复消息投递。

## 0.20.22 已完成范围

- 显式 stop/restart 边界会重置恢复尝试与 exhausted 诊断状态，让新会话从干净序列开始。

## 0.20.21 已完成范围

- 同一失败恢复序列中的 exhausted 诊断只发一次，成功重连后会重置。

## 0.20.20 已完成范围

- 自动恢复达到配置的次数上限时会发出 `exhausted` 诊断事件，不再静默停止。

## 0.20.19 已完成范围

- 可通过 `recovery.maxAttempts` 限制自动恢复次数，同时保留显式订阅需求触发恢复的路径。
- 新增次数上限校验与恢复上限序列回归覆盖。

## 0.20.18 已完成范围

- 自动 transport recovery 的冷却时间可通过 `recovery.cooldownMs` 配置。
- 新增配置校验与 fake-timer 边界回归覆盖。

## 0.20.17 已完成范围

- 连续 transport recovery 诊断现在带有单调递增的尝试编号，成功重连后会重置序列。
- 新增多次失败恢复序列的回归覆盖。

## 0.20.16 已完成范围

- transport recovery trace 现在区分 `scheduled`、`succeeded`、`failed` 结果，便于诊断重连失败。
- 新增失败恢复后成功重试的回归覆盖。

## 0.20.15 已完成范围

- 新增 transport 状态抖动覆盖，验证重复 `connected`/`disconnected`/`error` 通知不会造成订阅重复或丢失。

## 0.20.14 已完成范围

- Release 仅在发布后消费者诊断失败时保留明确 summary，不再阻断已经成功的发布；本地验证仍保持严格失败。

## 0.20.13 已完成范围

- 浏览器 CI 失败时自动保留 Playwright report 和 test-results artifact，便于事后定位 runner flaky 问题。

## 0.20.12 已完成范围

- 发布消费者验证在 CI import 失败时输出包目录和 peer 依赖链接诊断，便于定位仅在 runner 出现的问题。

## 0.20.11 已完成范围

- Release 按 tag 串行执行，并始终把 npm 版本、tag 和 commit 写入 GitHub step summary，便于定位 registry 或 workflow 失败。

## 0.20.10 已完成范围

- 新增多 topic 重复 reconnect 回归覆盖，确保恢复期间每个已分配 topic 恰好重放一次，避免 transport 订阅重复或丢失。

## 0.20.9 已完成范围

- 发布包验证同时接受 semver 和带 `v` 的 tag 输入，确保 release 触发检查与本地命令一致。

## 0.20.8 已完成范围

- 发布后验证会在 npm registry 传播期间轮询 tarball，降低刚发布时的误报失败。
- GitHub Release 在发布或跳过发布后，都会验证当前 tag 对应版本的 ESM/CJS 消费者；手动 npm 发布也适用。

## 0.20.7 已完成范围

- 新增 `pnpm verify:published`：从 npm 下载已发布包，并在干净临时消费者中验证主入口、hooks、Vue、Centrifuge 的 ESM/CJS 导入。
- 支持通过 `PUBLISHED_VERSION` 指定版本；未指定时跟随 npm 当前版本。

## 0.20.6 已完成范围

- 新增中英文发布检查清单，覆盖本地验证、打包消费者、打 tag、手动 npm 发布和发布后验证。
- 明确 npm 历史版本不可覆盖；缺失的历史版本必须从对应 git tag 重建。

## 0.20.5 已完成范围

- 新增公开消费者冻结覆盖：主入口、hooks、Vue、Centrifuge 子路径的 ESM 与 CommonJS 双格式消费。
- 校验发布包中的声明文件存在，并包含 replay、dedup 和 publication metadata 等关键类型。

## 0.20.4 已完成范围

- 新增真实 Chromium 多 Tab soak 场景，覆盖重复 fan-out、owner migration、BFCache 往返、reload 恢复和无重复投递。
- 将浏览器生命周期转换串成一个连续会话，能够捕获孤立用例难以发现的 timer 与 route 清理回归。

## 0.20.3 已完成范围

- 新增 React 动态 topic 生命周期覆盖。
- 验证 topic 替换会移除旧订阅后再接收新 topic，且通过 WebSocket hook 链路生效。

## 0.20.2 已完成范围

- 新增协议恢复回归：malformed binary/text frame 之后，合法 WebSocket publication 仍可继续投递。
- 将二进制截断、JSON 解析失败、nested envelope 和错误隔离串成一个兼容性序列验证。

## 0.20.1 已完成范围

- 新增 persistence mutation sequence soak 覆盖：hydration、重试恢复、topic 清理、后续 append 和全量清理。
- 验证串行 persistence 操作在瞬时失败后仍可继续使用。

## 0.20.0 已完成范围

- publication envelope 兼容性已覆盖 legacy、嵌套、fallback topic、原始 payload、metadata 和未知字段帧。
- 缺失或空 topic 会统一拒绝，同时继续支持 transport 提供的 fallback channel。

## 0.19.9 已完成范围

- 新增 dedup 与 replay/persistence 组合回归覆盖。
- 被 dedup 抑制的 publication 不会污染 replay 历史；TTL 到期后同一 message ID 可以再次被接受。

## 0.19.8 已完成范围

- IndexedDB replay adapter 在事务或请求失败后会使缓存连接失效。
- 已关闭或不可用的连接可沿用现有 persistence retry 路径恢复，不需要重建 adapter。

## 0.19.7 已完成范围

- IndexedDB replay adapter 在 open 失败后会丢弃 rejected promise，下一次操作可重新打开并恢复。
- 与既有跨 tab `versionchange` 连接重置行为保持兼容。

## 0.19.6 已完成范围

- IndexedDB replay adapter 遇到跨 tab `versionchange` 时会关闭旧连接，并在下一次操作时重新打开。
- schema 变化后不会继续复用失效数据库连接。

## 0.19.5 已完成范围

- React bus effect 在 StrictMode 和快速依赖切换下使用 generation 保护。
- 过期的异步清理不会覆盖最新的 active bus。

## 0.19.4 已完成范围

- Vue bus 在快速 reactive 依赖切换时使用 generation 保护重建流程。
- 过期的异步生命周期完成不会重新挂回旧 bus 实例。

## 0.19.3 已完成范围

- 新增可选 `dedup.sweepMs`，在安静期间定时删除过期 message ID。
- sweep 定时器遵循 DataBus 生命周期，默认关闭。

## 0.19.2 已完成范围

- `stop()` 和 pagehide 挂起边界会取消待执行的 persistence retry。
- 已取消的 retry 不会再次调用 adapter，也不会被当作持久化失败上报。

## 0.19.1 已完成范围

- WebSocket 二进制 publication 除 `ArrayBuffer` 外，也支持浏览器常见的 `Blob` 帧。
- Blob 转换失败会通过 transport error callback 隔离报告，不会打崩消息回调。

## 0.19.0 已完成范围

- persistence retry 会发出有界、可选开启的 `persistence_retry` reliability 事件，包含操作名和尝试次数。
- 诊断覆盖 hydration、append、全量/Topic 清理以及 retention 清理，不暴露 payload 或错误正文。
- 保持既有重试时序、默认单次尝试、adapter 契约和最终错误处理兼容。

## 0.18.0 已完成范围

- 新增可选 replay persistence retry，支持有限尝试次数和指数退避。
- append、hydration、手动清理、topic 清理和 retention 清理统一使用恢复路径。
- 导出公开 retry 配置类型，同时保持 persistence adapter 契约兼容。

## 0.17.0 已完成范围

- 新增可选的周期性 replay retention sweep，即使没有新 publication 也能清理 durable history。
- sweep 定时器遵循 start/resume 与 pagehide/stop 生命周期边界。
- 补充 fake-timer 覆盖，保护清理调度、销毁和非法配置行为。

## 0.16.0 已完成范围

- publication burst 期间会合并 retention cleanup，并以最新 cutoff 串行执行持久化 mutation。
- WebSocket binary 协议边界新增截断帧和非法帧回归覆盖。
- 继续保留并明确 legacy replay、JSON metadata 和手动清理的兼容保证。

## 0.15.0 已完成范围

- replay retention 会保留没有显式 timestamp 的 legacy 消息，只清理早于 cutoff 且带显式 timestamp 的记录。
- trace 时间戳支持注入 `trace.now`，与已有的 dedup 时钟注入保持一致。
- 补充 replay 清理、诊断和适配器行为的兼容性与生命周期回归覆盖。

## 0.14.0 已完成范围

- Vue composable 在同一个 bus 上切换 reactive topic 时会正确重绑。
- 跨页面 replay mutation 顺序与生命周期保证已写入文档并有回归测试。

## 0.13.0 已完成范围

- IndexedDB replay mutation 按 adapter 串行化，避免并发 append 的读改写丢历史。
- 完整 stop/restart 边界会清空 dedup 状态。
- 补充持久化失败诊断和生命周期回归覆盖。

## 0.12.0 已完成范围

- dedup TTL 支持可注入时钟，生命周期与过期测试不再依赖墙上时间。
- replay 持久化 append、hydration、退订和 retention 清理失败均有结构化诊断。
- publication metadata 做兼容性归一化：只接受非空 ID 与有限 timestamp。
- 补充 legacy、嵌套、fallback topic 和坏 metadata 协议夹具测试。

## 0.11.0 已完成范围

- 当持久化适配器支持 `clearBefore` 时，通过 `replay.retentionMs` 自动执行持久化回放留存。
- 周期性 trace 指标中加入去重接受/抑制计数。
- 覆盖 WebSocket、Centrifuge、Worker 边界与浏览器 E2E 的 publication metadata 兼容性测试。
- Service Worker transport 决策：在目标浏览器具备稳定的连接生命周期契约前，刻意保持不实现。

## 0.20.69 候选

1. ~~增加 peer 能力矩阵，并在 diagnostics 暴露 SDK、后端与 transport 身份。~~ 已交付：`getDiagnostics()` 携带协议版本、未知消息统计、peer 协议版本与 transport 身份；`getHealthSummary()`/`getMetrics()` 现在也会随单一对象输出实时 trace 指标与 sink 状态。
2. ~~统一 replay、dedup、trace、recovery 与 cluster 健康指标。~~ 已交付：`getDiagnostics()` + `getMetrics()` + `getHealthSummary()` 在单一快照中覆盖生命周期、恢复、dedup、replay、持久化、协议、transport、cluster、trace 指标与 sink 背压。
3. ~~优化 IndexedDB 并发 append 与清理路径。~~ 已交付：相邻 `appendBatch` 变更合并为单事务；`clear`/`clearTopic`/`clearBefore` 顺序保持。
4. ~~增加 adaptive dedup、async trace、prune 和长时多 Tab 性能基线。~~ 已交付：bench 覆盖负载加权评分、`getMetrics`、publishBatch 批次敏感性、adaptive dedup TTL、age 策略 replay prune、双 worker 跨 Tab fan-out，以及既有 dedup/async-sink/persistence 用例。

## 0.13.0 候选

1. ~~冻结公共导出面与 transport 无关的 publication 信封。~~ 已交付：根导出面由回归测试与 tag 间 `verify:compat` 门禁固定，transport 无关的 publication 信封（含 legacy/nested 帧兼容）已有文档与协议 fixture 覆盖。
2. ~~精确记录 at-least-once 投递与去重保证。~~ 已交付：architecture/API/capability 文档现在明确区分「每条已接受 transport publication 的本地至多一次扇出」与端到端投递，记录 transport/服务端的丢失与重复投递，并说明有界、可选 `messageId` 去重的边界，不再宣称 at-least-once 或 exactly-once。
3. ~~增加长时浏览器浸泡覆盖：replay 留存、重连、BFCache 与 owner 迁移。~~ 已交付：真实 Chromium 多 Tab 浸泡与重复 BFCache/reload/owner-handoff 场景在完整生命周期上连续运行。
4. ~~为 1.0 前的协议别名发布迁移指南与弃用策略。~~ 已交付：弃用策略位于发布清单，「Upgrading & Deprecation」指南已在两语言 getting-started 发布，legacy 协议帧在一个小版本内继续解析，当前版本通过 `getDiagnostics().protocol` 暴露。

## 更长期候选

1. **Replay 生命周期与留存**：增加持久化 `clear`/`clearTopic`，退订/替换时清理旧历史，并通过 trace 与 error handler 暴露持久化失败；
2. **可靠性诊断**：增加恢复/重试、owner ack、路由迁移结构化事件，元数据有界且默认关闭 trace；
3. **发布去重**：设计并实现可选、有界的 message-ID 窗口，覆盖本地分发、BroadcastChannel、WebSocket 与 replay，默认行为保持不变；
4. **适配层与协议对齐**：统一 React/Vue 生命周期和类型契约，补充二进制帧与恢复语义文档，并增加自定义 transport 兼容夹具；
5. **运维验证**：扩展浏览器和打包消费测试，增加去重/恢复/replay 清理回归基准，Push CI 继续作为发版门禁。
6. **TypeScript 7 工具链迁移**：阻塞在上游而非本仓库——2026-09-25 重新对着 registry 查过：`typescript-eslint` 最新发布 8.70.1 与最新 canary 8.70.2-alpha.7，两者的 peer 都仍是 `typescript >=4.8.4 <6.1.0`，所以装上 TypeScript 7（最新 7.0.2）会先让 lint 门禁失败，还轮不到我们自己的类型；两条发布通道里都不存在已经接纳 7.x 的版本可供试装。查最新版时请读 `dist-tags.canary`，不要把版本字符串排序：按字符串排会把 `8.9.1-alpha.9` 排到最后，因为 `9` 排在 `70` 之后，而真正需要盯的那条 alpha 线是 8.70.x。等任一条通道放宽上限后，再在独立分支迁移（TS 7 仍提供 `tsc` 可执行入口，`pnpm typecheck` 本身无需改动）。

## 发版检查清单

- 写好本次发布自己的 `## [x.y.z] - 日期` 一节（仓库已不再维护 `[Unreleased]` 段——版本标题在变更自身的 PR 里开出，权威流程见 `docs/zh/release-checklist.md`）；
- 执行 `pnpm check`、`pnpm lint`、`pnpm test:e2e`、`pnpm bench`、`pnpm bench:browser`；
- 从打包 tarball 做 ESM/CJS 消费冒烟验证；
- 推送 tag 后核验 GitHub Release 与 npm `latest` dist-tag。
